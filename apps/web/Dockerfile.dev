# Web 开发镜像：预装依赖，源码通过 volume 挂载
FROM node:24-alpine

WORKDIR /repo

COPY package.json package-lock.json ./
COPY apps/api/package.json apps/api/
COPY apps/web/package.json apps/web/

RUN npm ci

WORKDIR /repo/apps/web

CMD ["npm", "run", "dev"]
