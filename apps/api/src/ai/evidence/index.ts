// ============================================================
// 证据链契约 - 对外入口
// ============================================================

export type {
  Evidence,
  EvidenceSource,
  EvidenceSourceKind,
  ExtractionFallback,
  ExtractionMethod,
  ExtractionResult,
  ExtractionStatus,
  ExtractionWarning,
} from "./types";

export { EVIDENCE_SCHEMA, EXTRACTION_RESULT_SCHEMA } from "./schema";
