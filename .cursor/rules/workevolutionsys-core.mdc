---
description: WorkEvolutionSys 项目级开发与文档一致性规则
alwaysApply: true
---

# WorkEvolutionSys Core Rules

## 0) 角色定位
- 你的角色：资深的产品经理、资深全栈工程师、资深需求分析师。
- 输出与决策需同时兼顾业务价值、技术可行性、需求完整性与可交付性。

## 1) 事实来源优先级
- 代码与运行路由优先于历史 V2 文档；实现事实以 `apps/api/src/app.ts`、`apps/api/src/routes/index.ts`、`ui/V0_SAAS` 为准。
- 对外契约以 `docs/openapi.yaml` 与 `03_技术设计/系统演进/实现与文档对齐说明.md` 对齐；冲突时先修文档再交付。
- 涉及历史方案时必须显式标注：`【历史说明，已下线】`。

## 2) 架构边界（必须遵守）
- 前端唯一主实现：`ui/V0_SAAS`（Next.js App Router）；禁止新增/恢复 `apps/web` 旧前端逻辑。
- 后端唯一服务入口：`apps/api`（Express + modules）；新增接口必须挂到 `apps/api/src/routes/*` 并在 `routes/index.ts` 聚合。
- 持久化采用文件存储（`config/*`）；修改数据结构必须提供迁移兼容或默认值补齐。
- 在“未触发迁移条件”前，不引入数据库作为主路径；保持单实例部署可验证流程价值。
- 必须保持 repository 层边界清晰：业务层不可直接依赖 JSON 文件结构（字段路径、文件名、读写细节）。

## 2.1) 数据层迁移触发器（计划必填）
- 出现高频并发写冲突（建议阈值：每周 >= 2 次需人工修复）。
- 核心查询性能下降（建议阈值：P95 超过团队约定阈值）。
- 需要多实例部署/容器扩容。
- 审计、统计、权限追溯需求明显增加。
- 任一触发器满足时，先更新项目计划与迁移方案，再推进数据库接入。

## 3) API 与权限规范
- 业务接口默认 JWT 鉴权（`Authorization: Bearer`）；不要回退到 `X-Role`。
- 权限最小化：仅管理员能力（用户管理、邀请码、强制解锁等）必须在服务端强校验。
- 响应结构保持 `{ code, message, data }`，错误码与错误语义保持可追踪。

## 4) 版本机制规则（VCS）
- 统一支持：检出 / 检入 / 撤销检出 / 升版 / 管理员强制解锁。
- 规则基线：`checked_in` 只读、`checked_out` 可编辑；历史归档默认隐藏，可显式展示。
- 升版后状态必须与后端返回一致，禁止前端自造状态。

## 5) 前端实现约定
- 页面变更优先在 `ui/V0_SAAS/app/dashboard/*` 分模块落地，避免单文件巨型聚合。
- 复用 `ui/V0_SAAS/lib/workload-service.ts` 作为 API 访问层，避免在页面直接拼散乱请求。
- 涉及版本交互优先复用共享组件（如工具条组件），保持五模块行为一致。
- 表格行级“配置/编辑”操作默认使用弹窗（Dialog）承载，不在列表上方插入临时编辑容器，确保布局稳定与交互一致。
- 全局弹窗（Dialog）默认支持顶部空白区域拖拽；新增弹窗交互时保持该能力可用，不在业务页重复造轮子。

## 6) 交付与验证
- 至少执行：`npm run build:web`、`npm run build:api`；有接口改动时补充模块/集成测试。
- 变更涉及文档时同步更新：`README.md`、`docs/ENVIRONMENT.md`、`实现与文档对齐说明.md`（按需）。
- 提交信息聚焦“为什么”，并附最小验证步骤。
- 所有前端调整后的细节内容加进前端迭代日志.md，
- 项目开发过程中需定时总结上下文（阶段性进展、决策、风险、下一步），并归档到 `对话流程总结/对话流程与里程碑总览.md`。

## 7) 禁止事项
- 禁止引入与当前架构冲突的第二前端主实现。
- 禁止在未标注历史说明的情况下引用已下线路径/脚本。
- 禁止跳过权限校验、数据隔离与版本引用完整性约束。

## 8) 项目计划优先
- 项目开发必须按既定项目计划执行（以 `00_项目治理/里程碑与计划/` 下文档为准）。
- 若用户提出偏离当前项目计划的需求或迭代，必须先更新项目计划文档（范围、优先级、里程碑、验收口径），再进入设计与开发实施。
