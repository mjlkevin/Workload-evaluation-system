# 前端迭代日志

> 项目：WorkEvolutionSys  
> 范围：`ui/V0_SAAS`  
> 目的：记录本轮前端交互与视觉调整细节，便于回溯与继续迭代。

## 2026-04-05 调整记录

### 1) 仪表盘评估方案列表：行点击选中高亮

- 目标：用户点击表格数据行后，能明确区分“当前选中行”。
- 修改文件：`ui/V0_SAAS/app/dashboard/page.tsx`
- 实现细节：
  - 基于 `selectedGlobalVersionCode === row.globalVersion` 计算选中态。
  - 为行增加 `data-state="selected"` 与 `aria-selected`。
  - 增加选中样式：`data-[state=selected]:bg-primary/10`，hover 下为 `data-[state=selected]:hover:bg-primary/15`。
- 结果：单击即高亮，双击打开预览逻辑保持不变。

### 2) 全局卡片视觉柔化（减少“白亮刺眼”）

- 目标：降低卡片背景亮度，缓解长时间查看表单的视觉疲劳。
- 修改文件：`ui/V0_SAAS/app/globals.css`
- 实现细节：
  - `--card: #ffffff` -> `#f5f7fa`
  - `--popover: #ffffff` -> `#f7f9fc`
- 结果：`Card` / `Popover` 相关容器整体更柔和，信息层级保持。

### 3) 需求模块：未选“需求版本号”时子卡片默认折叠

- 目标：在未选择版本回读时，降低页面噪音，突出版本选择操作。
- 修改文件：`ui/V0_SAAS/app/dashboard/requirement-import/page.tsx`
- 实现细节：
  - 新增子卡片折叠状态 `sectionCollapsed`（8 个分区）：
    - `basicInfo`
    - `valueProposition`
    - `businessNeed`
    - `devOverview`
    - `productModule`
    - `implementationScope`
    - `meetingNotes`
    - `keyPoints`
  - 新增 `createInitialSectionCollapsed()`，默认全部 `true`。
  - 监听 `selectedVersionCode`，当版本号为空时重置为默认折叠。
  - 各子卡片接入受控折叠：`collapsed` + `onCollapsedChange`。
- 结果：未选版本时默认折叠；选择版本后可按原交互双击展开/收起。

### 4) 需求模块：版本与上下文卡片固定展开

- 目标：确保版本选择区始终可见，不参与折叠。
- 修改文件：`ui/V0_SAAS/app/dashboard/requirement-import/page.tsx`
- 实现细节：
  - `版本与上下文` 卡片设置为：`collapsed={false}`、`collapsible={false}`。
- 结果：该卡片始终展开，双击不会触发折叠。

## 验证记录

- 构建验证：`npm run build:web`（通过）
- 代码检查：相关文件未发现新增 lints 错误

## 后续可选优化（未实施）

- 卡片柔化提供多档主题（轻柔 / 中性 / 沉稳）供用户切换。
- 需求模块折叠状态按版本号持久化（切换版本时恢复最近展开状态）。

---

## 2026-04-05 增量更新（同日续更）

### 5) 折叠卡片摘要可读性与布局优化

- 目标：提升折叠态信息识别效率，减少视觉跳变。
- 修改文件：`ui/V0_SAAS/components/ui/card.tsx`
- 实现细节：
  - 折叠态标题颜色调整为与状态标签同色系：`text-blue-700 dark:text-blue-300`。
  - 折叠摘要由“标题+描述上下两行”改为“同一行水平排列”。
- 结果：收起后的卡片信息更紧凑，标题与状态感知更一致。

### 6) 需求模块：版本区工具栏抽离与重排

- 目标：降低“版本与上下文”卡片负担，强化顶部操作入口。
- 修改文件：`ui/V0_SAAS/app/dashboard/requirement-import/page.tsx`、`ui/V0_SAAS/components/workload/version-vcs-toolbar.tsx`
- 实现细节：
  - 将“保存版本 + VCS 操作”抽离为描述文案下方独立工具栏。
  - 工具栏采用与页面卡片一致的风格（圆角、边框、阴影、轻磨砂）。
  - “版本与上下文”字段区调整为 `2 行 3 列`（总方案版本号/需求版本号/检出状态）。
  - 工具栏与版本卡片组合为局部分组，组内间距收紧呈现紧挨效果。
  - “版本与上下文”卡片改为紧凑尺寸：`gap-4 py-4`，`CardHeader` 间距降为 `space-y-2`。

### 7) 需求模块：检出状态展现策略调整

- 目标：状态信息位置更符合“字段区展示、工具栏操作”的认知。
- 修改文件：`ui/V0_SAAS/app/dashboard/requirement-import/page.tsx`、`ui/V0_SAAS/components/workload/version-vcs-toolbar.tsx`
- 实现细节：
  - 工具栏中的“检出状态”字段支持按页面关闭渲染（`showStatusField`）。
  - 检出状态字段转移到“版本与上下文”字段区中展示。
- 结果：工具栏更聚焦操作，状态信息回归上下文字段。

### 8) 需求模块：客户行业字段说明交互统一

- 目标：说明信息由“常驻文字”改为“按需查看”，并与字段风格统一。
- 修改文件：`ui/V0_SAAS/app/dashboard/requirement-import/page.tsx`
- 实现细节：
  - 将两段常驻说明改为标题旁 `?` 悬浮提示（Tooltip）。
  - 移除“说明”胶囊，仅保留小号问号入口。
  - 客户行业展示框边框样式与其他字段统一（`border-border/70 + bg-background/95 + shadow-sm`）。
- 结果：字段区更干净，帮助信息可按需获取。

### 9) 仪表盘评估列表：头部工具栏布局对齐需求模块

- 目标：解决选中行后标题挤压变形，并统一工具栏视觉语言。
- 修改文件：`ui/V0_SAAS/app/dashboard/page.tsx`
- 实现细节：
  - 头部改为“标题上方 + 工具栏下方”结构。
  - 工具栏容器对齐需求模块样式（圆角、边框、阴影、磨砂）。
  - 标题区加最小宽度与不压缩策略，避免窄宽度时文字竖排变形。

### 10) 仪表盘评估列表：检出状态下沉到表格字段

- 目标：工具栏仅保留动作，状态回归数据行语义。
- 修改文件：`ui/V0_SAAS/app/dashboard/page.tsx`
- 实现细节：
  - 关闭工具栏中的检出状态展示：`showStatusField={false}`。
  - 在列表表格新增“检出状态”列，按每行版本记录显示 `已检出/已检入`。

### 11) 仪表盘评估列表：行选中交互增强

- 目标：支持“点击选中，再次点击取消选中”。
- 修改文件：`ui/V0_SAAS/app/dashboard/page.tsx`
- 实现细节：
  - 行点击逻辑改为 toggle：
    - 选中同一行再次点击时清空 `selectedGlobalVersionCode`。
- 结果：用户可快速退出选中态，避免强制保持高亮。

### 12) 仪表盘：评估方案列表与上方「总评估人天」列右缘对齐（xl）

- 目标：宽屏下列表主卡片右缘与顶部四宫格第三张卡（总评估人天）右缘对齐，右侧快速操作/最近动态与第四列同宽。
- 修改文件：`ui/V0_SAAS/app/dashboard/page.tsx`
- 实现细节：
  - 第二段 `section`：`lg:grid-cols-3` 保持，`xl:grid-cols-4` + `xl:gap-4` 与顶部概览区 `xl:grid-cols-4`、`gap-4` 一致。
  - 列表 `Card`：`lg:col-span-2` 保持，`xl:col-span-3`。
  - 侧栏容器：`xl:col-span-1`。
- 结果：`xl` 断点下列表占前三列轨道，侧栏占第四列，与顶部指标卡列边界一致。

### 13) 未保存修改 Context 拆分，减轻离开重页时的卡顿

- 目标：实施评估等重页在「未保存拦截」触发时不再因 `pendingHref` / `isDirty` 变化整页重渲染。
- 修改文件：
  - `ui/V0_SAAS/hooks/use-unsaved-changes.tsx`：双层 Provider——稳定 `setDirty` 与导航状态分离；导出 `useSetUnsavedDirty`、`useUnsavedNavigation`。
  - `ui/V0_SAAS/components/app-sidebar.tsx`：改用 `useUnsavedNavigation`。
  - `ui/V0_SAAS/app/dashboard/assessment/page.tsx`、`dev-assessment/page.tsx`、`resource-cost/page.tsx`、`requirement-import/page.tsx`：改用 `useSetUnsavedDirty`。
- 结果：业务页只订阅稳定 `setDirty` 引用；侧栏单独订阅导航状态，离开/弹窗时重页不再被 Context 连带刷新。

### 14) 主页评估方案列表：删除方案（确认后调用接口）

- 目标：选中列表行后可通过「删除方案」二次确认删除当前总方案（global）版本记录。
- 修改文件：
  - `ui/V0_SAAS/lib/workload-service.ts`：新增 `deleteGlobalPlanVersion(versionCode)`，请求 `DELETE /api/v1/versions/global/{versionCode}?templateId=default`。
  - `ui/V0_SAAS/app/dashboard/page.tsx`：工具栏增加删除按钮（未选中时禁用）；`AlertDialog` 展示版本号与项目名称说明；确认后删除、清空选中并刷新列表。
- 说明：与后端一致，需 `operator/admin`；未选中时点击会提示先选中行。

### 15) 主页评估方案列表：工具栏右侧关键字搜索

- 目标：在列表工具栏右侧提供搜索框，按关键字过滤表格行。
- 修改文件：`ui/V0_SAAS/app/dashboard/page.tsx`
- 实现细节：
  - 状态 `planListSearch`，`displayPlanRows` 对 `planRows` 做不区分大小写子串匹配。
  - 匹配字段：序号、项目名称、各版本号、状态、创建/更新/审核时间、检出状态文案（已检出/已检入）。
  - 工具栏布局：左侧操作按钮组 + 右侧 `type="search"` 输入框（带搜索图标）；窄屏纵向堆叠，宽屏同一行两端对齐。
  - 无数据时区分「暂无方案」与「有关键字但无匹配」提示。

### 16) 主页「新建方案」：弹窗录入项目名称 + 流程引导

- 目标：不再在点击后立即落库刷新列表；先弹窗填写项目名称，并在弹窗内展示「需求 → 实施评估 → 资源人天及成本」引导；创建成功后列表上方提供带 `globalVersion` 的快捷入口。
- 修改文件：`ui/V0_SAAS/app/dashboard/page.tsx`
- 实现细节：
  - 「新建方案」打开 `Dialog`：项目名称输入（可空，与 `createGlobalPlanVersion` 一致走「未命名项目」）、底部「建议完成顺序」三步卡片（图标与侧栏模块一致）及说明文案。
  - 主按钮「创建方案」调用 `createGlobalPlanVersion(trimmed || undefined)`；创建中禁用关闭按钮与表单，避免误关。
  - 成功后关闭弹窗、选中新建总方案、`loadPlans()`；`postCreateGuide` 驱动列表上方高亮区，三个 `Link` 指向需求/评估/资源页并附带 `?globalVersion=`。
  - 支持 Enter 提交；「收起引导」清除快捷入口区。

### 17) 新建方案弹窗：四步流程顺序与步骤卡片排版

- 目标：流程为「需求 → 实施评估 → 开发评估 → 资源人天及成本」；避免横向挤窄导致「实施评估」「资源人天及成本」等标题错乱换行。
- 修改文件：`ui/V0_SAAS/app/dashboard/page.tsx`
- 实现细节：
  - `newPlanWorkflowSteps` 增加「开发评估」`/dashboard/dev-assessment`（`Code2` 图标），稳定 `id` 作 React key。
  - 弹窗内步骤改为**纵向列表** + 步骤间 `ChevronDown`，每步整行宽度，`break-words` / `leading-snug` 保证长标题可读。
  - 创建后快捷入口改为 `grid` 两列（窄屏单列），按钮 `h-auto`、`whitespace-normal`、标题 `break-words`。
  - 弹窗宽度维持 `sm:max-w-md`，以纵向布局为主不再强塞四列横排。

### 18) 实施评估页：模块评估工作台初始无默认勾选

- 目标：首次进入（未回读历史版本）时，不默认选中报价预置模式、云产品标签，且模板条目（SKU 勾选）均为未选。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：
  - `selectedPresetMode` 初值为 `""`；折叠摘要显示「未选择」。
  - 模板首次加载 / 切换模板：云产品 `[]`、条目 `included: false`（仍保留 `customStandardDays` 供后续勾选使用）；切换模板时清空预置模式。
  - 切换工作表：不再自动全选该表下云产品，改为清空云产品选择。
  - `applyPreset`：显式区分「全量云产品」与其它模式；空字符串不再误触发全选云产品。
  - 版本回读：`selectedPresetMode` 仅当 payload 中显式给出时还原，缺省为 `""`（与旧数据无该字段时行为一致）。

### 19) 实施评估：参数与版本卡片暂时关闭滚动自动折叠

- 目标：禁用窗口滚动触发的折叠与视差，避免参数卡片随页面滚动自动收起/浮动。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：移除 `scroll` 监听与 `paramCardFadeProgress`；展开态固定 `opacity: 1`、无位移缩放；仍保留卡片自带的手动折叠（`collapsed` / `onCollapsedChange`）。

### 20) 实施评估：指标卡 + 工作台与多组织分标签收纳

- 目标：将人天构成等四宫格与「模块评估工作台」与「多组织推广估算」收进同一外层 Card，用标签切换；导出历史仍在下方独立卡片。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：引入 `Tabs` / `TabsList` / `TabsTrigger` / `TabsContent`；`模块评估` 含原四指标卡 + 模块评估工作台；`多组织推广估算` 含原表格、新增组织与合计说明。

### 21) 实施评估工作台：模块报价 + 报价模式行

- 目标：预置方案与「自定义人天」从标题同行挪至「模块报价」下单独一行，并以「报价模式」归类。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：`CardHeader` 增加「模块报价」小标题；其下「报价模式」标签 + `flex-wrap` 按钮组（`size="sm"`）；工作表 / 云产品区块顺序不变。

### 22) 实施评估工作台：评估模式（工作表）在报价模式之上

- 目标：模板工作表切换（如「模块报价」「套件报价-…」）归类为「评估模式」，并排在「报价模式」上方。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：在「模块报价」下先「评估模式」+ `sheets` 按钮网格（`sm:grid-cols-2 lg:grid-cols-4`），再「报价模式」+ 预置与自定义人天；云产品标签行仍在下方。

### 23) 实施评估工作台：去掉标题「模块评估工作台」与「模块报价」

- 目标：卡片展开区不再显示上述两处文案；折叠摘要去掉「模块评估工作台」前缀，直接从工作表等信息开始。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`

### 24) 实施评估：模块评估 / 多组织标签选中态蓝色底

- 目标：`TabsTrigger` 选中时使用蓝色背景与白字。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：两个 `TabsTrigger` 增加 `data-[state=active]:bg-blue-600`、`text-white` 及 dark 下相同覆盖。

### 25) 实施评估：评估模式工作表按钮宽度随文案收缩

- 目标：工作表切换按钮不再被 `grid` 列拉满整格，选中/未选中均按内容宽度显示，整体更紧凑。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：`grid sm:grid-cols-2 lg:grid-cols-4` 改为 `flex flex-wrap`；按钮 `w-fit`、`h-8`、`px-3`。

### 26) 实施评估：工作表行改「报价模式」、预置行改「预置评估模式」

- 目标：与业务用语对齐——工作表切换侧为「报价模式」，预置方案 + 自定义人天侧为「预置评估模式」。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`

### 27) 实施评估：云产品行归类与过滤模板小计行

- 目标：云产品切换区与「报价模式 / 预置评估模式」一致，左侧有归类文案；模板误标为云产品的「产品实施工作量小计(人天)」等汇总行不再出现在云产品按钮中。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：
  - `isSelectableCloudProductLabel`：排除含「产品实施工作量小计」或同时含「工作量小计」与「人天」的标签。
  - `allCloudNames` 聚合时仅纳入可选项；依赖 `allCloudNames` 的可见云、多组织范围等自动一致。
  - `CardHeader` 云产品行：左侧「云产品」+ 右侧 `flex-wrap` 按钮（`h-8`、`w-fit`）。
  - `useEffect`：`allCloudNames` 变化时从 `selectedCloudNames` 剔除已不在列表中的名称，避免历史回读残留无效选中。

### 28) 实施评估工作台：报价模式与预置评估模式同一行

- 目标：减少纵向占用，在足够宽度下将「报价模式」与「预置评估模式」两区块并排。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：外层由纵向 `space-y-4` 改为 `flex flex-col gap-4 md:flex-row md:items-start md:gap-6`；两子块均 `min-w-0 flex-1`；预置行标签增加与报价行一致的 `pt-2 sm:pt-1.5`，竖排时对齐；预置按钮区补 `flex-1` 便于与左侧列宽协调。

### 29) 实施评估：预置评估模式仅随「模块报价」工作表显示

- 目标：选中非「模块报价」工作表时不展示预置评估与自定义人天入口，避免与套件类工作表语义混淆。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：常量 `SHEET_MODULE_QUOTE`；`CardHeader` 内预置区块仅当 `selectedSheet === SHEET_MODULE_QUOTE` 时渲染；切换工作表若离开模块报价则清空 `selectedPresetMode` 与自定义人天状态（见 ### 49：`customModeByCloud`）；版本回读时若非模块报价则不还原预置/自定义人天（并清空）；切换模板且首表非模块报价时关闭自定义人天。

### 30) 实施评估工作台：预置/云产品标签区防横向撑破布局

- 目标：云产品、预置等标签较多时不再把整卡宽度撑出视口（Grid 子项默认 `min-width:auto` + 按钮 `whitespace-nowrap` 易触发）。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：`Tabs`/`TabsContent`/`CardContent`/模块评估内工作台 `Card`/`CardHeader` 与各行容器补 `min-w-0`、`max-w-full`；报价/预置/云产品三处按钮条在 `flex-wrap` 基础上增加 `overflow-x-auto` 与 `scrollbar-gutter:stable` 作兜底；预置与自定义人天按钮增加 `whitespace-normal`、`leading-snug` 以便极窄时换行而非撑宽父级；云产品标签按钮同样使用 `whitespace-normal text-center leading-snug`，并以 `h-auto min-h-8` + `py-1.5` 避免多行文案被固定高度裁切。

### 31) 实施评估：四宫格与主列再收敛横向溢出

- 目标：指标卡 `sm:grid-cols-4` 在默认 `1fr`（等价 `minmax(auto,1fr)`）下会被子内容最小宽度撑开，连带下方云产品条宽度被拉到视口外。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`、`ui/V0_SAAS/components/workload/module-shell.tsx`、`ui/V0_SAAS/components/ui/card.tsx`
- 实现细节：四宫格改为 `sm:grid-cols-[repeat(4,minmax(0,1fr))]` 且 `w-full min-w-0`；各指标 `Card`/`CardContent` 加 `min-w-0`；页面根容器、`fieldset`、含 `Tabs` 的外层 `Card` 加 `max-w-full min-w-0`；`ModuleShell` 主列加 `min-w-0`；全局 `Card` 根与内容包裹层、`CardHeader` 默认加 `min-w-0`，避免 Grid/Flex 子项 `min-width:auto` 链式撑宽。

### 32) 实施评估：云产品条目卡片去掉「双击折叠」提示文案

- 目标：减少表头区噪音；折叠能力仍可通过双击空白处使用（与通用 `Card` 行为一致）。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：移除云产品分组标题下的「双击空白处可折叠/展开该云产品卡片」说明段落。

### 33) 实施评估：云产品下 SKU 合并为单表头

- 目标：每个云产品内「选择 / 实施要点 / 评估说明 / 标准人天 / 自定义人天」只出现一次表头，避免每个 SKU 重复表头。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：每个云产品 `Card` 内改为一张 `Table`；`TableHeader` 仅一行；`TableBody` 中按 SKU 插入跨列分组行（`SKU：{name}`，`colSpan` 随是否开启自定义人天为 4 或 5），其下为该 SKU 的数据行；外层保留 `overflow-x-auto` 与圆角边框。

### 36) 实施评估：按 SKU 容器嵌套明细表（表头回置 SKU 内）

- 目标：将「选择、实施要点、评估说明、标准人天、自定义人天」与数据行一并收进各 SKU 独立容器，云产品层不再使用跨列表头下的统一大表。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：云产品下改为 `space-y-4` 多个 `rounded-lg border` 块；块顶为 `SKU：{name}` 标题条；块内仍为 `table-fixed` + `colgroup` + 与 ### 35 一致的列宽与换行策略；移除单表 `colSpan` 分组行与 `Fragment`。
- 【更正】见 ### 37：意图为云产品内**一张表**，SKU 行与紧随其后的列标题、数据行同属表格结构，而非每个 SKU 独立小表。

### 37) 实施评估：SKU 分组行 + 列标题行 + 数据行（单表）

- 目标：与「SKU：xxx」条在**同一表格**内依次排列：先 `SKU` 跨列行（等同原标题条样式），再该 SKU 的「选择 / 实施要点 / 评估说明 / 标准人天 / 自定义人天」表头行，再条目行；避免表头与数据落在 SKU 外的独立 `div` 盒子中。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：云产品内恢复**单** `Table` + `colgroup` + `table-fixed`；`TableBody` 内按 SKU 用 `Fragment` 串联三段子行：`TableCell` `colSpan` 的 SKU 行、`TableHead` 组成的列标题行、`items` 数据行；外层仅一层 `overflow-x-auto` 与圆角边框。
- 【调整】见 ### 38：列标题改回云产品表顶唯一 `TableHeader`，SKU 下不再重复表头行。

### 38) 实施评估：列标题置顶、SKU 下仅分组行 + 数据行

- 目标：「实施要点 / 评估说明 / 标准人天 / 自定义人天」只在每个云产品表格**最顶**出现一次；各 SKU 仍为跨列 `SKU：{name}` 行 + 该 SKU 条目行。（### 39 起取消独立「选择」列，改为点击行选中。）
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：在 `colgroup` 后增加唯一 `TableHeader`；从 `TableBody` 的每个 SKU `Fragment` 中移除重复的 `TableRow`+`TableHead` 行。

### 39) 实施评估：条目行点击选中替代复选框

- 目标：去掉勾选列，通过点击数据行切换「已选」；选中行用背景色区分；自定义人天输入格点击不触发行切换。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：移除「选择」列与 `checkbox`；`TableRow` 增加 `onClick` 切换 `included`、`cursor-pointer`、`tabIndex={0}`、`Enter`/`Space` 键盘切换、`data-state="selected"` + `aria-selected`；选中态 `bg-blue-500/12`（dark 下加深）；`colgroup`/`colSpan` 随列数改为 3 或 4；自定义人天 `TableCell` `stopPropagation`。

### 40) 实施评估：SKU 左列 rowspan + 要点/说明单行展示

- 目标：取消单独占满宽的 `SKU` 行；`SKU：{名}` 固定在最左列，与同条目的实施要点、评估说明、标准人天等同处一行；同一 SKU 多行条目时左列合并并垂直居中；单元格内不再上下叠行，过长用单行截断 + `title` 悬停全文。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：表头增「SKU」列；`colgroup` 首列收窄；首行数据渲染 `rowSpan={items.length}` 的 SKU `TableCell`（`align-middle`、`border-r`、`bg-muted/20`），`click` `stopPropagation` 避免误切换选中；实施要点与副模块文案合并为 `primary · moduleLine` 单行 `truncate`；评估说明单行 `truncate`；数据格统一 `align-middle`。
- 【后续】### 41：评估说明改为自动换行；新增「实施要点内容说明」列（绑定 `deliveryDesc`）。

### 41) 实施评估：实施要点内容说明列（Excel 对齐）

- 目标：与原始估算表一致，在「评估说明」左侧展示「实施要点内容说明」；长文案单元格内换行，配合 `table-fixed` 与 `colgroup` 控制列宽，减少右向溢出。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`、`apps/api/src/modules/templates/templates.usecase.ts`
- 实现细节：表格列序为 SKU、实施要点、**实施要点内容说明**、评估说明、标准人天、自定义人天；内容说明与评估说明使用 `break-words whitespace-normal text-sm leading-snug align-top`；`item.deliveryDesc`（模板/导入 JSON 已含，`example-template.json` 与 Excel 导入一致）；导入解析补充表头别名「要点内容说明」。仓库内未纳入用户所指 xlsx 文件，字段以现有模板与解析逻辑为准。

### 42) 实施评估：内容说明与评估说明列宽 70% / 30%

- 目标：在「实施要点内容说明」与「评估说明」两列之间按 **7:3** 分配**表格剩余宽度**（相对彼此，而非占满整表）。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：`Table` 上设 CSS 变量 `--assess-fixed-side`（无自定义人天列约 `22rem`、有则约 `31rem`，与左侧 SKU/实施要点 + 右侧人天列之和大致对齐）；`colgroup` 中两列 `width` 分别为 `calc((100% - var(--assess-fixed-side)) * 0.7)` 与 `* 0.3)`。

### 43) 实施评估：内容说明与评估说明表头字号一致

- 目标：「实施要点内容说明」与「评估说明」表头字体大小一致。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：去掉「实施要点内容说明」`TableHead` 上单独的 `text-[11px]` / `sm:text-xs` 等，与「评估说明」同为 `min-w-0 whitespace-normal`，继承表级 `text-sm`。

### 44) 实施评估：模块评估外层 Card 减小上边距

- 目标：含「模块评估 / 多组织推广估算」标签的外层 `Card` 顶部留白略收紧。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：该 `Card` 增加 `pt-4`，与组件默认 `py-6` 经 tailwind-merge 后为 `pt-4 pb-6`，底边距不变。

### 45) ModuleShell：标题与说明横向并行

- 目标：页面主标题（`h1`）与灰色说明文案同一行展示（窄屏可自动换行）。
- 修改文件：`ui/V0_SAAS/components/workload/module-shell.tsx`
- 实现细节：原 `space-y-1` 纵向栈改为 `flex flex-wrap items-baseline gap-x-3 gap-y-1`；`h1` 加 `shrink-0`，说明 `p` 为 `min-w-0 flex-1`，便于右侧占满剩余宽度并换行。

### 46) 实施评估：SKU 列单元格去掉「SKU：」前缀

- 目标：表头已有「SKU」列名，数据行左列不再重复写「SKU：xxx」，仅展示 SKU 名称。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：`rowSpan` 合并格内容由 `SKU：{skuName}` 改为 `{skuName}`。

### 47) 实施评估：「参数与版本」Card 减小内部上下留白

- 目标：顶部参数与版本卡片整体更紧凑。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：该 `Card` 拼接类名增加 `py-4`，覆盖组件默认 `py-6`；`CardHeader` 由 `space-y-3` 改为 `space-y-2`；表单项栅格 `gap-3` 改为 `gap-2`。

### 48) 实施评估：模块评估 Tabs 区 CardContent 减小上边距

- 目标：含「模块评估 / 多组织推广估算」的 `CardContent` 顶部留白略收。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：`CardContent` 由 `p-6` 改为 `px-6 pb-6 pt-3`（左右与底边不变，顶由 24px 改为 12px）。

### 60) 实施评估：难度系数默认与规则集 difficultyFactorList 对齐

- 背景：保存/校验调用 `calculate` 时，后端 `validateCalculateRequest` 要求 `difficultyFactor` 必须属于当前规则集 `baseRule.difficultyFactorList`；否则 `code 40003`、文案「规则校验失败」。示例 `config/rules/example-rule-set.json` 仅允许 `0、0.1、0.2、0.3`，页面原默认 `1` 必失败。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：表单默认 `difficultyFactor` 改为 `0.2`；`useEffect` 在 `ruleSet` 与 `form.difficultyFactor` 变化时，若当前值不在列表内则回落为列表中的 `0.2` 或首项。

### 61) 实施评估「保存版本」报「服务器内部错误」（请求体超限）

- 背景：大模板（数百条 `itemSelection` + 完整 `serverResult`）POST 到 `POST /api/v1/versions` 时，Express 默认 `express.json()` 仅约 **100kb**，超限触发 `entity.too.large`，原统一走 500 文案「服务器内部错误」。
- 修改文件：`apps/api/src/app.ts`（`express.json({ limit: "5mb" })`）、`apps/api/src/middleware/error-handler.ts`（对 `entity.too.large` 返回 413 与明确文案，避免再误判为未知 500）。
- 结果：正常保存版本不再因默认体积上限失败；若将来仍超限，前端将收到可读的 413 提示而非泛化 500。

### 62) 实施评估：模块评估四宫格 KPI 卡片压缩上下边距

- 目标：「人天构成 / 云产品工作量占比 / 勾选条目 / 总评估人天」四张卡减少纵向留白，保留较小上下边距。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：外层 `Card` 覆盖 `py-2.5 gap-2`（替代默认 `py-6 gap-6`）；`CardContent` 统一 `px-3 py-2`（原饼图 `p-3`、数字卡 `p-5`）；标题与内容间距 `mt-2`→`mt-1.5`；栅格 `gap-4`→`gap-3`；四卡加 `collapsible={false}` 避免误触双击折叠与「信息卡片」摘要条。

### 63) 实施评估：检出后仍无法勾选 / 点 SKU 列无反应

- 原因说明：（1）**只读条件**：`checkoutStatus === "checked_in"` 或 `versionDocStatus === "reviewed"` 时 `fieldset disabled`，整区不可编辑；须先对当前版本执行 **检出**（工具条），与「已检入」只读区分。（2）**SKU 列**：左侧 SKU 单元格曾 `stopPropagation` 且无任何逻辑，用户只点 SKU 名时不会切换勾选，误以为「选不了」。（3）**检出态不同步**：若列表数据里缺 `checkoutStatus`，`|| "checked_in"` 会把检出态误判为已检入。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`、`ui/V0_SAAS/lib/workload-service.ts`
- 实现细节：检出成功后用 `checkout` 接口返回写回对应记录的 `checkoutStatus` 等字段；`listModuleVersions` / `listGlobalVersions` 中 `checkoutStatus`/`versionDocStatus` 用 `??` 默认；SKU 列改为单击切换该 SKU 下全部条目勾选（只读时不变），并 `title` 提示。

### 64) 实施评估：去掉参数卡片标题「参数与版本」

- 目标：顶部参数与版本 `Card` 不再单独显示总标题，由「基本信息 / 评估信息」分组标题承担层级。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：移除该 `CardHeader` 内 `CardTitle`「参数与版本」；折叠态 `collapsedSummary` 仍保留「参数与版本」前缀便于识别。

### 65) 实施评估：检出状态移入「基本信息」并始终展示

- 目标：检出状态不再跟在 `VersionVcsToolbar` 按钮行末尾；放入参数卡「基本信息」栅格内，与版本/模板字段同级，且不依赖是否已选版本（无版本时显示「未选择版本」）。
- 修改文件：`ui/V0_SAAS/components/workload/version-vcs-toolbar.tsx`（导出 `VersionCheckoutStatusDisplay`，工具条内复用）、`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：基本信息区增加一行 `sm:col-span-2` 的 `VersionCheckoutStatusDisplay`，标签样式与 `text-[11px]` 字段一致；`VersionVcsToolbar` 设 `showStatusField={false}` 避免重复。

### 66) 实施评估：已检入时 SKU 表仅显示已勾选行

- 目标：`checkoutStatus === "checked_in"` 时，模块评估云产品表格不展示未勾选条目，便于只读浏览；检出后恢复展示全部行以便编辑。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：新增 `showOnlySelectedSkuRows`；按 SKU 组过滤 `rowItems`，`rowSpan` 随过滤后行数；某云下无已选行时整卡不渲染；全工作表无勾选时提示「已检入：当前工作表下暂无已勾选条目。」；云产品标题旁附「已检入 · 仅显示已勾选条目」说明。

### 67) 版本工具条：「版本历史」弹窗替代「显示/隐藏历史版本」

- 目标：去掉下拉过滤归档逻辑；按钮改为「版本历史」，点击弹出表格列出全部版本记录，按 `updatedAt` **新→旧** 自上而下排序；当前所选版本行高亮。
- 新增文件：`ui/V0_SAAS/components/workload/version-history-dialog.tsx`（`VersionHistoryDialog`、`recordsToVersionHistoryRows`）。
- 修改文件：`ui/V0_SAAS/components/workload/version-vcs-toolbar.tsx`（`onVersionHistory`）；`app/dashboard/assessment|page|requirement-import|resource-cost|dev-assessment/page.tsx`。
- 实现细节：各页版本/总方案下拉里 **始终包含归档版本**（移除 `showHistoricalVersions` 与 `buildVersionOptions` 过滤）；仪表盘 `loadPlans` 不再按归档过滤方案列表；初始化 `useEffect` 与历史开关解耦为仅挂载拉取（`[]`）。

### 68) 登录页：去掉登录态 CardDescription

- 目标：移除「输入账号密码后进入新版 V0 工作台」副标题；注册态仍保留「输入用户名、密码和推荐码完成注册」。
- 修改文件：`ui/V0_SAAS/app/login/page.tsx`

### 69) 实施评估：只读状态下禁止取消/改动 SKU 勾选

- 目标：`isReadonly`（已检入或已审核）时，避免用户点击行或批量操作取消勾选导致与已持久化数据不一致。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：表格行 `toggleRowIncluded` 首行 `if (isReadonly) return`；只读时去掉行点击/键盘切换与手型 hover；SKU 列点击显式 `if (isReadonly) return`；「全选」「全不选」「自定义人天」按钮 `disabled={isReadonly}`。

### 59) 实施评估：参数与版本卡片纵向压缩布局

- 目标：减少「参数与版本」Card 占用高度，整体更紧凑。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`、`ui/V0_SAAS/components/workload/version-vcs-toolbar.tsx`
- 实现细节：Card 增加 `gap-2`、`py-3`；`CardHeader` `space-y-2`；标题 `text-sm`；两组外层 `xl:grid-cols-2` 宽屏并排；分组 `rounded-lg px-3 py-2`、小标题 `text-xs`/`mb-1.5`；表单项 `space-y-0.5`、`text-[11px]` 标签；`select`/`Input` `h-8 text-xs`；工具条 `flex-wrap gap-1.5`、`Button` `size="sm"`；`VersionVcsToolbar` 新增可选 `compact`（`size="sm"`、状态区 `h-8`），实施评估页传入 `compact`。

### 58) 实施评估：参数与版本分为「基本信息」「评估信息」分组

- 目标：总方案/评估版本/模板/规则集 与 用户与组织相关数字 分两组容器，带分组标题。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：`CardHeader` 内原 `md:grid-cols-5` 大表拆为两块 `rounded-xl border border-border/50 bg-muted/15 p-4`；第一块标题「基本信息」四列栅格（`xl:grid-cols-4`）；第二块「评估信息」四列栅格；`CardHeader` 间距改为 `space-y-4`。

### 57) 实施评估：标准人天格加粗与档位着色

- 目标：标准人天数字加粗；大于 5 深橙色，大于 10 深红色（阈值含边界：>5、>10）。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：按 `Number(item.standardDays||0)` 分支 `font-semibold`；`>10` 用 `text-red-800 dark:text-red-400`；`>5` 用 `text-orange-800 dark:text-orange-400`；否则 `text-foreground`。

### 56) 实施评估：标准人天列标题与数据格左对齐

- 目标：「标准人天」表头与数据格均左对齐。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：`TableHead` 为 `text-left`；对应 `TableCell` 为 `text-left`、`tabular-nums`。

### 55) 实施评估：条目数据行悬浮色与表头 accent 一致且更亮

- 目标：鼠标悬停数据行时使用与表头同一语义色（accent/teal），且透明度高于表头，更易感知。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：云产品内 `TableBody` 行由 `hover:bg-muted/50` 改为 `hover:bg-accent/28 dark:hover:bg-accent/42`（表头为 `accent/12`、`accent/20`）；已选行仍为蓝色底，逻辑不变。

### 54) 实施评估：云产品条目表表头主题背景

- 目标：SKU 等列表头使用显眼且与全局主题一致的非灰色底。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：`TableHeader` 使用语义色 **`bg-accent`** 浅铺：`[&_th]:bg-accent/12 dark:[&_th]:bg-accent/20`，与 `globals.css` 中 `--accent`（teal）及 `--ring` 一致；`[&_th]:text-foreground`；表头 `TableRow` `hover:bg-transparent`，避免默认行 hover 冲淡底色。

### 53) 实施评估：按 SKU 表全列竖向分隔线

- 目标：云产品内模块评估表在 SKU 分组表上，各列之间均有与 `border-border/40` 一致的竖线。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：常量 `ASSESS_TABLE_COL_BORDER`（`border-l border-border/40`）用于实施要点起的各 `TableHead`/`TableCell`；SKU 列表头与合并格增加 `border-r border-border/40`，与左侧列右缘闭合；`cn` 合并类名。

### 52) Card 折叠容器：展开高度随内容增高（去掉 2000px 顶）

- 目标：可折叠 `Card` 展开后不再被 `max-h-[2000px]` 截断，模块报价等长列表随云产品块自然撑高。
- 修改文件：`ui/V0_SAAS/components/ui/card.tsx`
- 实现细节：展开态由 `max-h-[2000px] overflow-hidden` 改为 `max-h-none overflow-visible`；收起态仍 `max-h-0 overflow-hidden`；展开时 `max-height` 过渡对 `none` 可能无动画，以完整展示为先。

### 51) 实施评估：SKU 与实施要点竖线贯通表头与表体

- 目标：两列之间的 `border-border/40` 竖线在表头、合并 SKU 后的续行上连续可见。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：`Table` 增加 `border-collapse`；「实施要点」列所有 `TableHead`/`TableCell` 统一 `border-l border-border/40`；SKU 合并格去掉仅局部的 `border-r`，避免与左侧竖线重复。

### 50) 实施评估：加宽实施要点列、压缩评估说明列并换行展示要点

- 目标：实施要点列更宽且单元格换行完整展示；评估说明列略窄；中间「内容说明 / 评估说明」剩余宽度由约 70/30 调整为约 75/25。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：`colgroup` 实施要点列 `min(12rem,26vw)`；两列 `calc` 系数 `0.75` / `0.25`；`--assess-fixed-side` 无自定义列 `24rem`、有则 `33rem`；实施要点 `TableCell` 去掉 `truncate`，改为 `break-words whitespace-normal`；表头实施要点补 `min-w-0`。

### 49) 实施评估：自定义人天按云产品开关 + 按钮靠全选左侧

- 目标：「自定义人天」从模块报价预置区移除，放到每个云产品卡片工具条「全选」左侧；仅对该云产品表格展示自定义人天列并参与该云累计人天、总基础人天与饼图汇总。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：状态由 `customModeEnabled` 改为 `customModeByCloud: Record<string, boolean>`；`baseDays`、`cloudWorkloadSlices`、`currentItemsPayload`、检入/保存 payload 按条目 `cloudProduct` 判断是否采用自定义人天；持久化增加 `customModeByCloud`，并保留 `customModeEnabled: Object.values(customModeByCloud).some(Boolean)` 兼容旧数据；版本回读优先 `payload.customModeByCloud`，否则旧版 `customModeEnabled === true` 时对当前工作表内可选云产品键统一置 true；离开「模块报价」或非该工作表时清空 map；云产品表格内 `--assess-fixed-side`/自定义列以 `cloudCustom` 为准。

### 34) 实施评估：实施要点列去重展示

- 目标：避免「交付要点」与「交付模块」文案相同或副行再用 `itemName` 兜底时出现两行重复（如「总账业务处理」叠两次）。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：主行取 `deliveryPoint || itemName`；副行仅展示非空的 `deliveryModule`，且与主行去空格后比较，相同则不再渲染副行；副行不再用 `itemName` 兜底；该列增加 `whitespace-normal`、`max-w-xs` 便于多行可读。

### 35) 实施评估：模块评估表「评估说明」列宽与换行

- 目标：长评估说明在默认 `TableCell` 的 `whitespace-nowrap` 下单格被撑到上千像素，导致表格整体右溢。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：该表 `Table` 增加 `table-fixed`、`min-w-0`；`colgroup` 固定选择/实施要点/标准人天/自定义人天列宽，中间列占剩余宽度；表头相关列 `whitespace-normal`；「评估说明」`TableCell` 使用 `min-w-0 whitespace-normal break-words align-top`；实施要点列去掉 `max-w-xs` 改由列宽约束；数字列 `whitespace-nowrap`；外层滚动容器补 `min-w-0`。

## 2026-04-06 调整记录

### 56) 侧边栏新增「系统管理」入口

- 目标：在「用户管理」下方补充系统级配置入口，统一承载平台管理能力。
- 修改文件：`ui/V0_SAAS/components/app-sidebar.tsx`
- 实现细节：
  - 系统管理分组新增 `系统管理` 菜单项，路由为 `/dashboard/system-management`。
  - 非管理员账号沿用现有权限策略，不显示系统管理相关菜单。

### 57) 系统管理页：版本号编码规则（工具栏 + 表格）

- 目标：支持对不同模块（总方案、需求、实施评估、开发评估、资源人天及成本等）的版本号编码规则进行配置、禁用、生效操作。
- 修改文件：`ui/V0_SAAS/app/dashboard/system-management/page.tsx`
- 实现细节：
  - 新增“版本号编码规则”卡片，采用 `模块筛选 + 关键字搜索 + 刷新` 的工具栏布局。
  - 规则以表格展示：模块、模块编码、前缀、编码格式、示例、状态、生效时间、操作。
  - 单行操作包含：`配置`（进入编辑态）、`生效`、`禁用`。
  - 配置面板支持编辑前缀与格式，保存后实时回算示例版本号。

### 58) 系统管理页：预留其他管理容器布局

- 目标：为后续系统管理能力扩展预留稳定布局空间，避免后续改动破坏信息架构。
- 修改文件：`ui/V0_SAAS/app/dashboard/system-management/page.tsx`
- 实现细节：
  - 新增“其他系统管理内容（预留）”卡片。
  - 预置三块占位容器：字典与编码映射、通知与消息策略、审计与安全策略。

### 59) 系统管理页：版本号编码规则接入后端持久化接口

- 目标：将页面从前端内存态演示切换为真实配置读写，支持刷新后保留状态。
- 修改文件：`ui/V0_SAAS/app/dashboard/system-management/page.tsx`、`ui/V0_SAAS/lib/workload-service.ts`
- 实现细节：
  - 新增系统管理 API 访问层：查询列表、更新配置、规则生效、规则禁用。
  - 页面初始化改为拉取后端规则列表；工具栏刷新调用真实接口。
  - “配置/生效/禁用”操作改为异步请求并回刷数据；增加操作中禁用态，避免重复提交。
  - 生效时间显示改为按本地时间格式化展示（兼容 ISO 时间与 `--` 占位）。

### 60) 版本创建改为后端按规则生成编码

- 目标：移除前端硬编码版本号拼接，统一由后端依据系统管理规则生成，保证口径一致。
- 修改文件：`ui/V0_SAAS/lib/workload-service.ts`
- 实现细节：
  - `createModuleVersion` 不再本地生成版本号，仅提交模块类型与业务 payload。
  - `createGlobalPlanVersion` 不再拼接 `GLOBAL-` 前缀，改为读取后端返回的 `record.versionCode`。
  - 保留函数签名兼容现有调用点，降低页面改造范围。

### 61) 实施评估页：基本信息增加「项目名称」

- 目标：在参数卡「基本信息」首位展示项目名称；选总方案时自动带出仪表盘方案 `projectName`，未选总方案时可手填；保存/检入与导出使用当前项目名称。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：
  - 状态：`dashboardPlans`（`PlanRow[]`）、`projectName`；初始化 `setDashboardPlans(plans)`。
  - `applyGlobalVersionSelection`：切换总方案时从 `dashboardPlans` 回填 `projectName`；清空总方案不强制清空项目名。
  - `onLoadVersion(code, plansOverride?)`：优先读 payload `projectName`，否则按 `globalVersionCode` 在 `plansOverride ?? dashboardPlans` 中补全；嵌入初始化回读版本时传入 `plans` 避免闭包内计划列表未就绪。
  - 保存与检入 payload 增加 `projectName`（trim）；导出 `exportProjectName` 使用 `projectName.trim()`，缺省为「未命名项目」。
  - 折叠摘要条增加「项目」展示。

### 62) 全局弹窗：支持顶部空白区拖拽

- 目标：所有基于通用 `Dialog` 的弹窗都可通过顶部空白区拖拽移动，提升大屏编辑场景可用性。
- 修改文件：`ui/V0_SAAS/components/ui/dialog.tsx`
- 实现细节：
  - 在 `DialogContent` 顶部增加拖拽热区（避开右上角关闭按钮区域）。
  - 通过 `pointer` 事件记录拖拽位移，并用 CSS 变量叠加到居中 `translate` 上。
  - 支持拖拽结束与组件卸载时自动清理监听，避免残留事件绑定。

### 63) 系统管理：编码格式内嵌帮助图标说明

- 目标：在关键位置就近解释编码占位符，降低配置理解成本。
- 修改文件：`ui/V0_SAAS/app/dashboard/system-management/page.tsx`
- 实现细节：
  - 在规则列表表头“编码格式”旁增加帮助图标（Tooltip）。
  - 在配置弹窗“编码格式”标签旁增加同款帮助图标（Tooltip）。
  - 说明内容统一包含 `{PREFIX}`、`{GL}`、`{NN}`、`{NNN}`、`{YYYY}`、`{YYYYMM}`、`{YYYYMMDD}`、`{MODULE}` 的语义。

### 64) 实施评估：已检入状态不触发未保存拦截

- 目标：当当前版本检出状态为“已检入”时，切换主菜单不再提示“未保存修改”。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`
- 实现细节：
  - 新增 `suppressUnsavedPrompt`，当 `selectedVersionRecord.checkoutStatus === "checked_in"` 时生效。
  - 脏状态上报逻辑改为：抑制态下强制 `setDirty(false)`；仅在可编辑态继续 `setDirty(true)`。

### 62) 开发评估页：版本与参数增加「项目名称」（与实施评估一致）

- 目标：与 `/dashboard/assessment` 对齐：首位展示项目名称；选总方案自动带出 `projectName`；未选总方案可手填；保存/检入 payload 含 `projectName`；版本回读支持 payload 与计划列表补全；嵌入初始化传入 `plans`。
- 修改文件：`ui/V0_SAAS/app/dashboard/dev-assessment/page.tsx`
- 实现细节：
  - `dashboardPlans`、`projectName`、`applyGlobalVersionSelection`、`onLoadVersion(code, plansOverride?)` 逻辑同实施评估页。
  - `currentPayload()` 与 `createModuleVersion` 增加 `projectName`（trim）。
  - 汇总条展示「项目 …」；CSV 文件名嵌入项目名称 slug（非法文件名字符替换为 `-`）。
  - 脏标记依赖增加 `globalVersionCode`、`linkedAssessmentVersionCode`、`projectName`。

### 63) 实施评估：版本下拉仅「当前生效」+ 版本历史「按历史版本创建新版」

- 目标：主界面不再用下拉切换历史实施评估版本，仅展示按 `updatedAt` 排序后的最新一条（当前生效）；查看历史在「版本历史」；需回退内容时选中历史行，通过「按历史版本创建新版」提交快照并由后端生成新编码（版本号只增不减）。
- 修改文件：`ui/V0_SAAS/app/dashboard/assessment/page.tsx`、`ui/V0_SAAS/components/workload/version-history-dialog.tsx`
- 实现细节：
  - `pickLatestAssessmentRecord`、`buildLatestAssessmentVersionOptions`；`reloadVersions` 维护单选项与 `currentVersionCode` 兜底。
  - 嵌入 URL 带非当前生效的 `version` 时 toast 提示，仍加载当前生效版本。
  - `VersionHistoryDialog` 可选中行（高亮）、底部按钮；传入 `latestRecordId` 时禁止对当前生效行重复创建；可选 `onCreateFromHistory` / `createFromHistoryLoading`（其它模块不传则行为与旧版一致）。
  - 创建新版：`JSON` 深拷贝源记录 `payload` 后 `createModuleVersion("assessment", …)`，再 `reloadVersions` + `onLoadVersion`。
