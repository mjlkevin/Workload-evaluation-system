# 产品闭环收敛清单 V1（首发范围）

> 对应计划待办：`close-product-gaps`  
> 范围：`WBS` / `评审` / `标准 PLM` 的首发闭环收敛（MVP 口径）。

## 1. WBS（MVP）

- 当前状态：只读派生视图，可按最新 `global` 版本生成任务行。
- 首发验收项：
  - [x] 可展示任务列表与状态（未开始/进行中/已完成）
  - [x] 显示派生来源（`sourceGlobalVersionCode`）
  - [x] 显示模块键与关联版本（`moduleKey` / `linkedVersionCode`）
  - [ ] 任务编辑与持久化（转入二轮）

## 2. 评审（MVP）

- 当前状态：后端已支持团队评审/评论，前端可做最小演示。
- 首发验收项：
  - [x] 创建评审（按团队+总方案）
  - [x] 评审状态变更（open/closed）
  - [x] 评论列表与新增评论
  - [x] 最小权限控制（非授权用户拒绝）
  - [ ] 高级流程（模板化评审、通知、@提醒）转入三轮

## 3. 标准 PLM（MVP）

- 当前状态：策略尚未完整产品化。
- 首发策略：
  - [x] 明确“首发不阻塞”定位（不作为上线阻塞项）
  - [x] 在需求分层中归为三轮产品完整度能力
  - [ ] 完整策略配置与应用闭环（转入三轮）

## 4. 闭环结论（首发）

- 结论：`WBS` 与 `评审` 达到“最小可演示闭环”，`标准 PLM` 采用延期策略，不阻塞首发。
- 风险：若评审流程复杂度继续增加，需在三轮前补齐流程状态机与审计字段。
