# 阶段 2 · S7 收尾批实施方案（V1，**报批稿——未获批准不执行**）

> 会话标识 `WES-S5S4S7-01` · 成文日期 2026-08-30 · 事实源 [`阶段2-存储切换-实施计划.md`](./阶段2-存储切换-实施计划.md) §4.11 S7 行 / §6 / §10
>
> **一句话结论**：S7 不是「删两个文件」这么小——实取后确认它含 **6 项既定清单 + 4 项本方案新取证**，其中 3 项会牵到部署文档与 `docker-compose`，1 项（trace 空串提权加固）属安全语义变更。本方案把每一项的**删除面、证据、风险、待裁问题**逐条摊开，请架构侧按「决策请求」清单拍板后我再动手。

## 0. 开工前置（硬性，未满足不开工）

| 前置 | 当前实取（2026-08-30，`origin/main` = `6d92bd4`） | 满足条件 |
|---|---|---|
| S5（teams）合入 main | 分支 `qoder/s5-teams-json-removal` @ `642480f`，分支 CI `33316222109` / `33317196457` 全绿，**main 未合入** | 架构侧复核后合入，main CI 六套件实取 **1083** |
| S4（versions）合入 main | 分支 `qoder/s4-versions-json-removal` @ `eb2b478`（含 commit D 台账），代码顶 `8b32f1a` 的 CI `33322482874` 全绿 1080，**main 未合入** | 同上，S4 合入后 main 实取 **1080** |
| `config-integrity.ts` 无条可摘 | main 实取 `REQUIRED_FILES` **仍 2 条**（`config/versions/records.json` L31 / `config/teams/store.json` L36）→ D15 条件**尚未在 main 成立**（S5/S4 分支内已成立：`REQUIRED_FILES` = 0，实跑 `[config-integrity] OK, checked 0 files.`） | S5+S4 合入后台账自然收敛 |
| 九域 JSON 读写路径全删 | S1/S2a/S2b-1/S2b-2/S6/S3/S5/S4 全部交付，S4 后代码段收官 | 上一条完成即达成 |

**D15 的删除面在 main 上成立之前，本批第 2 项不得启动。**

## 1. 范围总览

架构侧交接工单 §六 给了 6 项。逐条实取后，其中 **3 项按原样可做**、**2 项比预期大**（要动部署文档 / 要重写测试守护）、**1 项本批不做只出评估**。另有 4 项是台账里已点名归 S7 的存量债务（B4/B6/B7/B9/B10），本方案一并摊开，**但不擅自扩边界**——是否纳入 S7 由架构侧勾选。

| # | 项 | 实取规模 | 本方案立场 |
|---|---|---|---|
| 1 | `utils/file.ts` 清理（B5） | 82 行文件，6 个导出 | **拆两半**：死代码即删；`modelVerifyStatusPath` 待 C1 裁决，不在本批强删 |
| 2 | `config-integrity.ts` 整体删除（D15） | 2 源文件 + 2 npm 脚本 + `main.ts` 启动调用 + **1 个环境变量 + 6 处文档 + 1 处 compose** | 可做，但**面比「删文件」大**，见 §3 |
| 3 | 死配置清理（开关） | 代码侧只剩 **1 处非注释命中**；`.env` 8 行死配置 | 代码侧可做；`.env` 属本机不入 git，**请求授权或交项目侧** |
| 4 | outbox 死符号退役 | 工单列 3 项，实取为 **5 项**（多出 `HARNESS_OUTBOX_STATUSES` / `HarnessOutboxStatus` 全死导出） | 建议本批做，**替代回归防线先写后删** |
| 5 | 防漂移计数归零验证（§6） | 现 7 条 / 12 处 | 可做，是前四项的收口验证，**不单列** |
| 6 | trace-pg 空串提权加固 | 仓储层 1 个隐式约定 | **本批不实施，只出评估结论**，见 §7 |
| 7+ | 台账存量债务 B4 / B6 / B7 / B9 / B10 | 见 §8 | 建议 **B7 并入 S7**（同为死代码收口），B4 / B6 / B9 / B10 **推阶段 3 或另案** |

## 2. 第 1 项 · `utils/file.ts`（B5）——实取后须拆两半

**实取（S4 分支顶）**：文件 82 行，导出 6 个，逐个调用方盘点：

| 导出 | 调用方（非注释实取） | 处置建议 |
|---|---|---|
| `resolveRootDir()` | `db/seed.ts:39`、`db/seed.guards.test.ts:16`、`ops/config-integrity.ts:4`、`services/export.service.ts:13`、`utils/file.ts` 内部 | **保留**（导出域与 seed 都在用） |
| `loadJsonFile()` | `test-helpers/single-doc-store-seed.ts:32`、`modules/knowledge/knowledge.usecase.test.ts:17`（读 seed 源 fixture） | **保留**（生产侧零调用方，仅测试 helper 用；可考虑移进 `test-helpers/`，属可选优化） |
| `saveJsonFile()` | **全仓零调用方**（唯一命中是 drift 白名单里的说明注释） | **删**（B5 死代码主体） |
| `ensureExportDir()` | `estimates.repository.ts`、`exports.usecase.ts`、`export.service.ts`、`modules.usecase.test.ts` | **保留**（Excel 导出落盘，与存储迁移无关） |
| `modelVerifyStatusPath()` | **生产在役**——`system-effective.ts:191 / :210` 读写 `config/system/model-verify-status.json`；测试 `system-effective.test.ts:114` | **待裁**（S3 开工盘点 C1 项：该文件是运行时模型校验状态缓存，不是业务 store，不属九域切换面） |
| `PROTOTYPE_EXPORT_SOURCE_XLSX_RELATIVE_PATH` | `export.service.ts:793` | **保留** |

**因此「B5 清理」的真实收敛结果 = 删 `saveJsonFile` 一个函数 + `no-sync-store-io` 白名单 #7 的 `expectedHits` 由 2 递减（实取后填）**。

- **待裁（决策请求 D-S7-01）**：`modelVerifyStatusPath` / `model-verify-status.json` 的归属——① 认定「运行时状态文件」永久豁免（建议，S7 只登记口径不动它）；② 迁入 PG（超出阶段 2 范围，属阶段 3 新域）；③ 本批强删（**不建议**，它仍在役，删了要改 `system-effective.ts` 两处读写并新增回归）。
- **建议**：S7 的 B5 条目改名「`saveJsonFile` 死代码删除 + `utils/file.ts` 口径定稿」，避免留下「B5 做了但 file.ts 还有 6 个导出」的误解。

## 3. 第 2 项 · D15 `config-integrity.ts` 整体删除——面比预期宽，含 1 个环境变量

**它现在被谁用（实取）**：

1. `apps/api/src/main.ts:15-24` —— **进程启动即调用** `runConfigIntegrityCheck("startup", false)`，由 `process.env.CONFIG_INTEGRITY_ON_STARTUP !== "false"` 控制，失败只 `logger.warn`（不 fail-fast）。
2. `apps/api/src/ops/config-integrity.cli.ts`（整文件）—— ops CLI 入口。
3. `apps/api/package.json:9-10` `ops:config:check` / `ops:config:check:repair`；根 `package.json:41` 同名转发脚本（`ops:check:config`）。
4. `apps/api/src/test-helpers/no-sync-store-io.drift.test.ts:50` 白名单 #1（`expectedHits: 4`）。
5. **`CONFIG_INTEGRITY_ON_STARTUP` 环境变量本身的引用面**：`docker-compose.dev.yml:21`、`docs/ENVIRONMENT.md:12 / :36`、`06_发布与部署/部署指南-R1.md:216`、`06_发布与部署/部署说明.md:74`、`06_发布与部署/首发发布演练检查清单-V1.md:9`。

**建议删除序（单 commit 内按此序，可整体 revert）**：① `main.ts` 启动段与 import → ② `config-integrity.ts` + `config-integrity.cli.ts` 两文件 → ③ 三处 npm 脚本 → ④ drift 白名单 #1 条目 + 计数更新 → ⑤ `docker-compose.dev.yml` 的 `CONFIG_INTEGRITY_ON_STARTUP` → ⑥ 四处文档口径校正（与 B9 文档面同批，见 §8）。

**先说一个与本项相关的反向事实（避免高估风险）**：S4 分支顶实取，摘完 teams / versions 两条后 `REQUIRED_FILES` **是空表**，`runConfigIntegrityCheck` 自此退化为「恒 ok、checked 0 files」的空跑——**它今天已经不再保护任何东西**。曾担心「删校验器会失去对 seed 源文件的保护」经复核**不成立**：`config/` 下 12 个被 git 跟踪的 JSON 中，S5+S4 合入后剩 10 个（seed 源 6：`knowledge/store.json`、`rules/example-rule-set.json`、`templates/example-template.json`、`versions/version-code-rules.json`、`system/requirement-settings.json`、`system/implementation-dependency-rules.json`；rag 评测 3；`system/model-verify-status.json` 1），而**这 10 个从来不在 `REQUIRED_FILES` 里**（`seed.ts:165-171` 的 `readJsonOr(..., null)` 静默默认值是既存行为，与本批无关，删不删校验器都一样）。S7 分支顶的注释已把这一判断写进表内留档。

- **待裁（决策请求 D-S7-02）**：三选一——① **整删（建议）**：校验器已无可检查对象，连 ops CLI 一起删净，同时删 `CONFIG_INTEGRITY_ON_STARTUP` 全链与四处文档口径，达成工单 §六 D15 原意；② **降级保留**：删 `main.ts` 启动调用与环境变量，但保留 `config-integrity.ts` + CLI 作为「将来新增配置文件时的巡检挂载点」——代价是仓内永久留一个空跑模块与它的 4 处同步 I/O 豁免条目；③ 整删但**不动**部署文档与 compose（不建议，会留下指向已删开关的环境变量说明）。
- 我的建议：**①（整删）**，理由：一个 `checked 0 files` 的模块不产生任何价值，却长期占着 drift 白名单 #1 的 4 处同步 I/O 豁免（§6 归零验证的最大单项就是它）；「将来挂载点」的诉求应等真有新的必查文件时按当时的约束重建，不靠空跑占位。

## 4. 第 3 项 · 死配置（开关）清理

**代码侧实取（S4 分支顶）**：`apps/` 下 `WES_STORE_*` 的**非注释命中只剩 1 处**——`modules.handlers.test.ts:102` `const STORE_PG_FLAG_KEYS = ["WES_STORE_USERS_PG"]`。users 域自身（`users.repository.ts:7-8`）已注明「JSON 分支已删，开关保留至 S7 统一退役」，即 **`WES_STORE_USERS_PG` 今天也是死配置**。

建议：① `ci.yml` 删 L133 `WES_STORE_USERS_PG: "true"`，并把 L122-131 的退役注记链收敛成一条「阶段 2 第 4 步已收官，九域开关全部退役」的终态注释；② `modules.handlers.test.ts` 删 `STORE_PG_FLAG_KEYS` 常量与 L151-155 的 env 还原循环，**八个 `_reset*ForTest()` 调用全部保留**（它们清的是进程内仓储单例，与开关无关，沿用 S5/S4 先例）；③ 反向证据要求：改完全仓 `WES_STORE_` 非注释命中 = **0**。

**`.env` 侧**：`apps/api/.env` **不入 git**，本机实取九个开关行中 S4 后 **8 行为死配置**（仅 `USERS` L5 名义在役，实则也已在役=False，见上）。

- **待裁（决策请求 D-S7-03）**：谁动 `.env`？① 授权执行侧直接删 8 行（改前备份、改后跑一次 `verify-local-db-tests.sh` 复验）；② 只在交付报告里给出逐行清单，由项目侧自己删（S5/S4 先例）。建议 **①**，理由：留 8 行死配置在本地，后续任何人按 `.env` 推断「哪个域还走 JSON」都会得到错误答案。
- **同批须一并处理的 env 样例文件（实取）**：根 `.env.example` 命中 3 处——L56 / L58 两条历史注释 + L64 `# WES_STORE_USERS_PG=true`（被注释掉的示例行）；`apps/api/.env.example` 与 `.env.local` 实取 **0 命中 / 不存在**。故本项只需把根 `.env.example` 的命名约定说明改为终态口径（「阶段 2 第 4 步收官，`WES_STORE_<DOMAIN>_PG` 全部退役，开关机制不再存在」）并删 L64 示例行，不新增改动面。

## 5. 第 4 项 · outbox 死符号退役——**先立防线，再拆**

工单列 3 项，实取为 5 项：

| 符号 | 位置 | 现役状态 |
|---|---|---|
| `HarnessWorkflowOutboxEntry` 类型 | `harness-runtime.worker.ts:69` | 仅被下一行的字段引用 |
| `outbox?: HarnessWorkflowOutboxEntry[]` 字段 | `harness-runtime.worker.ts:86` | 生产侧 `workbench-chat.workflow.ts:155 / :284` 仍写 `outbox: []`；**不落库、worker 不读**（全仓 `.outbox` 命中仅测试）；`workbench-chat.workflow.test.ts` 5 处断言其恒空 |
| `"outbox_enqueued"` 事件名 | `harness-runtime.types.ts:40`（`HARNESS_RUN_EVENT_TYPES` 成员） | 生产零写入；测试侧 4 处代码引用（3 冻结清单 + 1 负向守护）+ 1 处注释；另被 `repository.ts:600` 白名单**整体**引用 |
| `HARNESS_OUTBOX_STATUSES` 常量 | `harness-runtime.types.ts:31` | **全仓零消费者**（工单未列，本方案补入） |
| `HarnessOutboxStatus` 类型 | `harness-runtime.types.ts:32` | **全仓零消费者**（同上） |

DB 侧无残留：outbox 表已随 S2b-2 的 `0021_material_quasimodo.sql` 迁移删除。

**替代回归防线（工单 §六 明写「退役前须有替代回归防线」）——实取后性质变了，这是本项真正的难点**：`outbox_enqueued` 不只是一个「无人写入的事件名」，它同时是**运行时写入白名单成员**与**三个历史批次各自冻结过的词汇**：

- **生产侧在役**：`harness-runtime.repository.ts:600` 用 `HARNESS_RUN_EVENT_TYPES.includes(input.eventType)` 做 `appendRunEvent` 的事件名校验。删成员后，写入 `outbox_enqueued` 会从「允许但无人写」变为**主动拒绝**——这是防复活的正向收益，但也意味着它是语义变更（不是纯删代码）。
- **冻结守护 6 处（实取行号）**：`harness-runtime.types.test.ts` 三个用例各持一份含 `"outbox_enqueued"` 的 frozen 字面量数组（L72 / L93 / L128），循环体断言文案分别是「A2 / A2/B / A2/B/C 事件类型 X **不得移除**」（L78 / L102 / L139）；另有 `HARNESS_RUN_EVENT_TYPES.length === 16` **三处**（L83 / L106 / L143）。

即：**删一个事件名要同时改 6 处守护口径**（frozen 清单去字面量 ×3 + length 16 → 15 ×3 + 注释写明退役批次），否则 CI 立刻红。这不是「顺手删一条成员」，而是**一次有意的词汇表修订**——需架构侧确认「A2 / Batch B / Batch C / ISS-004 的 additive-only 冻结契约」允许被后续批次修订（若不允许，本项应直接判定为不做，符号永久留档）。

另一侧的守护可安全保留：`harness-runtime.worker.test.ts:327` 的负向守护 `assert.equal((await listEvents(run.harnessRunId, "outbox_enqueued")).length, 0)` 实取 `listEvents(runId, eventType?: string)`（同文件 L96）第二参是**裸 string**、不依赖事件名联合类型 → 事件名从 union 删除后该守护仍可编译、仍能钉「库里不存在该事件行」，是防复活的长期防线（L166 注释同族）。

建议序（同一 commit 内）：① 先按上段改**6 处词汇表冻结断言**（使「少一类」成为显式决定而非静默漂移），保留 `worker.test.ts:327` 负向守护并在注释里写明「守卫已退役通道，防复活」；② 删类型 `HarnessWorkflowOutboxEntry` / 两处死导出（`HARNESS_OUTBOX_STATUSES` / `HarnessOutboxStatus`）；③ 再按 D-S7-04 决定是否同时删 `outbox?:` 字段与两处 `outbox: []` 写入；④ 全仓反向证据 `HarnessWorkflowOutbox|outbox_enqueued|HARNESS_OUTBOX` 非注释命中 = 0。

- **待裁（决策请求 D-S7-04）**：`outbox?:` 字段与两处 `outbox: []` 写入——实取后本项**不是输出契约风险**：`outbox` 是 workflow 的**内存返回值字段**，不落库（全仓 `.outbox` 命中仅测试文件，无持久化路径）；写入侧 `workbench-chat.workflow.ts:155 / :284` 返空数组，且其注释已写明「S2b-2 后补偿链已删，outbox 恒空」；消费侧 **worker 不读该字段**（除类型定义外零命中）。现存守护是 `workbench-chat.workflow.test.ts` 的 **5 处** `assert.deepEqual(outcome.outbox, [], "…恒空")`（L183 / :248 / :671 / :706 / :744）。因此两个选项：① **一次删净（建议）**——删字段 + 两处写入，5 处断言改写为「返回结构不再包含 `outbox` 键」的正向锁定（`assert.ok(!("outbox" in outcome))`）；守护从「恒空」升级为「结构上不可能非空」，严格更强；② 先标 `@deprecated` 保留一个周期——仅当架构侧认为外部（非仓内）有代码依赖该返回结构时才有意义。本项不需快照反查（字段不落库）。

## 6. 第 5 项 · 防漂移计数归零验证（§6）

前四项落地后 §6 基线预期形态（**目标口径，实取值交付时回填，不提前登记**）：`no-sync-store-io` 白名单由 7 条 / 12 处收敛——D15 若整删则 −4 处（#1）、B5 删 `saveJsonFile` 则 #7 由 2 递减；`single-doc-serial-scope` 10 条不动。剩余应只有 #2（`db/seed.ts`）+ rag-baseline 3 文件 + prompt-registry 共 **6 条既定豁免**。

本项**不产生新决策**，只做两件事：① 两道守卫的计数钉定断言随动更新并实跑绿；② 在 §6 写「阶段 2 归零验证结论」段，把「0 处在役业务 store I/O」这句话换成可复核的实取数字。**验收判据：`test:modules` 与 `test:modules:serial-store` 全绿 + skipped 0，且守卫日志逐条点名数与台账一致。**

## 7. 第 6 项 · trace-pg 空串提权约定加固——本批只出评估

**实取**：`apps/api/src/modules/trace/trace-pg.repository.ts` 的 `ownerUserId` 语义为「空字符串 = 不加 owner 条件 = 查全量」；调用侧 `trace.usecase.ts:294-298` 有 admin 门禁，因此**今天不构成可利用的越权**。问题在仓储层：函数签名与注释都不表达「传空即提权」，任何新调用方漏判就是水平越权。

**评估结论（供架构侧拍板，本批不实施）**：

- **该改**：这是「隐式作用域」类缺陷，与工单 §六 描述一致，改法成熟、影响面小。
- **改法**：把 `ownerUserId: string` 换成显式 `scope: { kind: "own"; ownerUserId: string } | { kind: "all" }`（或直接两个方法名 `queryTracesByOwner` / `queryAllTraces`），让「查全量」在调用点**自证**。
- **代价**：触碰 trace 仓储接口 + usecase + `trace-pg.repository.test.ts`（16 条用例）+ `trace.repository.test.ts`，属接口重构不是删代码，**与 S7「只删不加」的性质不同**。
- **建议（决策请求 D-S7-05）**：**不放进 S7**，单独立项为阶段 3 的 SYS-SEC 小批（可挂 DEF 台账）。理由：S7 是收官批，价值在于「删净 + 计数归零 + 可预测」；掺进接口重构会让收官批的回归面从「删除」变成「行为等价性证明」，两者混在一起反而都不好看。若架构侧坚持一起做，我按「独立 commit E、单独可 revert」隔离。

## 8. 台账里已点名归 S7 的存量债务（勾选式，不默认纳入）

| 项 | 内容 | 建议 |
|---|---|---|
| **B7** | `credentials.store.ts` 的 `importApiKeyIfAbsent` 孤儿导出（唯一生产调用点随 S3 消失，仅剩自身测试引用） | **纳入 S7**——同为死代码收口，面小（1 导出 + 其测试用例），与第 4 项同类 |
| **B9** | 文档持久化口径残留：`README.md:32`（teams 行）、`:77`（versions 行）、`:78`（团队行）、`03_技术设计/系统演进/实现与文档对齐说明.md:18`、`docs/代码目录清单与关系图.md:47`；须含 §3 的 `CONFIG_INTEGRITY_ON_STARTUP` 四处文档 | **纳入 S7**（与 D15 同批，文档口径不能等）——逐文件实取后清单化校正，不凭记忆改写 |
| **B10** | ① `types/index.ts` 的 `VersionsStore`（L301）/ `migrateVersionRecord`（L616）类型孤儿；② `generateProjectVersionCode` 两条 `PROJECT-{uuid}` 兜底分支无回归覆盖（dev 库 92 行零命中） | ① **纳入 S7**（与 B7 同类，删孤儿类型）；② **推阶段 3**（要新增用例，属加不改删） |
| **B4** | 14 个 `*.test.ts` 从不进任何 `test:*` 脚本（既存缺口，main 同样零引用） | **推阶段 3 另案**——纳入即从「删代码」变成「补 CI 覆盖」，红脸概率高、与收官目标冲突 |
| **B6** | `scripts/` 全目录不在任何 tsconfig include 内，类型缺陷长期不可见 | **推阶段 3**（工单 §十 已登记为阶段 3 候选） |
| **B8** | `credentials.store.test.ts` 五条用例的前置互斥（需 `CREDENTIAL_KEK` 才能跑） | **推阶段 3**（测试基建，与存储迁移无关） |
| **B11** | `versionCode` 唯一性只有应用层防线，DB 无非唯一约束之外的保护；同 owner 重复实取 8 组；check-then-act 竞态未消除 | **推架构侧单独立项**——需 partial UNIQUE 索引 + 先清历史重复，属数据治理决策，不塞进 S7 |

## 9. 建议的 S7 提交拆分与验收

沿用 A/B/C/D 范式，**每段单独可 revert**：

| commit | 内容 | 独立性 |
|---|---|---|
| **S7-a** | 防线先行：outbox 正向锁定断言改造（§5 步骤①）+ 守卫计数基线复跑 | 只改测试，JSON/死码全在，CI 应绿 |
| **S7-b** | 死代码删除：`saveJsonFile`（§2）+ outbox 5 项（§5，按 D-S7-04 裁决结果）+ B7 `importApiKeyIfAbsent` + B10① 两个类型孤儿 | 纯删除 |
| **S7-c** | D15（§3，按 D-S7-02 裁决结果二选一/整删）+ `main.ts` 启动段 + 三处 npm 脚本 + compose | 可独立 revert（回退即恢复启动校验） |
| **S7-d** | 开关终态退役（§4 代码侧）+ drift 白名单计数随动 | 反向证据：`WES_STORE_` 非注释命中 0 |
| **S7-e** | 文档口径校正（B9 + 部署文档 + `ENVIRONMENT.md`）+ 台账（§6 归零结论、§10 S7 交付行与收官行、基线变更行）+ 看板回填 | 纯文档 |
| `.env` | 按 D-S7-03 授权结果处理，**不进 commit**（文件不入 git） | — |

**验收口径**：① 每段推分支 CI，五 job 全绿、逐套 fail 0 / **skipped 0**；② 六套件计数变化**逐条对到具体用例**并登记「基线变更」行（S7-b 删 outbox / B7 / B10 相关用例可能引起 harness 与 credentials 套件减量，**须先实取再写，不预测**）；③ 两道防漂移守卫日志逐条点名数与 §6 台账逐位一致；④ 全仓反向证据三张清单（`WES_STORE_` / `outbox` 符号 / `config-integrity`）非注释命中归 0；⑤ 收官后在 §10 写「阶段 2 收官行」，明确「九域 JSON 读写路径全部删除、开关机制全退役、DB 成为九域唯一主存储」。

## 10. 决策请求汇总（请逐条拍板）

1. **D-S7-01**：`modelVerifyStatusPath` / `model-verify-status.json` 归属——豁免（建议）/ 迁 PG / 本批强删？
2. **D-S7-02**：D15 删除深度——**整删（建议：校验器已 `checked 0 files`，连 ops CLI 与 `CONFIG_INTEGRITY_ON_STARTUP` 全链一同删净）** / 降级保留空跑 CLI 只删启动调用 / 整删但不动部署文档与 compose？
3. **D-S7-03**：`apps/api/.env` 的 8 行死开关——授权执行侧直接删（建议）/ 交项目侧删？
4. **D-S7-04**：outbox `outbox?:` 字段与 `workbench-chat.workflow.ts` 两处 `outbox: []`——**一次删净并将 5 处「恒空」断言升级为「无此键」断言（建议）** / 先 `@deprecated` 保留一个周期？
5. **D-S7-05**：trace-pg 空串提权加固——推阶段 3 独立立项（建议）/ 并入 S7 作独立 commit E？
6. **勾选 §8 债务清单**：B7 / B9 / B10① 是否按建议纳入 S7；B4 / B6 / B8 / B10② / B11 是否确认推阶段 3 或另案。
7. **开工时点**：批准即开工，还是等 S5+S4 合入 main 后一并开工？（本批**强依赖** S5+S4 已在 main，见 §0。）
