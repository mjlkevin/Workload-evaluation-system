# 阶段 1 全量异步化实现计划（V1 · 8 批）

> 创建：2026-08-18（批 2 交付时落地本计划文档）
> 基线：批 1 分支 HEAD `d26c04f`（`qoder/phase1-batch1-rbac-async`）
> 状态：批 1 ✅ 已合入 main（df1472a）· 批 2 ✅ 已合入 main（f63a1ba）· 批 3 ✅ 已合入 main（60b1319，CI run #109 全绿）· 批 4 ✅ 已合入 main（d2a0a40，CI run #111 全绿）· 批 5 ✅ 已合入 main（76d4b46，CI run #115/#116 全绿）· 批 6 ✅ 已合入 main（6fc8f5e，main CI run #32161504235 全绿）· 批 7 ✅ 已合入 main（6fd78f4，main CI run #32162148878 全绿）· 批 8 ✅ CI 全绿（a78b930，CI run #32164521552 六套件 skipped 全 0）——阶段 1 收口报告已交付，待架构侧复核
> 决策记录：D12（准入验收更正）· D15（config-integrity 登记为阶段 2 删除对象）· D16（CI 门禁缺口修补：test:harness + test:agent 接入，准入更新为六套件）

## 1. 目标与边界

**阶段 1 唯一目的**：让阶段 2 能够「只换实现、不动调用点」。全部 store accessor 与命令式守卫的签名从同步改为 async，函数体一字不改（仍走 JSON 文件）。验收 = 现有测试全绿、行为零变化。

**边界**：

- 只改签名不改实现；任何 store 切换到 PG 属阶段 2，本阶段禁止提前。
- `ops/config-integrity.ts` **不转换**（自带 `fs.readFileSync`、不经过 store accessor、启动期一次性路径），登记为阶段 2 删除对象（D15）。
- 不得修改任何测试断言；若某测试必须改断言才能通过，说明引入了行为变化，立即停止报架构侧。

## 2. 基线重测（批 1 交付，架构侧采纳）

- 21 个 accessor、208 调用点（生产 139 / 测试 69）
- async/sync 分布：37 / 102（accessor 调用点）
- 命令式守卫调用点：**110**（实测，AST 复核。指令原文 108，差异见批 2 报告口径说明）
  - `requireAuth` 54 / `requireRoleWithAuth` 30 / `requireAdmin` 21 / `requireRole` 5
  - async 上下文 35 / sync 上下文 75
- 二阶包装：约 10 函数 34 处

## 3. 分批表（8 批）

| 批 | 内容 | 关键范围 | 状态 |
|---|---|---|---|
| 批 1 | rbac 四工厂闭包改 async 形态（await + 自 catch rejection，行为零变化） | `src/rbac/middleware.ts` | ✅ 已完成（`a8add52` + `d26c04f`，架构侧复核通过） |
| 批 2 | middleware/auth.ts 三守卫 async 化 + 全部 110 处调用点补 await | `requireAuth` / `requireRole` / `requireRoleWithAuth` + `system.usecase.ts` 本地 `requireAdmin`；18 文件 | ✅ 已合入 main（f63a1ba） |
| 批 3 | auth 域 store accessor | `middleware/auth.ts` 的 `loadUsersStore`/`saveUsersStore` + `auth.repository.ts` 4 个（invite codes / password reset tokens） | ✅ 已合入 main（60b1319，架构侧复核通过，CI run #109 全绿） |
| 批 4 | versions 域 store accessor | `versions.repository.ts` 2 个（load/saveVersionsStore）；三个特殊点见 §4a | ✅ 已合入 main（d2a0a40，CI run #111 全绿） |
| 批 5 | system 域 store accessor + 被强制拖入的 KB 二阶包装（2026-08-18 依赖测绘修正，见 §4b） | `system.repository.ts` 8 个（4 配置 store 家族 load/save）+ 3 个 KB 二阶包装（resolveActiveKnowledgeBaseConfig/resolveActiveKnowledgeBaseCatalog/resolveDraftKnowledgeBaseConfigForTest）；批 4 遗留 3 处跨批依赖点本批真正生效 | ✅ 已合入 main（76d4b46，CI run #115/#116 全绿；三个重点确认项全部通过，见进度记录） |
| 批 6 | team + 零散域 store accessor + T6 修复 + CI 六套件补齐 | `team.repository.ts` 3 + `templates` 2 + `rules` 2 + `sessions` 3 + CI(harness/agent) + T6 确定性时钟 | ✅ CI 全绿（cc13f8e，CI run #32155412066；含根因修复：根 package.json 补 test:agent 路由 + test:rules/test:agent 移除 testcontainers） |
| 批 7 | trace + knowledge + model-verify-status（范围决策纳入） | `trace.repository.ts` 6 个同步函数、`knowledge.repository.ts` 的 `KnowledgeRepository` 类方法、`system-effective.ts` 的 `loadModelVerifyStatus`/`saveScenarioVerifyRecord` | ✅ CI 全绿（1b4dfc7，CI run #32155500875，rebase 于 cc13f8e） |
| 批 8 | ai-sessions + 剩余二阶包装 + 收尾证明（原批 9「启动路径」因 D15 失去主体内容，并入本批） | `ai-sessions.repository.ts` 2 个 + 剩余二阶包装（批 5 拖走 KB 三个后的其余部分）；CI 全绿确认 + 「全仓无同步 store I/O」防漂移证明 | 待开工 |

## 4a. 技术债期限登记（2026-08-18 架构侧定级）

- **D16 CI 门禁缺口修补（2026-08-18，架构侧漏项）**：ci.yml 原只跑 5 套件（test:rules/test:web/test:ai/test:modules/test:integration），test:harness（191 用例）与 test:agent（51 用例）不在 CI。根因：架构侧制定阶段 1 准入条件时只列举了四个套件名称。批 5 报告中主动披露。修补：①ci.yml test-with-db job 加入 test:harness + 全部步骤统一设 `TEST_DATABASE_URL`；②test-non-db job 加入 test:agent；③test-setup.mts 外部已提供 `TEST_DATABASE_URL` 或 `DATABASE_URL` 时跳过 testcontainers（避免与 GitHub Actions postgres service 冲突）；④阶段 1 准入条件从五套件更新为六套件。同时修复 T6（harness 退避时序 flaky）：不接受「放宽容差」，coordinator 新增 `now?: () => Date` 注入点，测试改为确定性时钟 + 精确断言。验收：CI 中 test:harness 191 用例全跑、skipped 为 0、退出码 0。

- **ISS-2026-08-18-003（CI test:modules 并发挂起，现以 `--test-concurrency=1` 串行绕行）**：期限 = **阶段 2 开工前必须完成根因定位**，给出结论（修复 / 确认串行为长期方案 + 理由）。若确认串行长期化，需同时给出 CI 时长增长预估与应对（分片、分 job 等）——阶段 2 依赖数据库的测试文件将从 17 个增至约 31 个，串行 + 数据库预计 CI 时长翻倍。责任人：阶段 2 开工者；登记于本文件与总看板问题池 ISS-2026-08-18-003。
- **ISS-2026-08-18-001（MSW 逃逸）**：提级为**前端问题池首位**；第二点（`onUnhandledRequest:'error'` 为何未拦下未匹配请求）优先于第一点（为何未 mock），因其直接决定前端门禁（CI `test-web` job）的实际强度。批 4 交付时附分诊结论。
- **ISS-2026-08-18-004（versions store 损坏文件静默当空库）**：`loadVersionsStore()` 的 `catch { return { records: [] } }` 模式使 JSON 损坏从可恢复变为不可恢复（下一次 save 把「空」落盘）。**阶段 1 不修**（修它属行为变化）；登记为**阶段 2 处理对象**——该 store 切换 PG 时此模式自然消失，但需确认新实现不重复此模式（读取失败必须显式报错，不得静默空库）。
- **批 4 跨批依赖点（特殊点 2）**：`versions.repository.ts` 的 `repairGlobalPlaceholderVersionCodes` 内调用 `loadVersionCodeRulesStore()`（system 域，**属批 5 范围**）。批 4 中它仍是同步函数，补 `await` 不改变行为；**批 5 执行时须确认该调用点不被遗漏**（批 5 把它改异步后，此处 `await` 才真正生效）。批 5 开工指令确认共 3 处跨批依赖点本批真正生效：`versions.repository.ts:27`、`versions.usecase.ts` `generateVersionCodeByRule`、`project-evaluations.usecase.ts` `generateProjectVersionCode`。
- **ISS-2026-08-18-001 修复方向裁决（2026-08-18 架构侧）**：采纳方案 a，机制必须是「收集器 + 框架层断言」——`setup.js` 的 `onUnhandledRequest:'error'` 改为收集器回调（method+url 记入模块级数组，不抛出），全局 afterEach 断言数组为空、非空以清单失败并清空；断言在测试框架层执行，无法被应用层 `.catch(() => {})` 吞掉。方案 c（全局 unhandledRejection）不可行（rejection 已被空 catch 处理，不触发事件）；方案 b（补单个 mock）不接受为最终方案（17 个盲点只修 1 个）。预期副作用：会浮出更多隐藏的未匹配请求，逐个补 mock 或修调用点，不得放宽断言/加白名单（确需白名单必须注释写明理由并显式加入）。排期：不阻塞批 5，阶段 1 结束前完成。另：17 处 `.catch(() => {})` 吞错作为生产层面问题另案登记（ISS-2026-08-18-005），不属阶段 1 范围。

## 4b. 批 5 依赖测绘（2026-08-18，开工前架构侧要求）

**8 个 accessor 直接调用点（生产，不含测试）共 53 处 / 14 文件**：

| accessor | 调用点 | 文件分布 |
|---|---|---|
| loadVersionCodeRulesStore | 7 | versions.repository:27（批 4 已 await）、versions.usecase:44、system.usecase×4（145/185/208/230）、project-evaluations.usecase:20 |
| saveVersionCodeRulesStore | 3 | system.usecase（196/218/239） |
| loadRequirementSystemConfigStore | 22 | main.ts:51（启动路径）、harness-boot.ts:140、harness.usecase.ts:362、system.usecase×7、ai.repository.ts:16、extractor.service×2、assessment.service:38、workbench-shared×2、workbench-chat-stream×2、company-profile.handler、report-analysis×2 |
| saveRequirementSystemConfigStore | 2 | system.usecase（305/326） |
| loadImplementationDependencyRulesStore | 5 | estimates.usecase×2（98/287）、system.usecase×3（716/735/748） |
| saveImplementationDependencyRulesStore | 2 | system.usecase（741/753） |
| loadKnowledgeBaseConfigStore | 7 | system.usecase×4（803/842/887/1027）+ **system.repository 内 3 个二阶包装（1220/1267/1334，强制拖入）** |
| saveKnowledgeBaseConfigStore | 3 | system.usecase（860/935/1072） |

**二阶包装确认结论**：

- **KB 家族 3 个必须并入批 5**（直接调用 `loadKnowledgeBaseConfigStore`，同步调用异步编译不过）：`resolveActiveKnowledgeBaseConfig`（:1210，调用点 :1220）、`resolveActiveKnowledgeBaseCatalog`（:1266，调用点 :1267）、`resolveDraftKnowledgeBaseConfigForTest`（:1319，调用点 :1334）。三者下游调用点共 6 处：knowledge-query.handler.ts×2（91/92）、rag-baseline.cli.ts:69、agent/default-registry.ts:89、system.usecase.ts×2（898/1037）。
- **KimiApiKey 家族 3 个确认不被拖入**：`resolveActiveRequirementKimiApiKey`（:425）/`resolveActiveApiKeyForScope`（:433）/`resolveDraftKimiApiKeyForTest`（:447）走 `getCachedApiKey`（`credentials.store.ts:35`，凭据域 DB 缓存）+ 环境变量，**不依赖本批 8 个 accessor**，无需同批变异步。其约 13 个调用点（harness-boot/harness.usecase/system.usecase/ai.repository/workbench 系列/report-flow/company-profile/extractor/assessment）本批零改动。

**批 5 规模汇总**：8 accessor 签名 + 3 二阶包装签名；生产调用点 53 + 6（二阶下游）= 59 处补 await，涉及约 17 个生产文件（最大级联源 `system.usecase.ts` 单文件 21 处；`main.ts:51` 启动路径与 harness 域为敏感点）；测试侧涉及 5 个测试文件（modules.handlers.test、system.kb-config.test、system.repository.test、workbench-dispatch.service.test、assessment.service.test）。

**批 5 工时重估：约 2.5 人日**（实现与 tsc 级联 ~1.0，其中 system.usecase 21 处 + AI handlers 为主要工作量；五套件/10 轮验证 ~0.75；CI/合入/文档 ~0.5；缓冲 ~0.25）。对照：批 4（15 文件 ~33 处）实际约 1 人日。风险：main.ts 启动路径与 harness 超时取值点若处于非 async 上下文需结构微调（仍属签名级联，不动语义）。**按指令报架构侧确认后再实现。**

## 4. 批 2 验证矩阵

- [x] `npx tsc --noEmit` 退出码 0
- [x] `npm run build -w apps/api` 退出码 0
- [x] 六套件本地退出码 0 且 skipped 全 0：`test:modules` 380 / `test:rules` 8 / `test:ai` 287 / `test:harness` 191（带容器/`TEST_DATABASE_URL`）/ `test:agent` 51 / `test:web` 356（D16 更新：准入条件从五套件更新为六套件，新增 test:harness + test:agent）
- [ ] CI 全绿（分支未推送，推送后确认）
- [x] diff 中零测试断言修改（`git diff` 断言行增删 0 行）

**批 2 特殊风险确认（响应后仍有后续语句）**：对 75 处需级联改签名的位置逐个确认「响应写出之后是否还有后续语句」。结论：**无高风险模式**（日志/缓存写入/状态清理类尾随语句 0 处）。AST 扫描命中 3 处低风险候选：①`requireRole`（auth.ts:185）与 ②`requireRoleWithAuth`（auth.ts:209）——403 响应后仅 `return false/null` 控制流，无副作用；③`harness.controller.ts` `eventsHandler`（SSE）——`res.write`×2 + `res.end()` 全部属写响应本身，`res.end()` 后无语句。三处均为既有结构，批 2 未新增/移动任何语句（diff 中新增行全部为 async 标记 / await 前缀 / 签名行），响应写出的相对顺序不变。

## 5. 阶段 1 最终证明（批 8 交付）

「全仓无同步 store I/O」的可复核证明：做成防漂移测试用例（而非一次性命令），防止后续新增代码重新引入同步 I/O：

- 断言 `readFileSync` / `writeFileSync` 不出现在任何 store 仓储与请求路径
- 允许例外：`ops/config-integrity.ts`（启动期路径，D15 已登记为阶段 2 删除对象）、`db/seed.ts` 的 seed 源读取

## 6. 进度记录

- 2026-08-17 批 1 交付：rbac 四工厂 async 化 + 基线重测，架构侧复核通过。
- 2026-08-18 批 2 开工：范围决策落定（纳入 trace/knowledge/system-effective、排除 config-integrity → D15），本计划文档建立。
- 2026-08-18 批 2 完成：三守卫 async 化 + 110 处调用点补 await（18 文件）+ 测试调用点补 await（断言零修改）；tsc/build/五套件全绿（预存 flake 已留痕）；待架构侧复核。
- 2026-08-18 批 1/批 2 合入 main：批 1（df1472a，含 test:modules 串行化 eef0172，CI run #103/#104 全绿）、批 2（f63a1ba，rebase 不叠加分支，CI run #105/#108 全绿）。
- 2026-08-18 批 3 完成：auth 域 6 个 store accessor 异步化（函数体一字未动）+ 生产调用点 27 处补 await（requireAuth:161 + auth.usecase 26）+ 测试 9 文件补 await（6 个辅助函数级联 async 化，断言行语义零修改）；tsc/build 0 错，五套件全绿（modules 380 / rules 8 / integration 1 / ai 287 / harness 191，skipped 全 0）；10 轮 test:modules 连跑全绿（380/380/0/0 × 10，单轮 ~16s）；待架构侧复核。
- 2026-08-18 批 3 复核通过并合入 main：架构侧核实 6 accessor 签名 async、函数体零改动、唯一断言变更为 await 括号包装（语义等价）。推送 CI run #109 全绿（lint/tsc/test-web/test-non-db/test-with-db 五 job 通过），--no-ff 合入 main（60b1319）。
- 2026-08-18 技术债定级：ISS-2026-08-18-003 期限登记（阶段 2 开工前根因定位）；ISS-2026-08-18-001 提级前端问题池首位（第二点优先）；ISS-2026-08-18-004 新入池（versions store 损坏静默空库，阶段 2 处理对象）。见 §4a。
- 2026-08-18 批 4 开工：versions 域 2 accessor 异步化；三个特殊点已确认（读中带写回写路径、跨批调用批 5 的 `loadVersionCodeRulesStore`、损坏静默空库本批不修），见 §4a。
- 2026-08-18 批 4 完成并合入 main：`loadVersionsStore`/`saveVersionsStore` 签名改 async（函数体仍 readFileSync/writeFileSync 未动），级联范围超出基线：versions.usecase 20 处补 await + project-evaluations（repository 5 函数、usecase 5 函数、controller 3 处）、team（usecase 2 函数、controller 2 处）、wbs.routes、agent/default-registry（WBS 工具回调）、workbench-context/dispatch 服务；测试 4 文件级联（3 个辅助函数 async 化，批 3 同类先例）。断言行变更仅 1 处 await 括号包装（harness.usecase.test.ts L1221，语义等价）。特殊点 1：回写路径 await 在 try 块内，回写失败仍走既有 catch，与同步版一致。特殊点 2：跨批依赖点位于 `versions.repository.ts` 的 `repairGlobalPlaceholderVersionCodes`（已补 await + 注释标记）。特殊点 3：未修，ISS-2026-08-18-004 已入池。验证：tsc/build 0 错，五套件全绿（modules 380 / rules 8 / integration 1 / ai 287 / harness 191，skipped 全 0），10 轮 test:modules 全绿；CI run #111 五 job 全绿，--no-ff 合入 main（d2a0a40）。
- 2026-08-18 批 4 复核通过 + 批 5 范围修正：架构侧复核通过（特殊点 1/2/3 与唯一断言变更全部确认）；ISS-2026-08-18-001 裁决采纳方案 a（收集器 + 框架层断言），见 §4a。批 5 依赖测绘完成（§4b）：8 accessor 生产调用点 53 处/14 文件；KB 二阶包装 3 个强制并入批 5（下游 6 处）；KimiApiKey 家族 3 个确认不被拖入（走凭据域 DB 缓存非 store accessor）；批 8 重定义为「剩余二阶包装 + CI 全绿 + 防漂移证明」。批 5 工时重估约 2.5 人日，已报架构侧待确认，实现暂缓。
- 2026-08-18 批 5 开工指令下达（ISS-001 复核通过 + 2.5 人日获批）并完成合入：8 accessor + 3 KB 二阶包装签名 async 化（函数体仍 readFileSync/writeFileSync 未动，四组样板零重构）；59 处补 await + 6 个计划外级联函数（findProviderOr404 / generateVersionCodeByRule / validateImplementationDependencies / calculateEstimateOnly / getActiveImplementationDependencyRules / AiUsecase.getRequirementSettings，均经调用链核实）；7 处对象字面量内调用提升为前置变量（harness-boot / workbench-shared / workbench-chat-stream / report-analysis）。三个重点确认项全部通过：①三处跨批点 await 就位并真正生效；②main.ts:51 仍在凭据预热 async IIFE 内，仅补 await；③harness-boot:140 在 async generator 内，提升变量处理。测试侧 7 文件 48 错全部处理，断言零修改（仅 await/async 级联 + 1 处 Awaited<ReturnType> 类型解包）。验证：tsc/build 0 错；modules 380/rules 8/ai 287/integration 1/web 356 全绿 skipped 全 0；test:modules 10 轮全绿；CI run #115 五 job + run #116 六 job（含 docker-build）全绿，--no-ff 合入 main（76d4b46）。harness 套件专项说明：该套件不在 CI（ci.yml 已核实），本地无容器时 76 个 DB 依赖用例按 `skip: !testDatabaseUrl` 设计性跳过；colima + testcontainers 容器模式下 skipped=0、190/191 过，T6（退避时序断言，宿主/VM 时钟漂移 ~70ms 超 150ms 容差窗）稳定失败、T7a 偏 flaky——两者在批 5 前 base（da05966）同样复现，属既有环境问题非本批引入，待架构侧裁决是否入池。
- 2026-08-18 批 5 实际工时约 1 人日（实现与级联 ~0.5，验证 ~0.3，CI/合入/文档 ~0.2），显著低于 2.5 人日重估：成本未集中在预估的 KB 下游级联（实际仅 6 处且均在 async 上下文），system.usecase.ts 21 处为 replace_all 批量模式成本低；真正额外成本在 6 个计划外级联函数的调用链核实。按指令下调批 6–8 估值：批 6（10 accessor）约 0.75 人日，批 7（trace 6 函数 + knowledge 类方法 + verify-status）约 1 人日（knowledge.repository 类方法级联面待测绘），批 8（ai-sessions 2 + 剩余二阶包装 + 防漂移证明）约 0.75 人日。
- 2026-08-18 ISS-2026-08-18-001 修复完成并合入 main：按裁决落地「收集器 + 框架层断言」（setup.js 收集器回调 + afterEach 断言，断言不在应用代码可触及的 promise 链上，无法被吞错 catch 架空）；负向对照验证成立；浮出的三类未匹配请求逐个补齐默认 handler（各带理由注释，未放宽断言、无白名单）；test:web 356/356 × 3 轮全绿 + build:web 通过，生产代码零改动；CI run #113 全绿，--no-ff 合入 main（9a31a4d）。17 处吞错另案登记 ISS-2026-08-18-005。
- 2026-08-18 批 6 完成：10 accessor 签名 async 化（team 3 + templates 2 + rules 2 + sessions 3，实际测绘 project-evaluations 已全部 async、0 改动）；级联：team.usecase 12 函数、templates.usecase 4 await、rules.usecase 3 await、sessions.usecase 2 函数、team.controller 11 await、sessions.controller 2 await。两个重点确认项：①`saveTeamStoreWithExpectedVersion` 读版本与写入之间无新让出点（readFileSync + writeFileSync 同步顺序，补 await 不扩大竞态窗口）；②project-evaluations 域批 4/5 已级联完成，本批 0 改动无重复。T6 修复：coordinator 新增 `now?: () => Date` 选项，传入 `scheduleRunRecovery`（已支持 `input.now`），T6 测试改为注入确定性 now + 精确断言（`availableAt === injectedNow + backoffMs`，不容差），消除宿主/VM/DB 时钟漂移 flaky。CI 门禁缺口修补（D16）：ci.yml 加入 test:harness（test-with-db job）+ test:agent（test-non-db job），准入条件更新为六套件；test-setup.mts 外部已提供 DB URL 时跳过 testcontainers；test-with-db 全部步骤统一设 `TEST_DATABASE_URL`。测试变更：机械性 await/async 级联（零断言逻辑变更）。验证：tsc/build 0 错；六套件全绿（modules 380 / rules 8 / harness 191 pass=115+skipped=76 / agent 51 / web 356 / ai 287 / integration 1）；10 轮 test:modules 全绿（380 × 10 = 3800 次执行）；CI run #32155412066 全绿（5 job 全部 success）。
- 2026-08-19 批 8 完成（阶段 1 收官批次）：ai-sessions 域 `loadAiSessionsStore`/`saveAiSessionsStore` + `appendAiSessionMessageIdempotent` 签名 async 化（函数体一字未动）；级联：usecase 7 函数、controller 9 处 await、`workbench-shared.ensureHomeAiSession`、workbench-chat/chat-stream/report-flow handlers（`||` 回退模式 await 解包）、`workbench-chat.workflow` 契约 2 处同步→Promise、`harness-session-sink.append`、workbench-view.usecase 1 处。剩余二阶包装测绘：除 ai-sessions 域外为空（KB 3/verify-status 2/KimiApiKey 家族/纯函数/批 4 项均已在前批消化）。核心交付 1：全仓无同步 store I/O 防漂移测试（`no-sync-store-io.drift.test.ts`，TS AST 扫描 src 非测试文件，豁免 = 模块顶层 async 且命名 `/^(load|save)[A-Z]\w*$/` 的函数体 + 8 条文件级白名单附理由；RED 17 处违例 → 白名单逐项裁决 → GREEN；失败输出「文件:行:列 + 函数」）；核心交付 2：skip 计数守卫（test-setup.mts：CI=true 且 TEST_DATABASE_URL 缺失 → 抛错使 DB 依赖套件失败，本地保持跳过）。测试变更：机械性 await/async 级联为主；断言消息文字更新 1 处（harness-runtime.usecase.test.ts「缺省路径必须保持同步布尔语义」→「缺省路径保持原有布尔语义（await 解包）」，随签名语义更新）；`deleteAiSession` 重载签名 1 由 boolean → Promise<boolean>（D5 混合签名，IIFE 最小差异保留）；workflow 契约 `appendSessionMessage`/`getSessionRecord` 返回类型同步→Promise（stub 3 处 + no-op dep 1 处 async 化）。验证：tsc/build 0 错；六套件全绿（modules 381 含新防漂移测试 / rules 8 / harness 191 pass=115+skipped=76（本地无 DB，CI 全跑）/ agent 51 / ai 287 / integration 1）；test:integration 本地首跑 api_server_not_ready 系 worktree 缺 `apps/api/.env.local`（gitignore 文件），复制主 worktree 环境文件后 pass 1/1，确认非代码回归；CI run #32164521552 全绿（六套件 skipped 全 0，`gh run view --log` 实取）。批 8 实际工时约 0.7 人日（实现与级联 ~0.35、防漂移测试与白名单裁决 ~0.15、守卫/验证/CI/报告 ~0.2），vs 估值 0.75。
- 2026-08-18 CI 根因修复（cc13f8e，批 6 分支 amend 替换 c48de9e）：架构侧指出原根因判定（testcontainers 启动失败）不成立——同 job 同 runner 的 test:rules（挂载 --test-global-setup）先成功（10-12s），仅 test:agent <1s 失败，时间特征与容器启动失败不符。重新定位：**真正根因为根 `package.json` 缺少 `test:agent` 路由脚本**（`npm run test:agent -w apps/api`）。CI 从仓库根执行 `npm run test:agent`，npm 报 `Missing script: "test:agent"` → exit 1，瞬间失败。修复：①根 `package.json` 补 `"test:agent": "npm run test:agent -w apps/api"`；②按架构侧要求提前落地批 8 清理项：`test:rules` 和 `test:agent` 均移除 `--test-global-setup=./test-setup.mts`（test-non-db job 彻底不接触容器）。CI run #32155412066（批 6）和 #32155500875（批 7）全部全绿。日志获取说明：GitHub Actions 日志下载需 repo admin 权限（API 和浏览器均返回 403），根因通过 GitHub API 时间戳分析（test:agent 在 6 次 CI run 中一致 <1s 失败）+ npm Missing script 复现确认。
