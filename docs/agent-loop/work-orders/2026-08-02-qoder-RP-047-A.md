# Qoder Work Order · RP-047-A

date: 2026-08-02
taskId: RP-047-A
issue: ISS-2026-08-02-003
requirement: RP-047
batch: A / 持久运行基础
executor: qoder1
reviewer: codex
finalAcceptor: user-owner
approvedDesignCommit: `e1c8961`
status: `ASSIGNED / WAITING_QODER_ACK`
allowNextTask: `false`
mainMergeAllowed: `false`
deliveryStatusAllowed: `已回填 / 待 Codex 复核`

## 1. Objective

按照已批准的 RP-047 规格和 Batch A 实施计划，建立 PostgreSQL-backed Harness
持久运行基础：

1. 扩展 Run 状态与 durable workflow 元数据；
2. 新增 Attempt、Checkpoint、Event、Output、Session Outbox 表；
3. 为 tool effect、artifact、submission、active session、event sequence、checkpoint、
   output 和 outbox 建立数据库幂等约束；
4. 实现 owner-safe 队列认领、lease heartbeat、事件序号、检查点追加、输出更新和
   outbox 入队 repository；
5. 用 Testcontainers 证明干净库迁移、已有 Harness 数据兼容、并发认领和幂等行为。

本批只交付基础设施。不得接真实模型、不得启动 Worker、不得修改 AI 页面、不得新增
异步 Run API、不得切换现有发送入口。

## 2. Approved Source Of Truth

按以下优先级执行：

1. `AGENTS.md`；
2. `codex-project-registry.md`；
3. 本 Work Order；
4. `docs/superpowers/specs/2026-08-02-rp-047-durable-multichannel-checkpoint-runtime-design.md`；
5. `docs/superpowers/plans/2026-08-02-rp-047-batch-a-durable-run-foundation.md`；
6. `QODER.md` 与 Qoder 协作协议。

若旧协议示例出现 `/Users/kevin/AI/Workload-evaluation-system-agent`，一律视为已注销历史
路径。唯一有效项目入口是 `/Users/kevin/AI/Workload-evaluation-system`。

本工单不存在留给 Qoder 自行选择的架构问题。若计划与当前代码无法同时成立，停止并
回填证据，不扩大范围、不自行改规格。

## 3. Project And Worktree Contract

- projectRoot: `/Users/kevin/AI/Workload-evaluation-system`
- webMainline: `ui/V2_PROTOTYPE`
- approvedDesignCommit: `e1c8961`
- assignmentCommit: 由 Codex 在派单消息中给出，必须包含本 Work Order 与 Batch A plan
- baseCommit: 与 assignmentCommit 相同，ACK 时用 `git rev-parse HEAD` 记录完整 SHA
- worktreePath: `/Users/kevin/AI/Workload-evaluation-system/.claude/worktrees/rp-047-a-durable-run-foundation`
- branch: `qoder/rp-047-a-durable-run-foundation`
- unrelatedChangesPolicy: `preserve-and-stop-on-overlap`

Qoder 必须从 assignmentCommit 创建独立 worktree。不得在当前主 checkout 编辑；不得从
未提交 working tree 复制文件；不得把用户其他 dirty changes 带入分支。

建议创建命令：

```bash
cd /Users/kevin/AI/Workload-evaluation-system
git worktree add \
  /Users/kevin/AI/Workload-evaluation-system/.claude/worktrees/rp-047-a-durable-run-foundation \
  -b qoder/rp-047-a-durable-run-foundation \
  <assignmentCommit-from-Codex-message>
```

尖括号内容来自 Codex 派单消息，不写入脚本或提交。创建后立即：

```bash
cd /Users/kevin/AI/Workload-evaluation-system/.claude/worktrees/rp-047-a-durable-run-foundation
git status --short --branch
git rev-parse HEAD
git merge-base --is-ancestor e1c8961 HEAD
```

Expected: worktree 干净；HEAD 等于 assignmentCommit；最后一条命令退出码为 0。

## 4. Mandatory Reading Order

任何编辑前完整读取：

1. `AGENTS.md`
2. `codex-project-registry.md`
3. `QODER.md`
4. `skills/speak-plainly/SKILL.md`
5. `skills/wes-qoder-worktree-protocol/SKILL.md`
6. `skills/wes-qoder-worktree-protocol/references/protocol.md`
7. `skills/wes-multi-agent-collaboration/SKILL.md`
8. `docs/superpowers/specs/2026-08-02-rp-047-durable-multichannel-checkpoint-runtime-design.md`
9. `docs/superpowers/plans/2026-08-02-rp-047-batch-a-durable-run-foundation.md`
10. `docs/agent-loop/work-orders/2026-08-02-qoder-RP-047-A.md`

实施计划要求的 `superpowers:executing-plans` 或等价 Qoder 执行计划能力必须在修改代码前
启用；实现任何功能或修复前遵循 TDD。

## 5. Required First Reply: Worktree Contract ACK Only

Qoder 第一条回复只能输出以下 ACK，填入真实值，然后停止等待 Codex/用户确认；不得在
同一回复开始编辑：

```yaml
worktreeContractAck:
  taskId: RP-047-A
  projectRoot: /Users/kevin/AI/Workload-evaluation-system
  worktreePath: /Users/kevin/AI/Workload-evaluation-system/.claude/worktrees/rp-047-a-durable-run-foundation
  branch: qoder/rp-047-a-durable-run-foundation
  baseCommit: <full-assignment-commit-sha>
  approvedDesignCommit: e1c8961
  worktreeClean: true
  allowedPaths:
    - apps/api/src/db/schema/harness.ts
    - apps/api/src/modules/harness/harness.types.ts
    - apps/api/src/modules/harness/harness.module.ts
    - apps/api/src/modules/harness/harness-runtime.types.ts
    - apps/api/src/modules/harness/harness-runtime.types.test.ts
    - apps/api/src/modules/harness/harness-runtime.repository.ts
    - apps/api/src/modules/harness/harness-runtime.repository.test.ts
    - apps/api/src/modules/harness/harness-runtime.migration.test.ts
    - apps/api/src/modules/harness/harness.usecase.test.ts
    - apps/api/src/routes/harness.routes.test.ts
    - apps/api/package.json
    - apps/api/drizzle/0014_*.sql
    - apps/api/drizzle/meta/0014_snapshot.json
    - apps/api/drizzle/meta/_journal.json
  forbiddenPaths:
    - ui/**
    - apps/api/src/modules/ai-sessions/**
    - apps/api/src/routes/ai-sessions.routes.ts
    - apps/api/src/services/ai/**
    - docs/openapi.yaml
    - 03_技术设计/系统架构/WES-Agent-升级总看板/**
    - config/**
    - apps/web/**
    - ui/V0_SAAS/**
  validationCommands:
    - USE_TESTCONTAINERS=true npm run test:harness -w apps/api
    - npm run test:modules
    - npm run build:api
    - npm run build:web
    - git diff --check
  stopConditions:
    - worktree 不干净或 HEAD 不是 assignmentCommit
    - approvedDesignCommit 不是 HEAD 祖先
    - 需要修改 allowedPaths 之外的文件
    - 需要新增依赖或修改 package-lock.json
    - 迁移出现 DROP/TRUNCATE/DELETE 或其他业务域变更
    - Testcontainers 无法完成真实 PostgreSQL 迁移测试
    - 发现基线已存在冲突实现或共享文件漂移
    - 需要真实数据库、真实 provider、生产 feature flag 或外部密钥
```

ACK 中的尖括号必须换成完整 SHA。ACK 被接受前不得执行任何写操作。

## 6. Allowed Paths

只允许创建或修改：

```text
apps/api/src/db/schema/harness.ts
apps/api/src/modules/harness/harness.types.ts
apps/api/src/modules/harness/harness.module.ts
apps/api/src/modules/harness/harness-runtime.types.ts
apps/api/src/modules/harness/harness-runtime.types.test.ts
apps/api/src/modules/harness/harness-runtime.repository.ts
apps/api/src/modules/harness/harness-runtime.repository.test.ts
apps/api/src/modules/harness/harness-runtime.migration.test.ts
apps/api/src/modules/harness/harness.usecase.test.ts
apps/api/src/routes/harness.routes.test.ts
apps/api/package.json
apps/api/drizzle/0014_*.sql
apps/api/drizzle/meta/0014_snapshot.json
apps/api/drizzle/meta/_journal.json
```

`0014_*.sql` 必须由 `npm run db:generate -w apps/api` 生成且恰好一个。不得手工创建第二个
migration，不得修改 0000–0013 migration。

## 7. Explicitly Forbidden

- 不得修改主 checkout；
- 不得使用 `git reset --hard`、`git clean -fd`、`git restore .`；
- 不得 rebase、merge main、cherry-pick 无关提交或推送远端；
- 不得修改任何总看板 HTML/JSON/event；
- 不得修改 AI Session、AI routes、dispatch service、前端或 OpenAPI；
- 不得创建 Worker、Recovery Coordinator、SSE 或新 HTTP route；
- 不得连接或迁移开发、演示、生产数据库；
- 不得使用 `DATABASE_URL` 运行迁移演练；只允许临时 Testcontainers 数据库；
- 不得调用真实模型、外部 API 或读取真实 API Key/token/cookie；
- 不得新增 Redis、Kafka、BullMQ、数据库依赖或第二套状态框架；
- 不得用宽泛类型断言、跳过测试、删除旧测试或降低现有约束让构建通过；
- 不得宣布“已交付”、不得合并 main、不得领取 RP-047-B。

## 8. Implementation Contract

严格逐项执行：

`docs/superpowers/plans/2026-08-02-rp-047-batch-a-durable-run-foundation.md`

不可变要求：

1. `harness_runs` 保留现有 file-analysis 语义，新列通过默认值兼容历史行；
2. `workbench_chat` 同一 aiSessionId 同时最多一个活动 Run；
3. `(ownerUserId, submissionKey)` 保证提交幂等；
4. owner 查询必须在 SQL where 中同时约束 owner 与 runId；
5. queue claim 必须使用 PostgreSQL `FOR UPDATE SKIP LOCKED`；
6. 同一 Run 同时最多一个 `claimed/running` Attempt；
7. 心跳必须绑定 attemptId + workerId + 未过期 lease；
8. event sequence 通过 `harness_runs.event_sequence` 原子递增，禁止 `MAX+1`；
9. checkpoint 必须包含 workflow/version、step、resumePolicy、stateHash 和 Runtime validation；
10. 同 checkpointKey 的不同 stateHash 必须明确冲突，不能静默覆盖；
11. tool effect、artifact、output、outbox 都有数据库唯一约束；
12. outbox 的 aiSessionId 必须与 Run 绑定一致；
13. final output 不得回退为 partial；
14. JSON runtime payload 上限 1 MiB；错误信息不得泄露 prompt、附件和凭据；
15. 所有迁移 additive；旧同步入口保持不变。

## 9. TDD And Commit Discipline

每个 Task 都要遵循：

1. 先写一个最小失败测试；
2. 运行并记录 RED 命令、退出码和失败原因；
3. 写最小实现；
4. 运行 focused GREEN；
5. 再做必要重构；
6. 运行 Task 级回归；
7. 按计划提交。

计划内提交顺序：

```text
feat(WES Agent): RP-047-A · 冻结持久运行状态契约
feat(WES Agent): RP-047-A · 建立持久运行数据库结构
feat(WES Agent): RP-047-A · 实现持久队列与租约认领
feat(WES Agent): RP-047-A · 固化检查点与事件幂等原语
chore(WES Agent): RP-047-A · 验证持久运行迁移兼容
```

如某一步因同一文件需要合理合并提交，可在 handoff 解释，但不得提交无关格式化或大范围
机械改写。

## 10. Required Verification

### 10.1 Focused RED/GREEN

按计划保存每个 focused test 的 RED 与 GREEN 证据。数据库测试必须使用临时
PostgreSQL 17 Testcontainer。

### 10.2 Final automated matrix

从 worktree 根目录运行：

```bash
USE_TESTCONTAINERS=true npm run test:harness -w apps/api
npm run test:modules
npm run build:api
npm run build:web
git diff --check
```

Harness 结果中 `harness-runtime.migration.test.ts` 不得 skip。若本机 Docker/Testcontainers
不可用，本批不能通过；停止并回填阻塞证据，不得用本地开发库替代。

### 10.3 Scope audit

```bash
git diff --name-only <full-assignment-commit-sha>...HEAD
git status --short --untracked-files=all
git diff --check <full-assignment-commit-sha>...HEAD
```

所有路径必须属于 Allowed Paths；worktree 最终无未提交修改。

### 10.4 Migration safety audit

```bash
ls apps/api/drizzle/0014_*.sql
rg -n "DROP TABLE|DROP COLUMN|TRUNCATE|DELETE FROM" apps/api/drizzle/0014_*.sql
rg -n "TODO|TBD|FIXME|placeholder|not implemented" \
  apps/api/src/modules/harness/harness-runtime.types.ts \
  apps/api/src/modules/harness/harness-runtime.repository.ts \
  apps/api/src/modules/harness/harness-runtime.types.test.ts \
  apps/api/src/modules/harness/harness-runtime.repository.test.ts \
  apps/api/src/modules/harness/harness-runtime.migration.test.ts
```

第一条必须只列一个 migration；后两条必须无输出。

## 11. Required Handoff

最终回复按 `docs/codex-workflows/external-ai-handoff-template.md` 结构回填，并至少包含：

```yaml
handoff:
  taskId: RP-047-A
  status: 已回填 / 待 Codex 复核
  allowNextTask: false
  projectRoot: /Users/kevin/AI/Workload-evaluation-system
  worktreePath: /Users/kevin/AI/Workload-evaluation-system/.claude/worktrees/rp-047-a-durable-run-foundation
  branch: qoder/rp-047-a-durable-run-foundation
  baseCommit: <full-assignment-commit-sha>
  finalCommit: <full-final-commit-sha>
```

正文必须列出：

- 目标与非目标；
- 逐文件变更和原因；
- migration 精确文件名及 additive 审计；
- RED 证据：命令、退出码、失败摘要；
- GREEN 证据：命令、退出码、测试数量；
- Testcontainers 干净库和已有 Harness 数据迁移证据；
- 并发 claim、lease heartbeat、event sequence、checkpoint、output、outbox 幂等证据；
- `git diff --name-only base...HEAD` 完整列表；
- 未执行验证及原因；
- 风险、残余问题、总看板同步建议；
- 明确声明未修改主 checkout、未迁移真实数据库、未接模型/Worker/API/UI；
- 下一步只能是“等待 Codex Gate A”，不能建议直接开始 Batch B。

## 12. Stop Conditions

出现任一情况立即停止，不继续试错或扩大改动：

1. 主 checkout 或新 worktree 路径/分支/baseCommit 与合同不符；
2. worktree 初始不干净，或发现 assignmentCommit 不含批准规格/计划/工单；
3. `e1c8961` 不是 baseCommit 祖先；
4. 必须修改 Allowed Paths 之外文件；
5. 需要新依赖或 `package-lock.json` 变化；
6. migration 需要删除、重命名或重写既有数据；
7. 同会话唯一约束会影响 legacy file-analysis 数据；
8. migration 演练只能依赖真实或本地长期数据库；
9. Testcontainers 不能启动或 0014 不能在历史 Harness 数据上应用；
10. 发现当前代码已由其他任务实现同名表、索引或 repository；
11. 测试只能通过删除断言、跳过关键场景、降低约束或使用不安全类型断言；
12. 需要真实密钥、外部 API、生产开关、main merge 或下一批权限。

停止时输出：阻塞条件、已执行命令、原始错误摘要、当前 diff、建议的最小决策；状态仍为
`已回填 / 待 Codex 复核` 或 `BLOCKED / WAITING_CODEX`，不得宣称完成。

## 13. Completion Boundary

Qoder 完成代码、测试、提交和 handoff 后即停止。只有 Codex 可以执行 Gate A、同步总看板、
决定是否集成，并发布 RP-047-B。用户是最终验收人。
