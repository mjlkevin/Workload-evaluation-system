# Qoder Work Order · RP-047-A2

date: 2026-08-03
taskId: RP-047-A2
issue: ISS-2026-08-02-003, ISS-2026-08-03-002
requirement: RP-047
replaces: RP-047-A rejected candidate `fae8a7d`
executor: qoder1
reviewer: codex
finalAcceptor: user-owner
approvedDesignCommit: `e1c8961`
status: `ASSIGNED / WAITING_QODER_ACK`
allowNextTask: `false`
mainMergeAllowed: `false`

## 1. Objective

在全新 worktree 中从 assignment commit 重新实施 Batch A。功能范围仍是 PostgreSQL-backed
Harness durable run 基础；必须修复 Codex Gate 记录的历史迁移、原子创建、并发幂等、
Runtime validation、安全错误、Node 24 Testcontainers 和依赖隔离问题。

批准执行计划：

`docs/superpowers/plans/2026-08-03-rp-047-batch-a2-durable-run-foundation-reexecution.md`

拒收证据：

`docs/agent-loop/audits/2026-08-03-codex-RP-047-A-gate.md`

## 2. Worktree Contract

- projectRoot: `/Users/kevin/AI/Workload-evaluation-system`
- worktreePath: `/Users/kevin/AI/Workload-evaluation-system/.claude/worktrees/rp-047-a2-durable-run-foundation`
- branch: `qoder/rp-047-a2-durable-run-foundation`
- baseCommit: Codex 派单消息中的完整 assignmentCommit
- unrelatedChangesPolicy: `preserve-and-stop-on-overlap`
- rejectedBranchPolicy: 不 cherry-pick、不复制、不以 `qoder/rp-047-a-durable-run-foundation` 为 base；该分支只读审计

## 3. Required First Reply

第一条回复只输出 Worktree Contract ACK，然后停止等待“ACK 已接受，开始实施”。ACK 必须列出
真实 baseCommit、worktree clean、approved design 祖先检查、Allowed/Forbidden Paths、验证命令
和停止条件。

## 4. Allowed Paths

```text
apps/api/test-setup.mts
apps/api/src/db/schema/harness.ts
apps/api/src/modules/harness/harness.types.ts
apps/api/src/modules/harness/harness.module.ts
apps/api/src/modules/harness/harness-runtime.types.ts
apps/api/src/modules/harness/harness-runtime.types.test.ts
apps/api/src/modules/harness/harness-runtime.repository.ts
apps/api/src/modules/harness/harness-runtime.repository.test.ts
apps/api/src/modules/harness/harness-runtime.migration.test.ts
apps/api/src/modules/harness/harness.usecase.test.ts
apps/api/src/routes/harness.routes.test.ts
apps/api/package.json
apps/api/drizzle/0014_*.sql
apps/api/drizzle/meta/0014_snapshot.json
apps/api/drizzle/meta/_journal.json
```

Ignored dependency directories created by `npm ci` are allowed runtime preparation, but no lockfile diff is allowed.

## 5. Forbidden Actions

- 不修改主 checkout、被拒 A1 worktree 或任何总看板文件；
- 不安装/卸载 Homebrew 包，不启动/停止/重配 Colima，不修改 Docker context、daemon、DNS、代理、`~/.docker`、`~/.colima` 或 shell profile；
- 不停止或删除现有 `rp047-pg17`，也不把它当作正式验收数据库；
- 不连接开发、演示、生产或其他长期数据库；
- 不修改 package-lock、UI 源码、AI Session、routes、dispatch、OpenAPI、config 或真实凭据；
- 不复用 A1 commit，不合并 main，不领取 Batch B，不宣布已交付。

## 6. Dependency And Docker Preflight

ACK 接受后先运行：

```bash
npm ci
npm ci --prefix ui/V2_PROTOTYPE
git diff --exit-code -- package-lock.json ui/V2_PROTOTYPE/package-lock.json
node -p 'require("./apps/api/node_modules/ajv/package.json").version'
test -x ui/V2_PROTOTYPE/node_modules/.bin/vite
docker info
docker context show
```

Expected: Ajv 为 8.20.x，Vite 存在，Docker daemon 可读。任何依赖安装失败、lockfile 变化或
Docker 不可读都立即停止。不得通过安装软件或修改机器配置解决。

Colima 环境只允许在单条验证命令进程内读取当前 Docker context socket，例如：

```bash
RP047_DOCKER_HOST="$(docker context inspect "$(docker context show)" --format '{{.Endpoints.docker.Host}}')"
DOCKER_HOST="$RP047_DOCKER_HOST" \
TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock \
USE_TESTCONTAINERS=true npm run test:harness -w apps/api
```

不得 export 到 profile 或修改 daemon。

## 7. Mandatory Implementation And Verification

逐项执行 A2 plan，保留每个 Task 的 RED/GREEN。最终必须通过：

```bash
USE_TESTCONTAINERS=true npm run test:harness -w apps/api
npm run test:modules
npm run build:api
npm run build:web
git diff --check
```

本机 Colima 需要 socket 时使用上一节的进程级环境变量版本，并在 handoff 同时给出命令和
环境说明。Harness script 必须包含 runtime types、migration、repository、usecase、routes，
必须 `--test-concurrency=1`，migration test 不得 skip。

额外 Gate：

- A1 Gate 中的 create eventSequence、回滚注入、20 路 event、invalid Runtime validation、
  concurrent checkpoint replay 和 safe error 用例全部绿色；
- 随机临时 database 执行 0000-0013、插入历史三类行、单独执行 0014、断言并删除数据库；
- Testcontainers 进程结束后无本任务新增残留容器；
- `git status --short --untracked-files=all` 最终 clean；
- diff 只包含 Allowed Paths，两个 lockfile 无 diff。

## 8. Stop Conditions

任一条件出现立即回填 `BLOCKED / WAITING_CODEX`：

1. worktree/base/branch/approvedDesign 不符；
2. 需要复用被拒分支或修改 Allowed Paths 之外文件；
3. Docker/Testcontainers/依赖安装不能按现有环境直接工作；
4. 需要机器级安装、代理、DNS、context、daemon 或 profile 修改；
5. migration 需要 destructive SQL、真实数据库或无法带历史数据升级；
6. 测试只能靠 skip、删除断言、泄露 payload 的原始错误或降低幂等约束通过；
7. lockfile 变化、外部密钥、模型、Worker、API、UI、main merge 或 Batch B 权限成为必要条件。

## 9. Handoff

按 `docs/codex-workflows/external-ai-handoff-template.md` 回填，并额外列出：

- A1 每个 Gate finding 对应的测试名和修复；
- worktree-local Ajv/Vite 依赖证据；
- Testcontainers 启停和随机数据库清理证据；
- 完整 RED/GREEN 与最终命令退出码；
- 未修改机器配置、未使用长期容器、未连接真实数据库；
- `allowNextTask=false`，下一步只能是等待 Codex Gate A2。
