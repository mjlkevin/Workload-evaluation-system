# Qoder Work Order — RP-047 Batch D · 前端多会话与后台任务体验

- 编制：Codex 主会话（O3-D），2026-08-07
- 实施计划：`docs/superpowers/plans/2026-08-07-rp-047-batch-d-multisession-ui.md`
- 范围基准：roadmap `docs/superpowers/plans/2026-08-03-rp-047-post-a2-execution-roadmap.md` Task 4（L174-200，唯一）
- 执行者：Qoder 协议执行会话（独立 worktree）

## 1. 合同坐标

- `projectRoot`: `/Users/kevin/AI/Workload-evaluation-system`
- `worktreePath`: `/Users/kevin/AI/Workload-evaluation-system/.claude/worktrees/rp-047-d-multisession-ui`
- `branch`: `qoder/rp-047-d-multisession-ui`
- `baseCommit`（Assignment Commit）：开工时 main HEAD，必须与
  `git log -1 --format=%H -- docs/agent-loop/work-orders/2026-08-07-qoder-RP-047-D.md` 解析一致
- 状态权威：执行会话最多到"已回填 / 待 Codex 复核"；`allowNextTask=false`、`mainMergeAllowed=false`

## 2. 目标与非目标

**目标**：在独立 worktree 实施并验证 RP-047 Batch D——SessionRuntimeStore（sessionId 键控）、
BackgroundRunProvider（Shell 层后台任务摘要与一次性完成通知）、SSE 消费与 cursor 持久化重连、
SessionRail 七态状态显示、明确停止入口（唯一 cancel 触发路径）、flag 关闭静默降级，
以及扩展项 E1（会话删除 409 守护接旧 DELETE 端点）与 E2（test:web 基线修复）。

**非目标**：不改用户发消息的旧同步主链路（streamHomeWorkbenchChat 行为零变更）；
不启用 feature flag 默认值、不拉起 Worker；不做 Batch E（灰度/回滚/故障演练）；
不实施 checkpoint 可观测性 spec（后续批次）与 O5 统一视图；不改后端 Batch B/C runtime。

## 3. 基线事实（冻结测试计数）

- main（Assignment Commit）预期基线：test:web **209**（Step 0 修复前含 **2 处已知既有失败**，
  见实施计划 §1.2）、test:harness **173**、test:modules **255**、test:ai **242**、
  test:integration **1**；build:api/build:web 退出 0。开工时复跑确认。
- 2 处既有失败归属已判定为 O4 及更早的基线漂移（非 Batch C 引入）：
  ① `aiWorkbenchReportGateConsistency.test.js` 扫描路径漂移（闸门已迁 workbench-shared.ts:162）；
  ② `HomeWorkspace.test.jsx` "检索客户主体"用例失败（归因待 Step 0）。
- 结束时：Step 0 后既有测试零失败零跳过零删改；新增 focused tests 全 GREEN；
  两套 package.json/lockfile 零新依赖。

## 4. 实施步骤（RED 先行，顺序执行）

### Step 0（E2）基线修复

- [ ] 修复 `aiWorkbenchReportGateConsistency.test.js`：扫描源 `chat.service.ts` →
  `apps/api/src/services/ai/handlers/workbench-shared.ts`；双正则断言与守护语义不变。
- [ ] 归因 HomeWorkspace 失败用例：根因在测试/mock 层 → 修测试；根因在业务代码 →
  **停止并回填疑点**（停止条件 §15），不擅自修业务。
- [ ] test:web 全绿后进入 Step 1。

### Step 1 会话隔离 RED/GREEN（G1）

- [ ] RED：focused test（MSW mock）——会话 A 事件流进行中切到 B 并发送；断言 A 的事件与
  最终消息不写入 B、不抢回当前渲染源；切换会话不清空 B 已有状态。
- [ ] 新建 `src/hooks/useSessionRuntimeStore.js`：sessionId 键控（消息视图/Run 状态/事件游标/
  unread 标记）；composer 草稿按 `userId:sessionId` 键控。
- [ ] `useChatMessages.js` / `useWorkbenchState.js` 接入 store；**旧同步发送链路行为不变**。

### Step 2 后台运行 RED/GREEN（G2）

- [ ] RED：离开 AI 页面（unmount）断言——本地 SSE 关闭、**零 cancel 调用**、provider 仍持有
  Run 摘要、完成事件触发一次性通知；Shell 显示活跃数量。
- [ ] 新建 `src/hooks/useBackgroundRuns.jsx`（BackgroundRunProvider + 消费 hook）；
  `Shell.jsx` 接线（包在 UnsavedChangesProvider 外层，仅登录态挂载）。
- [ ] 新建 `src/api/aiRuns.js`：fetch + Bearer 流式 SSE 读取（自含解析，不 import api/ai.js
  内部函数）；flag 关闭时 503/空/失败静默降级为"无后台任务"+ 退避重试。

### Step 3 恢复 RED/GREEN（G3）

- [ ] RED：模拟刷新（store 重建）→ 恢复序列（Sessions → active Runs → 按 aiSessionId 合并 →
  snapshot → 从持久 cursor 续订）；断言事件不丢不重。
- [ ] cursor 持久化 `localStorage` 键 `wes-run-cursor:<runId>`；登出清理敏感缓存
  （cursor/草稿/活跃会话键）但**不 cancel**。

### Step 4 明确停止（G4）

- [ ] RED：卸载/路由切换/刷新/登出四场景断言 cancel 调用次数为 0；"停止"按钮点击恰 1 次 cancel。
- [ ] SessionRail 行级"停止"（活动 Run 时出现）+ StatusPanel 主按钮；ConfirmDialog 二次确认；
  `api/aiRuns.js` cancel 接线。

### Step 5 SessionRail 状态

- [ ] 七态徽标：排队中/执行中/恢复中/等待确认/已完成未读/失败/已取消；文本+图标+颜色三通道，
  不得只依赖颜色。
- [ ] `useAiSessions.js` deleteSession 处理 409 `SESSION_HAS_ACTIVE_RUN` 文案
  （E1 前端侧，与后端接线同批）。

### Step 6 视觉与可访问性

- [ ] 1440px/760px 无横向溢出、键盘焦点路径、ARIA live region（完成通知 polite）；
  按 improving-wes-ui 口径单轮最多三个已证实 UI 根问题。

### Step 7 全量验证与回填

- [ ] §12 全量；UI scope checker；产出人工验收指引（flag 开启方法、预期行为、受限项声明）；
  业务提交 + 单独 handoff 提交，状态停在"已回填 / 待 Codex 复核"。

### E1 删除守护接旧端点（后端小修，可与 Step 4/5 并行）

- [ ] RED（modules focused，mock checker）：旧 DELETE `/ai-sessions/:sessionId` 命中活动 Run →
  409 `SESSION_HAS_ACTIVE_RUN` 且不删除；无 checker 场景行为不变。
- [ ] `ai-sessions.controller.ts` DELETE 与 `ai-sessions.routes.ts` L60 注入 activeRunChecker
  （复用 Batch C `harness-runtime.repository.hasActiveRunForSession`，不改 repository）。
- [ ] openapi.yaml DELETE /ai-sessions/{sessionId} 补 409 响应描述（不回填其他旧域）。

## 5. 硬口径（Gate D 审计合同，不得缩水）

> Gate D 完成定义（roadmap L200 原文）：**自动化证明会话隔离、后台继续、重连与明确停止；
> 浏览器人工验收覆盖 1440px、760px 和键盘路径；没有引入第二状态/UI 栈。**

- G1 会话隔离：A 的事件与最终消息不写入 B、不抢回当前会话；切换不清空其他会话状态。
- G2 后台继续：离页只关本地 SSE，零 cancel，摘要与一次性通知存活于 Shell 层。
- G3 重连：刷新/重登按 spec §12.4 六步序列恢复，cursor 续订不丢不重。
- G4 明确停止：唯一 cancel 触发路径是用户显式点击停止；四场景零 cancel 守护测试常驻。
- 无第二状态/UI 栈：package.json 零新依赖（含 dev）；无 Redux/Zustand/第二组件库。
- 旧同步链路行为零变更：streamHomeWorkbenchChat、useHarnessRun、isExplicitReportRequest、
  ADOPTABLE_INTENTS/0.85 全部原样；O10-A 快照集不动。
- 人工验收（1440/760/键盘）由用户在 Gate D 审计后执行；执行会话交付验收指引，
  不因无人工环境而宣称人工验收完成。

## 6. 关键参数（冻结）

- 分支/worktree：`qoder/rp-047-d-multisession-ui`（roadmap L176 原名）。
- cursor 持久化键：`wes-run-cursor:<runId>`（localStorage）。
- composer 草稿键：`wes-ai-composer-draft:<userId>:<sessionId>`（localStorage）。
- 既有活跃会话键沿用：`wes-ai-active-session-id`。
- 七态文本：排队中 / 执行中 / 恢复中 / 等待确认 / 已完成未读 / 失败 / 已取消。
- 错误码消费：`SESSION_HAS_ACTIVE_RUN`(409，删除冲突与提交冲突同码，实现已收敛)、
  `ASYNC_RUNS_DISABLED`(503)；响应 envelope `{ code, message, data }`。
- SSE 消费契约（Batch C 冻结）：`/api/v1/ai-runs/:runId/events?after=<cursor>`，
  `Last-Event-ID` 头优先；心跳 15s；终态排空后服务端主动关闭；断线不取消。
- E1 409 文案：「该会话仍有后台任务运行中，请先停止任务。」

## 7. Allowed Paths

新建（含测试）：

- `ui/V2_PROTOTYPE/src/hooks/useBackgroundRuns.jsx`
- `ui/V2_PROTOTYPE/src/hooks/useSessionRuntimeStore.js`
- `ui/V2_PROTOTYPE/src/api/aiRuns.js`
- `ui/V2_PROTOTYPE/src/__tests__/` 下 focused tests（限 background-runs / session-isolation /
  reconnect / explicit-stop / delete-guard / baseline-repair 主题）
- `docs/agent-loop/handoffs/2026-08-07-qoder-RP-047-D.md`

修改（前端）：

- `ui/V2_PROTOTYPE/src/hooks/useAiSessions.js`（409 处理与 store 衔接）
- `ui/V2_PROTOTYPE/src/pages/AiHomeWorkbench/index.jsx`（store/provider 接线）
- `ui/V2_PROTOTYPE/src/pages/AiHomeWorkbench/hooks/useChatMessages.js`（会话键控，旧发送行为不变）
- `ui/V2_PROTOTYPE/src/pages/AiHomeWorkbench/hooks/useWorkbenchState.js`（composer 键控）
- `ui/V2_PROTOTYPE/src/components/AiWorkbench/SessionRail.jsx`（七态与停止入口）
- `ui/V2_PROTOTYPE/src/components/Layout/Shell.jsx`（BackgroundRunProvider 挂载与通知）
- `ui/V2_PROTOTYPE/src/index.css`（状态徽标/通知样式）
- `ui/V2_PROTOTYPE/src/__tests__/aiWorkbenchReportGateConsistency.test.js`（E2① 扫描路径）
- `ui/V2_PROTOTYPE/src/__tests__/HomeWorkspace.test.jsx`（E2②，仅根因在测试/mock 层时）
- `ui/V2_PROTOTYPE/src/api/ai.js`（默认不改；仅当复用确有必要且行为零变更时，须 handoff 声明）

修改（E1 后端小修）：

- `apps/api/src/modules/ai-sessions/ai-sessions.controller.ts`（DELETE 接线 checker）
- `apps/api/src/routes/ai-sessions.routes.ts`（L60 旧分支注入 checker）
- `apps/api/src/modules/ai-sessions/` 下 focused test（新建或追加，mock checker）
- `docs/openapi.yaml`（仅 DELETE /ai-sessions/{sessionId} 409 描述）
- `03_技术设计/系统演进/实现与文档对齐说明.md`（E1 与 Batch D 前端接线增补）

## 8. Forbidden Actions

- 禁止引入任何新 npm 依赖（含 devDependencies）、第二状态框架（Redux/Zustand/Jotai 等）
  或第二 UI 组件库。
- 禁止修改旧同步链路行为：`streamHomeWorkbenchChat`、`api/harness.js`、`useHarnessRun.js`、
  `isExplicitReportRequest` 双正则、`ADOPTABLE_INTENTS`/0.85、O10-A 快照集。
- 禁止改动 Batch B/C 后端 runtime：`modules/harness/harness-runtime.*`、worker、recovery、
  projector、`ai-runs.routes.ts`、`routes/index.ts` 接线、drizzle、DB schema/迁移。
- 禁止修改 feature flag 默认值、Worker boot-start、SSE 服务端时序参数。
- 禁止在卸载/路由切换/刷新/关页/登出路径调用 cancel（G4 硬约束）。
- 禁止改动两套 lockfile、`apps/api/package.json`（E1 测试挂既有 test:modules 枚举或既有
  glob 覆盖，不新增 script）。
- 禁止编辑 main checkout、看板（`03_技术设计/系统架构/WES-Agent-升级总看板/`）与其他批次文档。
- 禁止把本批改动与基线修复混为同一提交（E2 归因需在 handoff 单独成节）。

## 9. 扩展项登记（超出即停止并回填疑点）

- **E1** 删除守护接旧端点（D8）：看板已承诺的 ISS-2026-08-06-003 剩余接线，限 §7 所列 5 处。
- **E2** test:web 基线修复（D9）：限 2 个测试文件；触业务代码即停。
- **E3** 人工验收指引文档：写入 handoff，不新增独立文件。
- checkpoint 可观测性 spec、O5 统一视图、Batch E 内容一律不实施；发现必要重叠时回填疑点。

## 10. RED 要求

每个 Step 的 focused tests 必须先以失败状态运行一次并记录（测试名、失败断言、运行命令），
再实现转 GREEN。G1–G4 四组守护测试为本批常驻回归资产，命名需含主题前缀
（`background-runs`/`session-isolation`/`reconnect`/`explicit-stop`）。

## 11. Handoff 格式与状态权威

按 `docs/codex-workflows/external-ai-handoff-template.md` 回填，单独提交，包含：
Worktree Contract ACK、变更表（对 §7 逐项）、RED 证据链、§12 全量输出摘要、
E2 归因专节、E1 接线证据、人工验收指引、风险与声明项、看板同步建议。
状态最多"已回填 / 待 Codex 复核"；不得宣布"已交付"、不得合并 main。

## 12. 验证命令（全量）

```bash
# 前端
npm run test:web                      # Step 0 后既有全绿 + 新增 focused 全 GREEN
npm run build:web
node skills/improving-wes-ui/scripts/check-ui-scope.mjs --base <Assignment Commit> -- <改动 UI 文件清单>

# 后端（E1 涉及）
npm run test:modules
npm run test:ai
npm run test:integration
npm run build:api

# 本机无 docker CLI：Testcontainers 用 Colima socket 等价环境
export DOCKER_HOST=unix:///Users/kevin/.colima/default/docker.sock
export TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock
USE_TESTCONTAINERS=true npm run test:harness -w apps/api   # 预期 173

# 守护 diff
git diff <Assignment Commit>..HEAD -- package.json ui/V2_PROTOTYPE/package.json package-lock.json ui/V2_PROTOTYPE/package-lock.json apps/api/package.json apps/api/drizzle
# 预期：零输出（依赖与 schema 零变更）
```

## 13. 风险与声明项

- Worker 未 boot-start + flag 默认关闭：本批不产生真实端到端执行；"后台继续"由
  mock 事件流 + 零 cancel 断言证明（实施计划 §4 诚实声明），端到端归 Batch E。
- E2 HomeWorkspace 失败若归因为业务代码回归：停止并回填，升级为独立 defect，
  不在本批擅自修复（§15 停止条件）。
- ajv 环境分叉预警（既有看板风险）：新 worktree build:api 若遇 structured-output.ts
  类型错误，先排查 ajv 版本（8.20.0 vs 6.15.0）再归因代码。
- MSW 2.x handler 写法与 1.x 差异：沿用项目内既有用法（UserManagement.test.jsx 等先例）。
- 人工验收缺口：1440/760/键盘由用户执行；执行会话不得以自动化替代或宣称通过。

## 14. 看板同步

执行会话**不直接改看板**。handoff 中给出建议页面：plan（Batch D 实施完成待 Gate D）、
testing（focused tests 基线入库 + E2 基线修复记录）、risks（人工验收缺口延续、
E1 关闭 ISS-2026-08-06-003 剩余项）、changes（事件行）。由主会话在 Gate D 审计时处置。

## 15. 停止条件

出现任一情况立即停止并回填疑点，不得自行扩围：

- 基线漂移（Assignment Commit 与 main HEAD 不一致、基线测试计数不符）；
- E2 HomeWorkspace 失败根因指向业务代码回归；
- Allowed Paths 之外才能完成需求（含新依赖、DB 迁移、flag 变更、boot-start）；
- 既有测试需要修改才能通过（非 E2 登记的两处与本批 additive 引起）；
- 旧同步链路行为被迫变更；owner 隔离或权限边界无法保证；
- 主 checkout 出现非预期改动。

## 16. 初始化提示词（派单时复制给执行会话）

```text
你是 RP-047 Batch D 的执行会话（按 Qoder worktree 协议执行）。项目入口
/Users/kevin/AI/Workload-evaluation-system。请依次执行：

1. 读取必读清单：AGENTS.md、QODER.md、skills/wes-qoder-worktree-protocol/SKILL.md、
   skills/speak-plainly/SKILL.md、skills/improving-wes-ui/SKILL.md（UI 表面硬门禁）、
   本工单全文（docs/agent-loop/work-orders/2026-08-07-qoder-RP-047-D.md）与实施计划
   （docs/superpowers/plans/2026-08-07-rp-047-batch-d-multisession-ui.md）。
2. 从 Assignment Commit（git log -1 --format=%H -- 本工单路径）创建 worktree 与分支
   qoder/rp-047-d-multisession-ui，输出完整 Worktree Contract ACK。
3. 复跑基线（§12 全量）：确认 test:web 209（含 2 处已知失败）、test:harness 173、
   test:modules 255、test:ai 242、test:integration 1 后开工。
4. 按 §4 顺序 RED 先行实施 Step 0→7 + E1；严守 §7 Allowed Paths、§8 Forbidden、
   §9 扩展项登记；Gate D 硬口径（§5）不得缩水；旧同步链路行为零变更。
5. 结束前跑 §12 全量验证，按 §11 提交业务实现 + 单独 handoff 提交，
   状态停在"已回填 / 待 Codex 复核"。

主会话补充预警：
- 本机无 docker CLI，Testcontainers 必须用 §12 的 Colima socket 环境变量组合。
- ajv 版本分叉是已知环境风险（§13）：遇 structured-output.ts 类型错误先查 ajv。
- Step 0 是开工前置：test:web 两处既有失败修复（E2）并全绿后才可进入 Step 1；
  HomeWorkspace 失败若归因业务代码，停止回填，不要擅自修业务。
- 默认按方案 A 单批执行 Step 0→7 + E1；若派单时用户指定拆批，按指定口径执行，
  但 Gate D 硬口径不缩水。
```
