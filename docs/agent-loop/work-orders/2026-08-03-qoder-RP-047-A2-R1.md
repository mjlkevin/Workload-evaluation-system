# Qoder Rework Order · RP-047-A2-R1

date: 2026-08-03
taskId: RP-047-A2-R1
parentTask: RP-047-A2
executor: qoder1
reviewer: codex
sourceGate: `docs/agent-loop/audits/2026-08-03-codex-RP-047-A2-gate.md`
status: `ASSIGNED / WAITING_QODER_R1_ACK`
allowNextTask: `false`
mainMergeAllowed: `false`

## 1. Worktree Contract

- worktreePath: `/Users/kevin/AI/Workload-evaluation-system/.claude/worktrees/rp-047-a2-durable-run-foundation`
- branch: `qoder/rp-047-a2-durable-run-foundation`
- reworkBaseCommit: `39d5ea9b537ce698e07e5195bc7af3d3a39d7306`
- 保留现有提交历史，不 rebase、不 amend、不复制 A1 代码。

第一条回复只输出 Rework Contract ACK，确认真实 HEAD、worktree clean、三条 Allowed Paths、
验证命令和停止条件，然后等待“R1 ACK 已接受，开始返工”。

## 2. Allowed Paths

```text
apps/api/src/modules/harness/harness-runtime.repository.ts
apps/api/src/modules/harness/harness-runtime.repository.test.ts
docs/agent-loop/handoffs/2026-08-03-qoder-RP-047-A2.md
```

除以上三条外不得修改；依赖目录保持 ignored，两个 lockfile 不得变化。

## 3. Required Rework

1. 先写 RED：`Date`、`Map` 和非普通对象作为 state/payload/content 时必须得到
   `HARNESS_RUNTIME_PAYLOAD_INVALID`，且不进入 DB 事务；保留循环引用、BigInt、超限与正常对象用例。
2. 修复普通对象判定：只接受原型为 `Object.prototype` 或 `null` 的对象。
3. 增加同 Run output 并发版本测试：不同 hash 并发写入后只有一行，版本集合连续，最终版本和
   `output_updated` 事件序号正确。
4. 修正 handoff：明确 15 个实现文件 + 1 个 handoff 文件；把旧 `headCommit` 字段改为
   `implementationCommit=dc93bb37ba931aa0d53b7627373357bb032c410b`，最终 R1 commit 在回复中单独报告。

## 4. Required Verification

```bash
RP047_DOCKER_HOST="$(docker context inspect "$(docker context show)" --format '{{.Endpoints.docker.Host}}')"
DOCKER_HOST="$RP047_DOCKER_HOST" \
TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock \
USE_TESTCONTAINERS=true npm run test:harness -w apps/api

npm run test:modules
npm run build:api
npm run build:web
git diff --check
git diff --exit-code -- package-lock.json ui/V2_PROTOTYPE/package-lock.json
git status --short --untracked-files=all
```

若 `test:modules` 产生已知配置副作用，只允许精确恢复
`config/system/requirement-settings.json` 到 R1 base；不得恢复其他路径。测试结束后确认无新增长期容器。

## 5. Stop Conditions

- 需要修改 Allowed Paths 之外文件；
- 需要改变 schema、migration、package script、lockfile 或机器级 Docker/Colima 配置；
- 无法先复现 RED，或只能通过降低普通对象/并发/错误安全约束使测试通过；
- 需要合并 main、进入 Batch B 或宣布已交付。

任一触发即回填 `BLOCKED / WAITING_CODEX`。完成后只报告“已回填 / 待 Codex 复核”，并提供
R1 commit、3 个变更文件、RED/GREEN、完整验证和容器清理证据。
