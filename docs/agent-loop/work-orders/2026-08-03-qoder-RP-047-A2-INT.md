# Qoder Work Order · RP-047-A2-INT

date: 2026-08-03
taskId: RP-047-A2-INT
requirement: RP-047
taskClass: integration-candidate-preparation
executor: qoder1
reviewer: codex
integrator: codex
finalAcceptor: user-owner
sourceCandidate: `qoder/rp-047-a2-durable-run-foundation @ d9790fe08e62964e0dacfe103b06647af02de81e`
sourceMergeBase: `7221503f69bee91f1bd1b87baa114577de466a51`
status: `ASSIGNED / WAITING_QODER_ACK`
allowNextTask: `false`
mustReworkFirst: `false`
mainMergeAllowed: `false`

## 1. 业务目标

在一个全新的 Qoder worktree 中，以“包含本工单的业务主线提交”为基线，按原提交顺序移植
已经通过 Codex Gate 的 A2 候选，形成可独立复核的集成分支。Qoder 负责移植、冲突处理、
集成态验证和结构化 handoff；Codex 负责独立审计、返工判定和最终主线集成。

本任务不开发 Batch B，不增加 Worker/API/UI 能力，不修改需求状态为“已交付”，不允许 Qoder
直接合并 `codex/role-driven-ai-home-workbench`。

## 2. 事实来源

执行前必须读取：

1. `AGENTS.md`
2. `codex-project-registry.md`
3. `QODER.md`
4. `skills/speak-plainly/SKILL.md`
5. `skills/wes-qoder-worktree-protocol/SKILL.md`
6. `skills/wes-qoder-worktree-protocol/references/protocol.md`
7. `skills/wes-multi-agent-collaboration/SKILL.md`
8. `docs/superpowers/specs/2026-08-02-rp-047-durable-multichannel-checkpoint-runtime-design.md`
9. `docs/superpowers/plans/2026-08-03-rp-047-post-a2-execution-roadmap.md`
10. `docs/agent-loop/audits/2026-08-03-codex-RP-047-A2-R1-gate.md`
11. 本工单

项目规则优先于历史 Qoder protocol reference 中残留的 `-agent` 路径。唯一项目入口是：

```text
/Users/kevin/AI/Workload-evaluation-system
```

## 3. Assignment Commit 解析

本工单不能把“包含自身的 commit”硬编码在文件正文中。Qoder 必须在主 checkout 只读执行：

```bash
cd /Users/kevin/AI/Workload-evaluation-system
ASSIGNMENT_COMMIT="$(git log -1 --format=%H -- docs/agent-loop/work-orders/2026-08-03-qoder-RP-047-A2-INT.md)"
test -n "$ASSIGNMENT_COMMIT"
test "$ASSIGNMENT_COMMIT" = "$(git rev-parse codex/role-driven-ai-home-workbench)"
printf '%s\n' "$ASSIGNMENT_COMMIT"
```

Expected: 输出一个 40 位 commit，并且它等于 Qoder 开始时的业务主线 HEAD。若主线已经继续前进、
本工单不在 HEAD、或者两者不相等，立即停止并回填 `BLOCKED / BASELINE_DRIFT`，不得自行选择新基线。

## 4. Worktree Contract

- projectRoot: `/Users/kevin/AI/Workload-evaluation-system`
- worktreePath: `/Users/kevin/AI/Workload-evaluation-system/.claude/worktrees/rp-047-a2-integration`
- branch: `qoder/rp-047-a2-integration`
- baseCommit: 第 3 节解析出的 `ASSIGNMENT_COMMIT`
- sourceBranch: `qoder/rp-047-a2-durable-run-foundation`
- sourceCommit: `d9790fe08e62964e0dacfe103b06647af02de81e`
- rejectedBranch: `qoder/rp-047-a-durable-run-foundation @ fae8a7d`，只读审计，禁止使用
- unrelatedChangesPolicy: `preserve-and-stop-on-overlap`
- deliveryStatusAuthority: Qoder 只能回填“已回填 / 待 Codex 复核”

## 5. Required First Output

任何编辑或 worktree 创建前，先输出：

```markdown
## Worktree Contract ACK
projectRoot: /Users/kevin/AI/Workload-evaluation-system
worktreePath: /Users/kevin/AI/Workload-evaluation-system/.claude/worktrees/rp-047-a2-integration
branch: qoder/rp-047-a2-integration
baseCommit: <第 3 节命令实际输出的 40 位 SHA>
taskId: RP-047-A2-INT
sourceOrder: docs/agent-loop/work-orders/2026-08-03-qoder-RP-047-A2-INT.md
sourceCandidate: d9790fe08e62964e0dacfe103b06647af02de81e
allowedPaths: 本工单第 7 节 17 个路径
forbiddenActions: 本工单第 8 节全部条目
validationCommands: 本工单第 11 节全部命令
stopConditions: 本工单第 12 节全部条目
statusAuthority: 已回填 / 待 Codex 复核
```

如果所有字段精确匹配，可在同一执行轮继续施工；任一字段不匹配必须停止，不得猜测修正。

## 6. 创建隔离 Worktree

先确认目标 branch 和 worktree 均不存在：

```bash
cd /Users/kevin/AI/Workload-evaluation-system
test ! -e .claude/worktrees/rp-047-a2-integration
test -z "$(git branch --list qoder/rp-047-a2-integration)"
git worktree add -b qoder/rp-047-a2-integration \
  /Users/kevin/AI/Workload-evaluation-system/.claude/worktrees/rp-047-a2-integration \
  "$ASSIGNMENT_COMMIT"
```

进入新 worktree 后确认：

```bash
cd /Users/kevin/AI/Workload-evaluation-system/.claude/worktrees/rp-047-a2-integration
git status --short --branch
test "$(git rev-parse HEAD)" = "$ASSIGNMENT_COMMIT"
test "$(git branch --show-current)" = "qoder/rp-047-a2-integration"
```

Expected: worktree clean，HEAD 和 branch 精确匹配。

## 7. Allowed Paths

集成候选最多包含以下 17 个路径：

```text
apps/api/drizzle/0014_talented_deathstrike.sql
apps/api/drizzle/meta/0014_snapshot.json
apps/api/drizzle/meta/_journal.json
apps/api/package.json
apps/api/src/db/schema/harness.ts
apps/api/src/modules/harness/harness-runtime.migration.test.ts
apps/api/src/modules/harness/harness-runtime.repository.test.ts
apps/api/src/modules/harness/harness-runtime.repository.ts
apps/api/src/modules/harness/harness-runtime.types.test.ts
apps/api/src/modules/harness/harness-runtime.types.ts
apps/api/src/modules/harness/harness.module.ts
apps/api/src/modules/harness/harness.types.ts
apps/api/src/modules/harness/harness.usecase.test.ts
apps/api/src/routes/harness.routes.test.ts
apps/api/test-setup.mts
docs/agent-loop/handoffs/2026-08-03-qoder-RP-047-A2.md
docs/agent-loop/handoffs/2026-08-03-qoder-RP-047-A2-INT.md
```

前 16 个路径必须来自 A2 原始提交序列；最后一个文件只用于本次集成 handoff。

## 8. Forbidden Actions

- 不编辑主 checkout，不 stage 或修改主 checkout 的 `config/auth/users.json`、`config/versions/records.json`；
- 不使用、复制或 cherry-pick A1 `fae8a7d`；
- 不执行整分支 merge、rebase、squash、amend、force push；
- 不修改 Allowed Paths 外的代码、配置、文档、看板、lockfile；
- 不删除测试、不降低断言、不使用 skip 掩盖 migration/Testcontainers 失败；
- 不安装或重配 Homebrew、Colima、Docker context、daemon、代理、DNS 或 shell profile；
- 不停止或删除既有 `rp047-pg17`，不连接开发、演示、生产或其他长期数据库；
- 不接触真实 API Key、token、cookie、私钥或用户业务数据；
- 不开发 Worker、Recovery Coordinator、异步 Run API、SSE 或前端多会话；
- 不合并业务主线、不清理 A1/A2 worktree、不领取 Batch B、不宣布 RP-047 已交付。

## 9. 固定移植顺序

先证明来源提交链未漂移：

```bash
test "$(git rev-parse qoder/rp-047-a2-durable-run-foundation)" = \
  "d9790fe08e62964e0dacfe103b06647af02de81e"
git log --reverse --format='%H %s' \
  7221503f69bee91f1bd1b87baa114577de466a51..d9790fe08e62964e0dacfe103b06647af02de81e
git diff --name-status \
  7221503f69bee91f1bd1b87baa114577de466a51..d9790fe08e62964e0dacfe103b06647af02de81e
```

Expected: 精确得到 8 个提交和原 A2 的 16 个文件。然后按顺序执行：

```bash
git cherry-pick \
  c94f9da2f62b8c34d249e6db2a94fc46a8d58bdd \
  048322489e3b1c0d4bea5ced4c1385a399fc404f \
  a16527e8ec0463925995ad2fee6c65fa37a74b57 \
  312d33e7cf9c741d2748ff9490e74f91aad6c8ce \
  748af1ae09a0f4e8b26bbffc8dacbc5e43200af4 \
  dc93bb37ba931aa0d53b7627373357bb032c410b \
  39d5ea9b537ce698e07e5195bc7af3d3a39d7306 \
  d9790fe08e62964e0dacfe103b06647af02de81e
```

冲突处理规则：

1. 只能解决第 7 节前 16 个路径内的冲突；
2. 保留当前主线新增的 RP-031、UI、协作和看板事实；
3. 保留 A2 已 Gate 通过的 additive migration、repository、测试和安全边界；
4. 任何需要修改第 17 个 handoff 之外的新路径，立即 `git cherry-pick --abort` 并停止；
5. 不通过删除主线测试、退回旧 fixture 或丢弃 A2 测试解决冲突。

## 10. 依赖与 Docker 预检

在集成 worktree 运行：

```bash
npm ci
npm ci --prefix ui/V2_PROTOTYPE
git diff --exit-code -- package-lock.json ui/V2_PROTOTYPE/package-lock.json
node -p 'require("./apps/api/node_modules/ajv/package.json").version'
test -x ui/V2_PROTOTYPE/node_modules/.bin/vite
docker info
docker context show
```

Expected: 两个 lockfile 无变化；Ajv 和 Vite 可用；Docker daemon 可读。失败时停止，不修改机器环境。

## 11. Mandatory Verification

### 11.1 Harness 真实 PostgreSQL 门禁

```bash
RP047_DOCKER_HOST="$(docker context inspect "$(docker context show)" --format '{{.Endpoints.docker.Host}}')"
DOCKER_HOST="$RP047_DOCKER_HOST" \
TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock \
USE_TESTCONTAINERS=true npm run test:harness -w apps/api
```

Expected: Harness 全部通过、0 fail、0 skip；migration 使用随机临时 database；容器结束后停止。

### 11.2 主线集成回归

```bash
npm run test:modules
npm run test:ai
npm run test:integration
npm run test:web
npm run build:api
npm run build:web
npm run test:security
```

Expected: 所有命令退出 0。测试数量记录实际结果，不沿用 A2 旧基线的 143 项作为硬编码数量。

### 11.3 范围与清洁度

```bash
git diff --check "$ASSIGNMENT_COMMIT"..HEAD
git diff --exit-code "$ASSIGNMENT_COMMIT"..HEAD -- \
  package-lock.json ui/V2_PROTOTYPE/package-lock.json
git diff --name-only "$ASSIGNMENT_COMMIT"..HEAD | sort
git status --short --untracked-files=all
docker ps --format '{{.Names}}'
```

Expected:

- 业务代码差异只在第 7 节前 16 个路径；
- 新集成 handoff 是唯一额外文件；
- 两个 lockfile 无变化；
- 提交 handoff 后 worktree clean；
- 没有本任务新增长期容器，既有 `rp047-pg17` 未被停止或删除。

如 `npm run test:modules` 只产生已知 `config/system/requirement-settings.json` 四个模型字段副作用，
先核对 diff 精确等于已知字段，再只恢复该单一文件。出现任何其他副作用必须停止并报告。

## 12. Stop Conditions

任一条件出现立即停止并回填 `BLOCKED / WAITING_CODEX`：

1. Assignment Commit 与当前业务主线不一致；
2. 目标 branch/worktree 已存在或不是 clean；
3. A2 source branch 不再指向 `d9790fe`，或 8 个提交/16 个文件不一致；
4. 冲突扩展到 Allowed Paths 之外；
5. 需要整分支 merge、rebase、squash、amend 或修改历史；
6. lockfile 变化、Docker 不可读、Testcontainers 需要机器级配置；
7. migration 出现 destructive SQL、skip、长期数据库连接或历史数据不兼容；
8. 任一验证失败且需要降低测试、删除断言或扩大功能范围才能通过；
9. 需要真实凭据、用户数据、Worker/API/UI/Batch B 权限；
10. 需要编辑主 checkout、合并主线、清理 worktree/分支或修改总看板。

## 13. Handoff 文件

创建：

```text
docs/agent-loop/handoffs/2026-08-03-qoder-RP-047-A2-INT.md
```

必须包含：

- `projectRoot`、`worktreePath`、`branch`、`baseCommit`、`sourceCommit`、最终 HEAD；
- 8 个 cherry-pick 的成功/冲突情况和每个冲突的处理依据；
- 最终 changed files 与第 7 节 Allowed Paths 对比；
- Harness、modules、AI、integration、Web、API/Web build、安全扫描的命令与结果；
- migration 0 skip、随机数据库清理、Testcontainers 容器停止证据；
- lockfile 无变化、无运行态 config、无机器级配置、无长期数据库、无真实密钥；
- 风险和未验证项；
- `allowNextTask=false`、`mainMergeAllowed=false`；
- 状态只能是“已回填 / 待 Codex 复核”，下一 owner 为 `codex`。

handoff 单独提交：

```bash
git add docs/agent-loop/handoffs/2026-08-03-qoder-RP-047-A2-INT.md
git commit -m "docs(WES RP-047): 回填 A2 集成候选待 Codex 审计"
```

## 14. Codex 后续审计合同

收到 handoff 后 Codex 将独立执行：

1. 检查 worktree/branch/base/source/final HEAD；
2. 复核 diff 是否严格限制在 17 个 Allowed Paths；
3. 复跑 Testcontainers Harness、modules、AI、integration、Web、API/Web build、安全扫描；
4. 检查 migration additive、owner 边界、JSON 普通对象、并发事件/checkpoint/output、错误脱敏；
5. 检查两个 lockfile、运行态 config、容器和机器配置边界；
6. 发布 `ACCEPTED_FOR_MAINLINE_INTEGRATION`、`REWORK_REQUIRED`、`REJECTED` 或 `USER_DECISION_REQUIRED`；
7. 只有审计通过后，Codex 才能把最小 patch 集成业务主线并发布 Batch B 工单。

## 15. Qoder 可直接执行的初始化提示词

```text
执行 WES 工单 RP-047-A2-INT。

唯一项目入口：/Users/kevin/AI/Workload-evaluation-system
工单：docs/agent-loop/work-orders/2026-08-03-qoder-RP-047-A2-INT.md

先完整读取工单第 2 节列出的全部文件，按第 3 节命令解析 Assignment Commit。任何编辑或
worktree 创建前，先输出第 5 节要求的 Worktree Contract ACK。字段全部匹配时可在同一轮
继续执行；不匹配立即停止。

你只负责在 qoder/rp-047-a2-integration 独立 worktree 中形成并验证 A2 集成候选，不得编辑
主 checkout，不得合并业务主线，不得领取 Batch B，不得修改看板，不得宣布已交付。

严格按第 9 节顺序 cherry-pick 8 个已审核提交；冲突只能在第 7 节 Allowed Paths 内解决。
完成第 10-11 节全部验证后，按第 13 节生成 handoff 并提交，最终状态只能是：
已回填 / 待 Codex 复核。
```
