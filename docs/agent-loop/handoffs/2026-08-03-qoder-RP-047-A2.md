# Qoder Handoff · RP-047-A2 Durable Run Foundation

```text
taskId: RP-047-A2
status: 已回填 / 待 Codex 复核
allowNextTask: false
assignmentCommit: 7221503f69bee91f1bd1b87baa114577de466a51
approvedDesignCommit: e1c8961（祖先关系已在 ACK 阶段核验）
```

## 目标

在全新 worktree 中从 assignment commit 重新实施 RP-047 Batch A（PostgreSQL-backed Harness
durable run 基础），逐项修复 Codex Gate A 记录的历史迁移、原子创建、并发幂等、Runtime
validation、安全错误、Node 24 Testcontainers 与依赖隔离问题。不复用被拒 A1 分支任何实现。

## Worktree 身份

- worktreePath: `/Users/kevin/AI/Workload-evaluation-system/.claude/worktrees/rp-047-a2-durable-run-foundation`
- branch: `qoder/rp-047-a2-durable-run-foundation`
- baseCommit: `7221503f69bee91f1bd1b87baa114577de466a51`
- headCommit: `dc93bb37ba931aa0d53b7627373357bb032c410b`
- commits（base..HEAD，按序）:
  - `c94f9da` feat(WES Agent): RP-047-A2 · 冻结持久运行状态契约
  - `0483224` feat(WES Agent): RP-047-A2 · 建立持久运行数据库结构
  - `a16527e` feat(WES Agent): RP-047-A2 · 实现原子队列创建与租约认领
  - `312d33e` feat(WES Agent): RP-047-A2 · 固化检查点与事件幂等原语
  - `748af1a` feat(WES Agent): RP-047-A2 · 修复测试容器启动并证明历史迁移
  - `dc93bb3` fix(WES Agent): RP-047-A2 · 迁移演练兼容构建类型检查
- `git status --short --untracked-files=all`: clean（本 handoff 文件提交后仍 clean）

## A1 Gate finding → 测试与修复映射

| A1 Gate finding | A2 修复 | 对应测试 |
|---|---|---|
| 迁移无历史行升级证明 | 随机临时 database 重放 0000..0013 + legacy 三类行 + 单独 0014 + 断言 + DROP | `historical migrations replay then 0014 backfills legacy rows additively`（harness-runtime.migration.test.ts） |
| 正式 Testcontainers 命令 exit 7 | 进程级 `DOCKER_HOST`/`TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE` 环境变量，未改机器配置 | `USE_TESTCONTAINERS=true npm run test:harness -w apps/api` 93/93 pass |
| Node 24 ERR_AMBIGUOUS_MODULE_SYNTAX | test-setup.mts 改 `fileURLToPath(new URL("./drizzle/", import.meta.url))` | 全部 Testcontainers 运行 setup 正常加载并完成迁移 |
| createQueuedRun 非原子、返回 eventSequence=0 | Run + run_queued 事件 + 最终重读同一事务；序号由 Run 行原子递增 | `createQueuedRun returns the persisted eventSequence`、`createQueuedRun rolls back the run when run_queued event insert fails`（CHECK 约束注入） |
| event sequence 只测串行 | 20 路并发断言精确连续、无重复无空洞、Run 计数器一致 | `appendRunEvent assigns exact consecutive sequences under 20-way concurrency` |
| checkpoint validation 宽松 | 5 个 checks 精确 `=== true`、validatedAt 可解析、validatorVersion 非空 | `commitCheckpoint enforces strict runtime validation`（9 种坏对象全拒且无持久行） |
| checkpoint 并发重放不幂等 | Run 行 `FOR UPDATE` 串行化 + 同 key/hash 幂等返回 created=false、异 hash 抛 `CHECKPOINT_KEY_CONFLICT` | `commitCheckpoint is idempotent under concurrent replay`、`concurrent checkpoint commits with different keys get consecutive sequences` |
| 错误泄漏 SQL params/state | 统一 `HarnessRuntimeError` 固定 code，pg 错误映射 `HARNESS_RUNTIME_INTERNAL`，不回显载荷 | `safe errors never leak SQL params or state` |
| script 遗漏 runtime types/迁移用例 | test:harness 重写为 5 文件 + `--test-global-setup` + `--test-concurrency=1` | 93 用例 0 skip |
| 依赖回退主 checkout | worktree 内 root 与 V2 各自 `npm ci`；解析证据证明不回退 | 见“依赖隔离证据” |
| 会话活动 Run 唯一约束缺口 | 部分唯一索引 + `ACTIVE_WORKBENCH_RUN_EXISTS` 安全错误 | `one workbench session cannot hold two active runs` |

## 变更文件

- `apps/api/src/modules/harness/harness-runtime.types.ts`(.test.ts): 新增，状态/事件/检查点契约与纯类型测试
- `apps/api/src/modules/harness/harness-runtime.repository.ts`(.test.ts): 新增，原子创建/认领/lease/事件/检查点/输出/outbox 原语 + 20 个 DB 测试
- `apps/api/src/modules/harness/harness-runtime.migration.test.ts`: 新增，随机临时 database 历史迁移演练
- `apps/api/src/db/schema/harness.ts`: 八态 enum、13 新列、5 新表、队列/owner/会话索引
- `apps/api/src/modules/harness/harness.types.ts`: HARNESS_RUN_STATUSES 扩为八态
- `apps/api/src/modules/harness/harness.module.ts`: barrel 导出 runtime types 与 repository
- `apps/api/src/modules/harness/harness.usecase.test.ts`、`apps/api/src/routes/harness.routes.test.ts`: fixture 补齐 13 个新字段默认值
- `apps/api/drizzle/0014_talented_deathstrike.sql` + `meta/0014_snapshot.json` + `meta/_journal.json`: additive 迁移（无 DROP/TRUNCATE/DELETE）
- `apps/api/test-setup.mts`: Node 24 ESM 路径修复
- `apps/api/package.json`: 仅 test:harness script 一行精确重写

## 验证命令与结果

所有 Testcontainers 命令仅用进程级环境变量（`DOCKER_HOST` 为当前 colima context socket，
`TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock`），未 export profile、未改 daemon：

- `USE_TESTCONTAINERS=true npm run test:harness -w apps/api`: **93 pass / 0 fail / 0 skip**，容器正常停止
- `npm run test:modules`: **143 pass / 0 fail**
- `npm run build:api`: **pass**（tsc 退出 0）
- `npm run build:web`: **pass**（仅既有 chunk-size 警告）
- `git diff --check`: clean
- `git status --short --untracked-files=all`: clean
- `git diff --stat 7221503..HEAD`: 15 个文件全部命中 Allowed Paths；`package-lock.json` 与 `ui/V2_PROTOTYPE/package-lock.json` 无 diff
- 预检（ACK 后）: root/V2 `npm ci` 成功；Ajv 8.20.0；Vite 可执行；`docker info`/context 可读

## 依赖隔离证据

- `require.resolve('drizzle-orm')` 从 `apps/api` 解析到 worktree 根 `node_modules/drizzle-orm`（0.45.2），不回退主 checkout
- `require.resolve('vite/package.json')` 解析到 worktree `ui/V2_PROTOTYPE/node_modules/vite`
- 预检 `require("./apps/api/node_modules/ajv/package.json").version` = 8.20.0（worktree-local）

## Testcontainers 与随机数据库清理证据

- 每次运行日志均出现 `[test:setup] PostgreSQL container started` → `Migrations complete` → `Container stopped`
- migration test 在 admin 连接上 `CREATE DATABASE rp047_a2_<随机12位>`，after() 先 `pool.end()` 再 `pg_terminate_backend` 后 `DROP DATABASE`
- 测试进程结束后 `docker ps` 仅剩既有只读容器 `rp047-pg17`（未触碰）与会话级 ryuk（testcontainers 自动回收）；无本任务新增长期容器

## 边界声明

- 未修改机器配置：未安装/卸载任何软件，未动 Colima/Docker daemon、context、DNS、代理、shell profile
- 未使用长期容器作验收库、未连接任何开发/演示/生产数据库（`rp047-pg17` 保持原状只读存在）
- `config/system/requirement-settings.json` 测试 side effect 已核对仅为已知 4 个 model 字段并精确恢复（git status clean 证明）
- 未合并 main、未领取 Batch B、未宣布已交付

## 风险

- Testcontainers 依赖进程级 `DOCKER_HOST` 环境变量；复核方若不带该变量直跑将 exit 7（与 A1 Gate 复现一致），复核命令见上
- build:web 的 chunk-size 警告为既有问题，非本批引入
- 本批只含 repository/schema/迁移层；Worker、API 路由、AI Session、UI 属后续批次范围

## 是否需看板同步

否。本 handoff 不修改任何总看板文件；待 Codex Gate A2 复核通过后由 Codex 决定是否同步
testing / changes / plan 页面。

## 下一步建议

- 等待 Codex Gate A2 独立复核（复核命令与环境说明已在上文给出）
- `allowNextTask=false`；Gate 通过前不得发布 Batch B，Qoder 停止等待
