# Handoff · ISS-2026-08-10-002（右下角全局「后台任务」角标不计数）

> 状态：**已回填 / 待主会话复审**（Qoder 侧状态上限，交付与否由 Codex/用户决定）
> 执行：KIMIK3 · 2026-08-10 · 工单 `docs/agent-loop/work-orders/2026-08-10-qoder-ISS-2026-08-10-002-shell-badge-refresh.md`

## 目标

修复右下角全局悬浮角标（Shell 层 `BackgroundRunProvider`）恒显「后台任务 0」：provider 缺「新 run 创建」刷新触发，提交后活跃列表停留挂载时空数组，导致角标恒 0、新 run 不建 SSE、终态通知与 SessionRail 行级徽标全链缺失。按工单方案 A 实施：context 暴露节流刷新入口 `notifyRunsChanged`，由工作台提交成功回调 / 统一视图发现新 runId 时调用；零 cancel 硬口径与 flag 关闭静默降级不变。

## Worktree

- projectRoot: /Users/kevin/AI/Workload-evaluation-system
- worktreePath: /Users/kevin/AI/wes-worktrees/iss-2026-08-10-002
- branch: qoder/iss-2026-08-10-002-shell-badge-refresh
- baseCommit: 64b8398（工单实填；main 当前 HEAD 42e45af 仅为其上的派发 docs commit，代码面无差异）
- taskId: ISS-2026-08-10-002（右下角全局「后台任务」角标不计数）
- 主检出 14 个用户未提交文件 + 1 个未跟踪文件全程未触碰；本任务全部 diff 落在 worktree 分支。

## 变更文件

- `ui/V2_PROTOTYPE/src/hooks/useBackgroundRuns.jsx`（修改）：context 新增 `notifyRunsChanged`——1.5s 节流（leading 立即刷新 + 窗口期内至多一次 trailing，调用不丢）+ mounted 守卫，复用既有 `refresh`（含失败指数退避）；加入 context value 与无 provider 兜底对象；只触发列表刷新，不发起 cancel。
- `ui/V2_PROTOTYPE/src/pages/AiHomeWorkbench/index.jsx`（修改）：① 包装 `chat.sendMessage`——提交成功（await 返回）后调用一次 `notifyRunsChanged`；② 新增统一视图新 runId 发现 effect（`runId || id` 键控去重），覆盖挂载首拉 / 页签返回重拉 / 对账重拉三条统一视图刷新路径。
- `ui/V2_PROTOTYPE/src/__tests__/background-runs.test.jsx`（修改，仅追加用例 + import）：新增 3 例（见 RED 节），未新建测试文件、未改既有用例断言。
- `docs/agent-loop/handoffs/2026-08-10-qoder-ISS-2026-08-10-002.md`（新建）：本回填。
- `ui/V2_PROTOTYPE/src/__tests__/unified-view.test.jsx`：未改动（3 例新用例全部落入 background-runs.test.jsx 即可覆盖，无需追加）。

## RED 证据（先红后绿，实跑输出）

修复前（base 代码 + 新用例），`npx vitest run src/__tests__/background-runs.test.jsx`：

```
Test Files  1 failed (1)
     Tests  3 failed | 7 passed (10)
```

- 用例 1「新 run 创建经 context 刷新入口计数变 1 并建立 SSE 消费终态」：`Expected: "function" / Received: "undefined"`（`typeof contextApi.notifyRunsChanged`，context 无入口）。
- 用例 2「刷新入口节流——窗口期多次调用仅 leading 一次列表请求」：同上，`notifyRunsChanged` 为 undefined。
- 用例 3「工作台提交成功后右下角角标 2s 内计数为 1（用户症状复现）」：`findByText('后台任务 1', timeout: 2000)` 超时——Shell 全挂载下发问、submitRun 成功，角标停留「后台任务 0」，用户症状原样复现。

实现后同命令：`Test Files 1 passed (1) / Tests 10 passed (10)`（含既有 G2 零 cancel、终态通知、N2 去重等 7 例全绿回归）。

## 验证命令与结果

- `npm install`（worktree 根）: pass，exit 0
- `npm install`（worktree `ui/V2_PROTOTYPE`）: pass，exit 0
- `npx vitest run src/__tests__/background-runs.test.jsx`（GREEN 复跑）: pass，10/10
- `npm run test:web`: pass，**42 文件 283/283**（基线 280 + 新增 3）
- `npm run test:modules`: pass，**321 pass / 0 fail**（后端零改动复验）
- `npm run build:api`: pass，exit 0
- `npm run build:web`: pass，exit 0（`✓ built in 674ms`；chunk >500 kB 为既有体积提示，非错误）
- `git diff 64b8398 -- apps/ package-lock.json`: pass，**零输出**（`wc -l` = 0）
- `node skills/improving-wes-ui/scripts/check-ui-scope.mjs --base 64b8398 -- ui/V2_PROTOTYPE/src/hooks/useBackgroundRuns.jsx ui/V2_PROTOTYPE/src/pages/AiHomeWorkbench/index.jsx`: pass，`No new deterministic UI findings.`
- `npx ui-skills start`（improving-wes-ui 前置）: 已在 worktree 根执行
- diff 范围核对（`git status --porcelain` 于提交前）：仅 3 个 Allowed Paths 源文件；handoff 为本文件

## git log 实输出（分支）

```
ce098cb fix(ISS-2026-08-10-002): 右下角全局角标补新 run 刷新触发——BackgroundRunProvider context 暴露节流入口 notifyRunsChanged（1.5s leading+trailing、mounted 守卫、零 cancel 不变），工作台提交成功回调与统一视图发现新 runId 时各调用一次；此前提交后 provider 活跃列表停留挂载时空数组，角标恒 0、SSE 不建、终态通知与 SessionRail 徽标全链缺失
64b8398 docs(工单): ISS-2026-08-10-002（右下角全局角标不计数）工单编制——根因锁定 Shell provider 缺新 run 刷新触发，待用户拍板派发
```
（handoff 提交后 git log 以实际为准）

## 风险

- **人工验收缺口**：工单 §9 验收口径（发问后 2s 内角标 ≥1、完成后收敛并弹「已完成」通知、切走返回数字一致、SessionRail 行级徽标同步）需并入 MT-ISS001-001 人工复测；自动化已覆盖同链路（用例 1/3），但真机 SSE 时序未实测。
- **测试缺口**：用例 2 只断言 leading 节流不放大请求（50ms 窗口），trailing 分支（窗口期调用保底刷新）未做 1.5s 实等断言，避免慢测试；trailing 逻辑简单但无专门用例。
- **兼容**：context value 新增字段为纯增量，无 provider 兜底对象已同步补 `notifyRunsChanged: () => {}`，旧调用方零影响；`sendMessage` 包装仅透传参数与返回值，同步路径（flag 关闭回退）行为逐字不变——该路径下也会多发一次 `/ai-runs` 刷新请求（幂等空刷，静默降级语义内）。
- **范围外观察 1（顶栏即时性）**：`useChatMessages.sendMessage` 在 submitRun 成功后并不主动 `refreshUnifiedView`（统一视图仅挂载与 visibilitychange 重拉）；验收时顶栏「进行中 1」的即时出现可能依赖重挂载/页签返回。本修复保证右下角 2s 内计数，顶栏提交后即时性若需对齐，建议另立 Issue 评估（文件在 Allowed Paths 外，未动）。
- **范围外观察 2（O8 页面级流式）**：`useChatMessages` 的 `activeRunId` 取自统一视图 runs，同样依赖统一视图刷新才发现新 run——逐字流式呈现可能延迟到下次统一视图重拉，与本缺陷同源不同面；未动，供主会话判断是否入池。
- **范围外观察 3（测试基建）**：共享 MSW handlers 无 `GET /ai-runs/:runId` 快照默认 handler（既有现象，产生 unhandled 警告）；本次仅在新用例内局部补 handler，未动 `mocks/handlers.js`（Allowed Paths 外）。

## 是否建议看板同步

是。建议页面：
- `issues.html`：ISS-2026-08-10-002（右下角全局角标不计数）→ 「已回填 / 待主会话复审」，关联 MT-ISS001-001 复测。
- `testing.html`：新基线 test:web 283 / test:modules 321；MT-ISS001-001 补「右下角角标」复测项。
- `changes.html`：本修复合入行（待复审通过后）。
看板收官与状态流转由 Codex/用户执行，本次未触碰任何看板文件。

## 下一步建议

- 待主会话复审：复核 diff 范围（3 源文件 + 本 handoff）、RED 证据与验证矩阵；复审通过并经用户批准后合入 main。
- 复审建议复跑：`npm run test:web && npm run test:modules && npm run build:web`（worktree 依赖已装，可直接复跑）。
- 人工验收：按工单 §9 并入 MT-ISS001-001 复测（发问 2s 内角标 ≥1 / 完成收敛 + 通知 / 切走返回一致 / SessionRail 徽标）。
- 范围外观察 1/2 请主会话裁定是否入需求池。
