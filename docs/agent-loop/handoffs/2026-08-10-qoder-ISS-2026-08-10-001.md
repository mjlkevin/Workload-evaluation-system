# Handoff · ISS-2026-08-10-001（ISS-003 复验残留：未完成时返回占位不恢复 + 后台任务角标不显示）修复回填

- **状态**：已回填 / 待主会话复审
- **来源工单**：`docs/agent-loop/work-orders/2026-08-10-qoder-ISS-2026-08-10-001-badge-and-unmount-snapshot.md`
- **关联**：ISS-2026-08-09-003（离页返回旧缓存渲染、AI 回复不显示）复验残留两处体验缺口

## 目标

修复用户 2026-08-10 复验 ISS-003 实测反馈的两处展示层体验缺口：

1. **根因 A（后台任务角标数据源只返回活跃 run）**：统一视图 runs 增补「近期已完成 run」，角标「已完成」计数不再永远为 0；配套 completedInBackground 对账在重挂载后不再依赖卸载前 ref。
2. **根因 B（页面级离开不存快照，未完成占位无法恢复）**：离开工作台页面（组件卸载）时把当前会话视图（含进行中 loading 占位）写入快照；重挂载首帧经既有 `reconcileWithBackendMessages` 对账路径恢复渲染——后端为准、仅保留未完成进行中占位（与 ISS-003 C2 同一合并语义）。

边界遵守：未改 SSE 协议、未改 run 状态机、未新增依赖、未动 package.json、未新建测试文件、未引入新组件库。

## Worktree

- projectRoot: /Users/kevin/AI/Workload-evaluation-system
- worktreePath: /Users/kevin/AI/Workload-evaluation-system/.claude/worktrees/iss-2026-08-10-001-badge-and-unmount-snapshot
- branch: qoder/iss-2026-08-10-001-badge-and-unmount-snapshot
- baseCommit: e589fc2（main 当前 HEAD，≥ 工单要求的 7943b23）
- taskId: ISS-2026-08-10-001（ISS-003 复验残留两处体验缺口）
- commit: ff235bb

`git log --oneline -4` 实际输出：

```text
ff235bb fix(ISS-2026-08-10-001): 后台任务角标数据源补近期已完成 run + 离页卸载快照恢复进行中占位——Run 进 completed 终态即从统一视图消失致角标永远计 0、离页不写快照致未完成占位不可恢复；重挂载对账不再依赖卸载前 ref
e589fc2 docs(工单): ISS-2026-08-10-001（离页占位不恢复 + 角标不显示）修复工单编制派发——根因取证修正（角标词无错，真根因为统一视图只返活跃 run）
3aaabdf docs(总看板): ISS-2026-08-10-001 入池 · ISS-003 复验残留两处体验缺口（离页占位不恢复 + 角标状态词错配）根因取证
9d373fe docs(总看板): ISS-2026-08-09-003（离页返回旧缓存渲染）合入收官（0c08c3c）——读取侧对账关闭，待离页返回复验
```

## 变更文件

- `apps/api/src/modules/harness/harness-runtime.repository.ts`：接口与实现新增 `listRecentlyCompletedRunsForOwner(ownerUserId, limit?)`——status='completed'、按 updatedAt 倒序、limit clamp 到 ≤10，与活跃查询同为 owner 隔离，仅读取不改状态机。
- `apps/api/src/modules/harness/workbench-view.usecase.ts`：`WorkbenchViewRepoPort` 增补该端口；`getUnifiedView` 将近期已完成 Run（上限 10，常量 `RECENTLY_COMPLETED_RUNS_LIMIT`）按 runId 去重合并进 runs 视图项，status 原样透传（completed → latestEventKind `run_completed`）。
- `ui/V2_PROTOTYPE/src/pages/AiHomeWorkbench/hooks/useChatMessages.js`：
  - 新增卸载清理（useEffect cleanup）：有活跃会话且有消息时 `sessionRuntimeStore.setSessionMessages(activeSessionKey, messagesRef.current)`——G1 快照此前只在会话切换/迟到回填点写入，离页即丢占位。
  - 重挂载首帧（本地为空且存在快照）以快照参与既有 `reconcileWithBackendMessages` 对账；新增闸门 `hasUnfinishedSnapshotTail`——**仅当快照尾部存在未完成进行中占位（loading/streaming/error/action）才恢复**，已完结快照不恢复（后端为准）。
- `ui/V2_PROTOTYPE/src/pages/AiHomeWorkbench/index.jsx`：
  - completedInBackground 的 run 键由 `run.id` 改为 `run.runId || run.id`（统一视图真实字段是 `runId`；原单键在真实数据下全为 undefined，会把跨 run 状态误判为同一 run 迁移——本次合并近期已完成 run 后必须修正）。
  - 新增「重挂载对账」一次性窗口：重挂载后统一视图 runs 含已完成 run 且本地存在该会话未完成进行中占位（卸载快照）时，触发一次 `loadSessions()` 对账，不依赖卸载前 ref。
- `apps/api/src/modules/harness/workbench-view.usecase.test.ts`（既有文件内新增 3 例）：近期已完成 Run 合并且 status 透传 / 活跃+已完成同时返回 / ownerUserId 透传且 limit ≤ 10。
- `ui/V2_PROTOTYPE/src/__tests__/session-isolation.test.jsx`（既有文件内新增 1 例）：回复未完成时卸载再进入，问题气泡与进行中占位均恢复渲染。
- `ui/V2_PROTOTYPE/src/__tests__/unified-view.test.jsx`（既有文件内新增 1 例 + setup 补计 ai-sessions 调用）：重挂载后 runs 仅含已完成 run 时角标「进行中 0 · 已完成 1」渲染、占位恢复快照渲染、触发一次对账重拉。
- `docs/agent-loop/handoffs/2026-08-10-qoder-ISS-2026-08-10-001.md`：本回填文件（新建）。
- `ui/V2_PROTOTYPE/src/hooks/useSessionRuntimeStore.js`：未改动（仅复用既有 API，与工单口径一致）。

## 验证命令与结果

- `npm run test:web`：**pass**，42 文件 280 用例全过（基线 278 + 新增 2：算式 278+2=280）。
- `npm run test:modules`：**pass**，321 用例全过、0 失败（基线 318 + 新增 3：算式 318+3=321；A 为后端小改，workbench-view 用例已覆盖近期已完成 run 合并）。
- `npm run build:api`：**pass**（tsc 零错误）。
- `npm run build:web`：**pass**（vite build 零错误；chunk >500kB 提示为既有告警，与本批无关）。
- `node skills/improving-wes-ui/scripts/check-ui-scope.mjs --base e589fc2 -- <4 个前端改动文件>`：**pass**，输出 `No new deterministic UI findings.`
- RED 证据（实现前首轮）：后端 3 新例失败（近期已完成合并 ×2 + 隔离透传 ×1）、前端 2 新例失败（卸载/重挂载占位恢复 ×1、重挂载对账触发 ×1），既有 9+13 用例全过；实现后全部转绿。
- 前端前置规范：`npx ui-skills start` 已执行（路由层 + 分类加载）；本批为状态/交互行为修复，无视觉样式改动，最小上下文即 improving-wes-ui 既有质量清单。

## 风险

- **测试缺口（真实浏览器）**：占位恢复与角标计数由 RTL/jsdom 渲染断言覆盖（组件级渲染证据）；真实浏览器 1440px/760px 证据本轮未采集——工单已明确人工验收口径由用户合入后执行（开异步开关发一句问题 → 未完成时切走再返回：问题气泡+进行中占位可见、角标「进行中 ≥1」；完成后返回：完整回复可见、角标「已完成 ≥1」）。
- **跨测试文件污染修复方式**：HomeWorkspace.test.jsx 的 L780/L1856 两用例共用 `session-action` 与同文项目报文，初版「快照全量恢复」会被模块级 store 跨用例残留顶出同文重复消息；已通过 `hasUnfinishedSnapshotTail` 闸门在实现侧收敛（已完结快照不恢复），未改动该文件（不在 Allowed Paths）。语义影响：纯本地已完结消息（如「项目已创建并关联」通知）重挂载后不再复现，与「后端为准、仅保留未完成占位」口径一致。
- **既有候选（未证实、未处置）**：角标「进行中」计数的状态清单（queued/running/recovering/waiting）未含 `cancelling`（后端活跃态含）；`failedRuns` 在生产侧同样只来自活跃查询，failed 为终态永不返回——均为本批范围外观察，建议后续单独立项核实。
- **兼容**：统一视图 runs 仅做加法（active ∪ recentlyCompleted ≤ 10），不删除既有字段；repository 为 additive 接口扩展，无迁移。

## 是否建议看板同步

是。建议页面：

- `issues.html`：ISS-2026-08-10-001（ISS-003 复验残留两处体验缺口）状态推进至「已回填 / 待主会话复审」，链接本回填与提交 ff235bb。
- `testing.html`：登记本批 RED→GREEN 证据链（test:web 280 / test:modules 321 / 双构建零错误 / check-ui-scope 无新发现）与 RED 首轮失败记录。
- `changes.html`：登记提交 ff235bb 与 worktree/分支信息。
- `risks.html`（可选）：登记「cancelling 未计入进行中」「failedRuns 永不返回」两个范围外候选观察。

（按协作协议，看板事实由 Codex/主会话定稿，本回填仅建议。）

## 下一步建议

- **待 Codex 复核**：复核 diff 范围（7 文件全在 Allowed Paths 内）、抽查验证命令实输出；通过后按集成队列最小补丁合入。
- 合入后由用户按工单 §5 人工验收口径执行真机复验（未完成/已完成双路径）。
- 验收通过后由 Codex/user 决定 ISS-2026-08-10-001 是否关闭为「已交付」；本回填不宣称已交付。
