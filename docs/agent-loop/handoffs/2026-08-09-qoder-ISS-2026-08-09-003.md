# Qoder Handoff · ISS-2026-08-09-003（离页返回前端旧缓存渲染、AI 回复不显示——读取侧对账缺陷）

- **状态**：已回填 / 待 Codex 复核
- **工单**：`docs/agent-loop/work-orders/2026-08-09-qoder-ISS-2026-08-09-003-frontend-reconcile.md`

## 目标

修复异步通道离页返回后前端用发送时刻旧缓存出图、AI 回复不显示的读取侧对账缺陷。三处根因对应三项修复（代号均附业务主题）：

- **C1（离页返回旧缓存渲染 · 最小修）**：`loadSessions` 重拉列表后，当前会话仍在列表时换用新拉取的同 id 对象，不再复用发送时刻的旧 `current` 对象。
- **C2（离页返回旧缓存渲染 · 对账修）**：会话切回（存在离页快照时）与页签切回（visibilitychange）触发一次会话数据重拉；G1 快照与后端 messages 对账合并——**后端为准**，仅保留本地仍未完成的进行中尾部（loading/streaming/error/action 及未落库用户消息），后端已应答轮次的过期 loading 占位丢弃；L124 同会话重映射守卫同步细化，不再被残留 loading 无条件阻断。
- **C3（离页返回旧缓存渲染 · 体验修）**：「后台任务」角标接入统一视图 `unifiedView.runs` 活跃/已完成计数（O5 接口已一次取齐 runs）；检测到离页期间完成的 run（活跃→completed 或从活跃列表消失）时触发 C2 对账重拉。

边界遵守：未改后端、未改 SSE 协议、未新增依赖、未引入新组件库/样式体系（角标复用 tokens.css 变量与内联样式）。

## Worktree

- projectRoot: /Users/kevin/AI/Workload-evaluation-system
- worktreePath: /Users/kevin/AI/Workload-evaluation-system/.claude/worktrees/iss-2026-08-09-003-frontend-reconcile
- branch: qoder/iss-2026-08-09-003-frontend-reconcile
- baseCommit: 0c3a6eb
- taskId: ISS-2026-08-09-003

## 变更文件

- `ui/V2_PROTOTYPE/src/hooks/useAiSessions.js`：C1——`loadSessions` 的 `setActiveSession` 改为取新列表同 id 对象（后端最新为准）。
- `ui/V2_PROTOTYPE/src/pages/AiHomeWorkbench/hooks/useChatMessages.js`：C2——新增 `reconcileWithBackendMessages` 对账合并（后端 messages 为准 + 仅保留未完成进行中尾部 + 已应答轮次丢弃过期 loading）；会话切换分支去掉 `storedMessages ||` 短路；存在离页快照的切回触发 `loadSessions` 重拉；新增 visibilitychange 页签切回重拉（loadSessions + refreshUnifiedView）；L124 守卫细化为对账而非无条件 `return prev`。
- `ui/V2_PROTOTYPE/src/pages/AiHomeWorkbench/index.jsx`：C3——侧栏新增「后台任务 进行中 X · 已完成 Y」角标（数据源自 `unifiedView.runs`，计数为 0 时不渲染）；新增 run 状态迁移侦测，离页期间完成的 run 触发 C2 对账重拉。
- `ui/V2_PROTOTYPE/src/__tests__/session-isolation.test.jsx`：RED 回归 2 例（①异步通道完成后切回会话 assistant 回复必须渲染；②会话切换时本地残留 loading 占位不得阻断目标会话消息刷新）；`setupDualSessions` mock 升级为有状态持久化（chat 请求到达即落库用户消息、响应放行追加 assistant、GET 反映最新状态），以支撑切回重拉路径且与真实后端行为一致。
- `ui/V2_PROTOTYPE/src/__tests__/unified-view.test.jsx`：C3 回归 1 例（角标按统一视图 runs 展示活跃/已完成计数）。

未新建测试文件，未动 package.json / package-lock.json（`git diff --stat 0c3a6eb -- apps/ package-lock.json` 零输出实测）。

## 验证命令与结果

- `npm install`（worktree 根 + `ui/V2_PROTOTYPE/` 两步）：均 exit 0。
- RED 先行（实现前）：`npx vitest run src/__tests__/session-isolation.test.jsx src/__tests__/unified-view.test.jsx` → 3 failed / 10 passed，失败恰好为 3 个新用例，失败原因均为预期症状（回复不渲染 / 角标不存在）。
- GREEN 聚焦复跑：同命令 → **13 passed (13)**。
- `npm run test:web`：**278 passed (278)，42 文件全绿**。算式：基线 275 + 新增 3（session-isolation ×2 + unified-view ×1）= 278。中途曾出现 HomeWorkspace 标准草稿用例被「无条件切回重拉」顶掉新会话的回归，已将重拉触发条件细化为「存在离页快照的会话切回」后复跑全绿。
- `npm run test:modules`：**318/318 pass, fail 0**（后端零改动复验；测试中产生的 `config/auth/users.json` 临时测试用户为套件副作用，已还原未提交）。
- `npm run build:api`：pass（tsc 零错误）。
- `npm run build:web`：pass（`✓ built in 778ms`，仅有既有 chunk 体积提示）。
- `git diff --stat 0c3a6eb -- apps/ package-lock.json`：零输出（纯前端批）。
- `node skills/improving-wes-ui/scripts/check-ui-scope.mjs --base 0c3a6eb -- <3 个前端源文件>`：`No new deterministic UI findings.`
- `npx ui-skills start` 前置已执行；按路由层选取 `mattpocock/diagnosing-bugs`（系统性缺陷诊断：先建可复现 pass/fail 回路再修复）作为本批最小 UI 技能上下文，RED 先行流程与其 Phase 1 一致。

## git log --oneline -4（实际输出）

```text
1209e13 fix(WES AI 工作台): ISS-2026-08-09-003（离页返回旧缓存渲染、AI 回复不显示）· 读取侧对账——C1 重拉取新列表同 id 对象 / C2 后端 messages 为准合并 + 切回重拉 / C3 后台任务角标接统一视图 runs 计数
0c3a6eb docs(总看板): C3（异步通道漏带用户问题）合入收官（e14aa12）——ISS-2026-08-09-002 全部疑点关闭，待人工验收真实模型链路
e14aa12 merge(WES AI 工作台): C3（异步通道漏带用户问题）小修复合入主线——意图误判根因修复，独立复审一次通过，用户批准
795b928 docs(agent-loop): C3（异步通道漏带用户问题）小修复 handoff 回填——验证矩阵全绿实测与 modules 计数偏差说明
```

## 风险

- **人工验收缺口（留待用户）**：工单 §5 人工口径未执行——开异步开关发一句问题 → 切走页签 → 等后台完成 → 返回，AI 回复必须可见；「后台任务」角标在离页期间/返回时正确计数。单测以有状态 mock 覆盖同路径，但未在真实模型链路 + 真实浏览器页签切换下验证。
- **视觉证据缺口**：C3 角标为新增视觉元素，行为由测试覆盖，但未做渲染截图核对（本批为行为修复，按 improving-wes-ui step 8 聚焦测试验证；如需视觉确认可补浏览器截图）。
- **重拉放大**：存在离页快照的会话切回、页签切回、run 完成侦测三处都会触发 `loadSessions` 全量列表重拉，频率高于修复前；单次开销与既有首屏加载相同，未引入轮询。
- **对账合并边界**：本地进行中尾部仅保留 loading/streaming/error/action 及其前方未落库用户消息；极端场景（同一会话多轮并发未完成占位）合并粒度为「尾部一段」，中间历史一律以后端为准。

## 是否建议看板同步

是。建议页面：testing（278/278 实测与 3 例新回归资产入档）、changes（本批 C1/C2/C3 读取侧对账变更记录）、sources（ISS-2026-08-09-003 状态由「修复中」推进至「待人工验收」，由 Codex/用户定稿）。

## 下一步建议

- 待 Codex 复核本 handoff 与 diff（5 文件，236 insertions / 16 deletions）。
- 复核通过后按工单 §5 人工验收口径在真实模型链路验收；验收通过再由 Codex/用户决定是否合入主线并关闭 ISS-2026-08-09-003。
