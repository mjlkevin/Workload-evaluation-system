# Qoder Handoff · RP-047-A2-INT

date: 2026-08-03
taskId: RP-047-A2-INT
requirement: RP-047
taskClass: integration-candidate-preparation
executor: qoder1
reviewer: codex
status: `已回填 / 待 Codex 复核`
allowNextTask: `false`
mainMergeAllowed: `false`
nextOwner: `codex`

## 1. Worktree 身份

- projectRoot: `/Users/kevin/AI/Workload-evaluation-system`
- worktreePath: `/Users/kevin/AI/Workload-evaluation-system/.claude/worktrees/rp-047-a2-integration`
- branch: `qoder/rp-047-a2-integration`
- baseCommit: `1cc297d79b3af476c33a418ee73dbbcc7a922df3`（工单 Assignment Commit，等于开始时业务主线 `codex/role-driven-ai-home-workbench` HEAD）
- sourceBranch: `qoder/rp-047-a2-durable-run-foundation`
- sourceCommit: `d9790fe08e62964e0dacfe103b06647af02de81e`
- sourceMergeBase: `7221503f69bee91f1bd1b87baa114577de466a51`
- 最终 HEAD（handoff 提交前）: `521bbd2b7555322fd7b3536d2a3c01b065f09487`
- 最终 HEAD（含本 handoff 提交后）: 见本提交，`git rev-parse HEAD` 可直接读取

基线核验：`git log -1 --format=%H -- docs/agent-loop/work-orders/2026-08-03-qoder-RP-047-A2-INT.md`
输出 `1cc297d79b3af476c33a418ee73dbbcc7a922df3`，且等于 `git rev-parse codex/role-driven-ai-home-workbench`，无 BASELINE_DRIFT。

## 2. 来源链核验

- `git rev-parse qoder/rp-047-a2-durable-run-foundation` = `d9790fe08e62964e0dacfe103b06647af02de81e`，与工单固定值一致。
- `git log --reverse 7221503..d9790fe` 精确返回 8 个提交；`git diff --name-status 7221503..d9790fe` 精确返回 16 个 A2 文件。
- A1 `qoder/rp-047-a-durable-run-foundation @ fae8a7d` 未以任何方式使用，仅保持只读存在。

## 3. Cherry-pick 记录（工单第 9 节固定顺序）

| # | 源提交 | 集成后提交 | 结果 |
|---|---|---|---|
| 1 | `c94f9da` 冻结持久运行状态契约 | `7f03e48` | clean |
| 2 | `0483224` 建立持久运行数据库结构 | `a4eb5e7` | clean（harness.usecase.test.ts / harness.routes.test.ts auto-merge） |
| 3 | `a16527e` 原子队列创建与租约认领 | `162c6eb` | clean |
| 4 | `312d33e` 检查点与事件幂等原语 | `21c4fa0` | clean |
| 5 | `748af1a` 测试容器启动与历史迁移证明 | `e2d30ff` | clean（apps/api/package.json auto-merge） |
| 6 | `dc93bb3` 迁移演练兼容构建类型检查 | `ebbd8b9` | clean |
| 7 | `39d5ea9` A2 结构化 handoff | `58566fd` | clean |
| 8 | `d9790fe` 普通对象边界 + output 并发回归 | `521bbd2` | clean |

8 个 cherry-pick 全部一次成功，无人工冲突。三处 auto-merge 全部落在 Allowed Paths 内：
`apps/api/package.json`、`apps/api/src/modules/harness/harness.usecase.test.ts`、
`apps/api/src/routes/harness.routes.test.ts`。这 3 个文件同时被主线分叉后提交修改过，
auto-merge 同时保留了主线新增事实与 A2 新增内容，处理依据为工单第 9 节冲突规则第 2、3 条。

完整性补充核验：

- 8 个 A2 新增文件（migration SQL、0014 snapshot、5 个 harness-runtime 源/测试文件、A2 handoff）
  与源提交 `d9790fe` 逐一 `git diff --quiet` 比对，全部 IDENTICAL。
- `apps/api/drizzle/meta/_journal.json`、`apps/api/src/db/schema/harness.ts`、
  `apps/api/src/modules/harness/harness.module.ts`、`harness.types.ts`、`test-setup.mts`
  在主线分叉后未被主线修改，cherry-pick 结果即为 A2 版本。

## 4. Changed Files 与 Allowed Paths 对比

`git diff --name-only 1cc297d..HEAD | sort` 精确输出以下 16 个路径，全部属于工单第 7 节
前 16 个 Allowed Paths，无超范围文件。第 17 个路径为本 handoff 文件，单独提交。

```text
apps/api/drizzle/0014_talented_deathstrike.sql          (A)
apps/api/drizzle/meta/0014_snapshot.json                (A)
apps/api/drizzle/meta/_journal.json                     (M)
apps/api/package.json                                   (M)
apps/api/src/db/schema/harness.ts                       (M)
apps/api/src/modules/harness/harness-runtime.migration.test.ts   (A)
apps/api/src/modules/harness/harness-runtime.repository.test.ts  (A)
apps/api/src/modules/harness/harness-runtime.repository.ts       (A)
apps/api/src/modules/harness/harness-runtime.types.test.ts       (A)
apps/api/src/modules/harness/harness-runtime.types.ts            (A)
apps/api/src/modules/harness/harness.module.ts          (M)
apps/api/src/modules/harness/harness.types.ts           (M)
apps/api/src/modules/harness/harness.usecase.test.ts    (M)
apps/api/src/routes/harness.routes.test.ts              (M)
apps/api/test-setup.mts                                 (M)
docs/agent-loop/handoffs/2026-08-03-qoder-RP-047-A2.md  (A)
docs/agent-loop/handoffs/2026-08-03-qoder-RP-047-A2-INT.md (本提交新增)
```

`config/auth/users.json`、`config/versions/records.json` 未出现在集成分支任何提交或工作区；
主 checkout 的这两个既有运行态修改与 3 个 untracked 看板草稿文件均保持原样未动。

## 5. 验证命令与结果（工单第 10、11 节全量）

### 5.1 依赖与 Docker 预检（第 10 节）

| 命令 | 结果 |
|---|---|
| `npm ci` | pass（exit 0） |
| `npm ci --prefix ui/V2_PROTOTYPE` | pass（exit 0） |
| `git diff --exit-code -- package-lock.json ui/V2_PROTOTYPE/package-lock.json` | pass，两个 lockfile 无变化 |
| `node -p 'require("./apps/api/node_modules/ajv/package.json").version'` | `8.20.0` |
| `test -x ui/V2_PROTOTYPE/node_modules/.bin/vite` | pass |
| `docker info` / `docker context show` | pass，context 为 `colima`，未做任何机器级配置 |

### 5.2 Harness 真实 PostgreSQL 门禁（第 11.1 节）

```bash
RP047_DOCKER_HOST="$(docker context inspect "$(docker context show)" --format '{{.Endpoints.docker.Host}}')"
DOCKER_HOST="$RP047_DOCKER_HOST" \
TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock \
USE_TESTCONTAINERS=true npm run test:harness -w apps/api
```

结果：`tests 95 / pass 95 / fail 0 / skipped 0`，exit 0。
migration 使用随机临时 database；运行结束时输出
`[test:setup] Stopping PostgreSQL container...` 与 `[test:setup] Container stopped`。

### 5.3 主线集成回归（第 11.2 节）

| 命令 | 结果 |
|---|---|
| `npm run test:modules` | pass，227/227，0 fail / 0 skip |
| `npm run test:ai` | pass，190/190，0 fail / 0 skip |
| `npm run test:integration` | pass，1/1 |
| `npm run test:web` | pass，29 文件 / 206 用例全通过 |
| `npm run build:api` | pass（tsc exit 0） |
| `npm run build:web` | pass；仅既有 chunk-size warning（与 R1 Gate 记录一致） |
| `npm run test:security` | pass；`[secret-scan] no tracked secret fields found` |

测试数量以本次集成态主线实际套件为准，未沿用 A2 旧基线 143 项硬编码。

### 5.4 范围与清洁度（第 11.3 节）

| 检查 | 结果 |
|---|---|
| `git diff --check 1cc297d..HEAD` | pass，无空白错误 |
| `git diff --exit-code 1cc297d..HEAD -- package-lock.json ui/V2_PROTOTYPE/package-lock.json` | pass，无变化 |
| `git diff --name-only 1cc297d..HEAD` | 精确等于第 4 节 16 个路径 |
| `git status --short --untracked-files=all`（handoff 提交后） | clean |
| `docker ps --format '{{.Names}}'` | 仅 `rp047-pg17`（既有，未停止未删除）；无本轮新增长期容器 |
| `config/system/requirement-settings.json` 副作用 | 本轮 test:modules 未产生任何工作区副作用，无需恢复 |

### 5.5 Migration 边界

- `0014_talented_deathstrike.sql`：22 条 `CREATE TABLE/INDEX`，其余全部为 `ADD COLUMN` /
  `ADD CONSTRAINT`；无 DROP、TRUNCATE、DELETE 等 destructive SQL，属纯 additive migration。
- Testcontainers 随机数据库随容器停止清理；未连接 `rp047-pg17` 或任何长期数据库。

## 6. 边界声明

- 未编辑主 checkout，未 stage/修改主 checkout 任何文件。
- 未使用 A1 `fae8a7d`；未执行整分支 merge、rebase、squash、amend、force push。
- 未安装或重配 Homebrew、Colima、Docker context、daemon、代理、DNS 或 shell profile。
- 未接触真实 API Key、token、cookie、私钥或用户业务数据。
- 未开发 Worker、Recovery Coordinator、异步 Run API、SSE 或前端多会话（Batch B-E 保持锁定）。
- 未修改总看板，未领取 Batch B，未宣布 RP-047 已交付。

## 7. 风险与未验证项

1. 本轮为集成候选移植验证，未执行真实生产数据库迁移演练；`0014` 只在 Testcontainers
   随机库上证明向前兼容，生产迁移演练属 Batch E 范围。
2. 三个 auto-merge 文件虽经全量测试验证，Codex 审计时建议逐文件复核主线新增内容与
   A2 新增内容的并存正确性（尤其 `apps/api/package.json` 的 scripts/deps 合并段）。
3. Worker、Recovery Coordinator、异步 API、前端恢复均未实现；本候选不提供任何用户可见行为。
4. 交付状态由 Codex Gate 与用户验收决定，本 handoff 不代表 RP-047 交付。

## 8. 状态与下一步

- status: `已回填 / 待 Codex 复核`
- allowNextTask: `false`
- mainMergeAllowed: `false`
- nextOwner: `codex`（按工单第 14 节执行独立审计：复核 17 路径边界、复跑全量验证、
  发布 `ACCEPTED_FOR_MAINLINE_INTEGRATION` / `REWORK_REQUIRED` / `REJECTED` /
  `USER_DECISION_REQUIRED`）
