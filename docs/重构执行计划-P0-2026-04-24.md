# 重构执行计划 · P0 阶段（2026-04-24）

> 本文档是 **P0 阶段重构的执行台账**：记录目标、范围、进度、决策、TBD 与接手指引。
> **目标读者**：继续推进该重构的下一位 AI 或工程师。
> **与 v2 设计方案的关系**：v2 HTML（`需求深度分析-重构方案-v2-2026-04-22.html`）描述**目标态架构**，本文档描述**迁移路径与当前进度**。两者互补。

---

## 0. 快速接手指引（三步法）

新 AI / 工程师接手时，**按顺序**读：

1. **先读本文档** —— 知道做什么、做到哪、决策是什么。
2. **再读 v2 HTML 基线**（位置见 §2）—— 知道最终目标长什么样。
3. **最后 `git log --oneline` 看最近提交**（尤其前缀 `feat(T1)`/`feat(T2)`/`feat(db)`）—— 验证本文档与代码是否一致。

本文档**每次完成一个 T 任务后必须更新**（§4 状态矩阵、§6 文件地图、§9 变更记录）。

---

## 1. 背景与动机

2026 年 03-04 月初版 `apps/api/src/services/ai.service.ts` 膨胀到 **2541 行**，承载了 Kimi API 调用、Excel 解析、企业画像抽取、规则回退、评估草稿生成等全部 AI 相关逻辑。直接问题：

- **单文件难以测试**：业务规则与 HTTP 细节混在一起，无法独立 mock。
- **AI 厂商锁定**：Kimi endpoint / payload 硬编码在各业务函数里，换 provider 需要动几十处。
- **证据链断裂**：AI 输出直接进入 ExcelRow，人工修改无法追溯；规则降级理由散落在 try/catch 文案里。
- **与 v2 设计不兼容**：v2 提出"7 角色责任接力"、"DSL 完备性检查"、"证据驱动的评审"，这些能力无法在现有单体结构上叠加。

P0 的目标：**搭建能承接 v2 能力的基座**，不破坏现有功能。

---

## 2. v2 基线文档

**文件**：[`docs/需求深度分析-重构方案-v2-2026-04-22.html`](需求深度分析-重构方案-v2-2026-04-22.html)（2026-04-24 入库，1211 行）

**样本来源**：真实项目 `OPP202509190408 · 利民星空旗舰版`（2025-11-04 拉群 → 2026-02-11 评估封版，14 人协同群）。所有角色画像与流程节点可追溯到该样本。

**13 节结构**：

| # | 节 | 本 P0 相关 | 涉及任务 |
|---|---|---|---|
| 01 | 核心判断（3 根本错位） | — | 背景参考 |
| 02 | 产品原则（P-1 ~ P-5） | ✅ 强相关 | T2（P-1 AI Suggests Human Decides）、T6（P-5 规则皆 DSL） |
| 03 | **用户画像（7 角色）** | ✅ 强相关 | T7 |
| 04 | 业务流程图（8 阶段） | — | 上层流程参考 |
| 05 | **接力子流程（5a/5b/5c/5d）** | ⚠️ 部分 | P1 实体 AssessmentHandoff |
| 06 | 系统架构图 | ✅ 参考 | 本次 P0 落实 L5（PG）、LAI（Kimi）、D1-D7 之 Evidence |
| 07 | 业务 × 系统切入点（矩阵） | — | 参考 |
| 08 | **核心领域实体**（18 个） | ✅ 部分 | T2 Evidence 是其中一个 |
| 09 | DSL 与规则引擎（双引擎） | ⚠️ 部分 | T6（只做完备性引擎一条规则） |
| 10 | 用户故事（30 条） | — | 验收参考 |
| 11 | **关键设计决策（15 条 D-1~D-15）** | ✅ 强相关 | §5 关键决策台账须对标 |
| 12 | 落地路线图（P0-P3） | ✅ 强相关 | 本文档 P0 范围是 v2 路线图 P0-1/P0-3 的首批交付 |
| 13 | **开放问题 TBD（9 条）** | ✅ 全录入 §7 | — |

### 2.1 v2 路线图 P0（共 3 项，本次只啃其中一部分）

- **P0-1 数据层升级**：JSON → Postgres，并补齐 v2 新实体（SOW / Narrative / Deliverable / Handoff / RateCard / QualityGate / SealedBaseline / DevAssessment / CollabWorkspace / Methodology）
  - **本次 P0 只做**：PG 基座 + `evidences` 表；其余 9 个新实体留待 P0.2/P1
- **P0-2 规则引擎**：双引擎 + DSL + 灰度 + 命中追溯
  - **本次 P0 只做**：完备性引擎雏形 + 1 条规则（T6）；计算引擎重构、灰度、命中追溯均留待 P0.2
- **P0-3 Kimi Agent 基座**：ModelProvider + 长文抽取 + 证据链 + 置信度
  - **本次 P0 覆盖**：T1（ModelProvider）、T2（证据链契约）、T3（长文抽取器）、T4（迁移旧代码）；置信度算法留待 P0.2

> 📌 本文档所述 P0 **≈ v2 路线图 P0-1/P0-3 的首批交付 + P0-2 雏形**。精确命名可称为 **P0.1**，保留 P0.2 做剩余工作。

### 2.2 v2 产品原则与本次 P0 的对应关系

| 原则 | 本次 P0 体现 |
|---|---|
| P-1 AI Suggests, Human Decides | Evidence.history 保留覆盖轨迹；`confidence` 字段支撑 |
| P-2 责任接力 | ❌ P0 不做，留待 P1（AssessmentHandoff 实体） |
| P-3 角色分层交互 | ❌ P0 不做（不动前端） |
| P-4 证据不入库只留结论 | ✅ Evidence.rawText 存段落摘录；不存原文件 |
| P-5 规则皆 DSL | ⚠️ T6 搭雏形，但只一条规则 |

---

## 3. P0 范围边界

### ✅ 做

1. **AI 基座抽象层**：`ModelProvider` 接口 + `KimiProvider` 实现 + 注册表。
2. **证据链契约**：`Evidence` / `ExtractionResult` 类型 + JSON Schema。
3. **长文抽取器**：从 Excel / 文本产出 `ExtractionResult`。
4. **渐进迁移**：旧 `ai.service.ts` 调用点改走新 Provider，但保留外部接口签名。
5. **持久化升级**：引入 Postgres + Drizzle ORM，`evidences` / `extraction_results` / `change_logs` 进 PG。
6. **DSL 引擎雏形**：实现一条"SOW 完备性"规则作为概念验证。
7. **7 角色 RBAC**：角色常量 + 权限矩阵（仅后端约束，不动前端 UI）。
8. **冒烟测试**：IA 初评一条 happy path 走通 Vitest。

### ❌ 不做（推迟到 P1+）

- 不动前端 UI（V0_SAAS）。
- 不迁旧 JSON stores（users / versions / templates / system configs）。
- 不接入第二个 AI provider（OpenAI / 通义 etc.）。
- 不引入 BullMQ / 任务队列。
- 不实现完整 DSL 规则集（只做 1 条）。

---

## 4. P0 任务矩阵

| 编号 | 任务 | 状态 | Commit | 交付物 | 验收 |
|---|---|---|---|---|---|
| **T1** | AI Provider 基座抽象层 | ✅ 已完成 | [`948451a`](../../commit/948451a) | `apps/api/src/ai/provider/*` (errors / model-provider / kimi-provider / registry / index) | TS 通过、行为与旧 `requestKimiCompletion` 一致 |
| **T2** | Evidence / ExtractionResult 契约 | ✅ 已完成 | [`93212a6`](../../commit/93212a6) | `apps/api/src/ai/evidence/*` (types / schema / index) | TS 通过、JSON Schema draft-07 合法 |
| **DB** | Postgres + Drizzle 基座 | ✅ 已完成 | [`e50281c`](../../commit/e50281c) | `apps/api/src/db/*`、`apps/api/drizzle/*`、`.env.example` | `workload_eval` + `workload_eval_test` 两库均有 `evidences` 表 |
| **T3** | 长文抽取器 `RequirementExtractor` | ✅ 已完成 | (本次提交) | `apps/api/src/ai/extractor/*` (types / ai-extractor / rule-extractor / requirement-extractor / index / requirement-extractor.test) + `npm run test:ai` | 输入 `workbookText` → `ExtractionResult`；8 条用例覆盖 AI 成功 / AI 部分缺字段 / ProviderError 整体降级 / JSON 解析失败 / Provider 不可用 / disableAi / 无 Provider / 规则路径缺字段 |
| **T4** | 迁移旧 `ai.service.ts` 使用新 Provider | ✅ 已完成 | (本次提交) | `apps/api/src/ai/bootstrap.ts` + `bootstrap.test.ts`；`ai.service.ts` 4 个 Kimi 调用点（`parseRequirementImportByKimi` / `summarizeCompanyProfileByKimi` / `chatWithKimi` / `generateAssessmentDraftByKimi`）改走 `defaultProviderRegistry.get("kimi")` | ✅ Kimi HTTP 底座（`requestKimiCompletion` / `fetchKimiCompletionOnce` / `buildKimiRequestError` / `sleep` / `isFetchAbortError`）整体删除；`ai.service.ts` 2541 → 2408 行（-133）。**注**：30% 瘦身目标未达 —— 剩余体量主要是规范化/handler 业务逻辑（非 T4 范围），P0.2 再决定是否拆分；现有 `parse-requirement-workbook.test.ts` 2/2 通过、TS 仅历史 3 错、bootstrap 3 测试 + extractor 8 测试全绿 |
| **T5** | Evidence / change_log 持久化 | ⏳ 待做 | — | `extraction_results` 表 + `change_logs` 表 + `EvidenceRepository` | 插入/按 extractionId 查询/追加 history 三个路径；`workload_eval_test` 集成测试通过 |
| **T6** | DSL 引擎雏形 + 1 条规则 | ⏳ 待做 | — | `apps/api/src/dsl/*` + 规则 `sow-completeness-v1` | 规则语法可声明"必填证据字段集合"，对缺失字段产出 `RuleViolation` |
| **T7** | 7 角色 RBAC 常量 + 权限矩阵 | ⏳ 待做 | — | `apps/api/src/rbac/*` 常量 + `permissions.ts` 矩阵 | 后端中间件能按角色拦截路由，7 个角色 × N 个能力位全覆盖 |
| **T8** | IA 初评冒烟测试（Vitest） | ⏳ 待做 | — | `apps/api/src/ai/extractor/__tests__/ia-initial-smoke.test.ts` | Vitest 跑通一条完整路径：上传 Excel → 抽取 → 证据入库 → DSL 校验 → 角色拦截 |

**依赖关系**：T1→T2→T3（AI 抽取）；T2→DB→T5（落盘）；T2→T6（DSL 消费证据）；T1-T7 皆完成后 T8 冒烟。

---

## 5. 关键设计决策台账

> 原则：**每项决策必须有理由、有时间；有替代方案者写明为何不选**。

| ID | 日期 | 决策 | 理由 | 替代方案 |
|---|---|---|---|---|
| D-01 | 2026-04-24 | **Provider 接口内置重试/超时，非调用方责任** | 与旧 `requestKimiCompletion` 一致，避免每个业务点重复写重试 | 暴露裸 `fetch` 让调用方自行处理（放弃：违反"调用方只管业务"原则） |
| D-02 | 2026-04-24 | **ProviderError 保留 `legacyReason` 字段** | `toFriendlyFallbackReason` 旧文案还在用，改造需渐进 | 直接断掉兼容性（放弃：T4 一次性迁移风险过大） |
| D-03 | 2026-04-24 | **Evidence.history 追加而非替换** | v2 强调"不可抵赖"，人工修改不能抹除 AI 原始证据 | 用 change_logs 表记录（仍保留，但 history 字段提供快速可见性） |
| D-04 | 2026-04-24 | **confidence 三档而非自由浮点** | 规则=1.0、人工=1.0、AI=模型自报；简化 DSL 阈值决策 | AI 用 logprobs 计算置信度（放弃：Kimi 未必稳定暴露） |
| D-05 | 2026-04-24 | **引入 Postgres**（推翻最初"P0 继续用 JSON"约束） | 证据链的 relational join 在 JSON 下几乎无法做；本机已装 Postgres.app 无额外部署成本 | 继续 JSON（放弃：T5 change_log 查询能力不足） |
| D-06 | 2026-04-24 | **Drizzle ORM**，非 Prisma / TypeORM / raw `pg` | TS-first、零 codegen 运行时、SQL 可见、CJS 兼容好 | Prisma（放弃：CJS 有坑、codegen 重）；raw `pg`（放弃：repository 维护成本高） |
| D-07 | 2026-04-24 | **渐进迁移**（P0 只新增表，旧 JSON 不动） | 一次性全迁阻塞 T5、风险大；users/versions 的 schema 需要单独设计 | 全量迁移（放弃：工作量翻倍） |
| D-08 | 2026-04-24 | **Schema 约定**：uuid 主键、snake_case、timestamptz、jsonb、硬删 | PG 主流实践；uuid 与现有 `randomUUID()` 直接对应；jsonb 容纳联合类型 | bigserial / camelCase / 软删（放弃：见下方注） |
| D-09 | 2026-04-24 | **迁移手动触发**，不在应用启动时自动跑 | 生产部署需要显式审批；开发机也便于回滚 | 启动自动 migrate（放弃：一次手滑全库变更） |
| D-10 | 2026-04-24 | **测试策略**：独立 `workload_eval_test` + 事务回滚 | 事务回滚比 TRUNCATE 快；独立库避免污染 dev 数据 | testcontainers（推迟到 CI） |

**注（D-08）**：
- 不用 bigserial：uuid 可在应用层生成，不依赖 DB 单调序号，未来可分库。
- 不用 camelCase：PG 默认会把 camelCase 转小写，除非加双引号；统一 snake_case 省心。
- 不用 deleted_at 软删：P0 简化；审计由 change_logs 表承担。

---

## 6. 文件地图（代码新增位置）

```
apps/api/
├── src/
│   ├── ai/
│   │   ├── provider/               ← T1：模型调用抽象层
│   │   │   ├── errors.ts           ProviderError 统一错误模型（9 个错误码）
│   │   │   ├── model-provider.ts   ModelProvider 接口 + 请求/响应类型
│   │   │   ├── kimi-provider.ts    KimiProvider 实现（含重试/超时/温度兼容）
│   │   │   ├── registry.ts         ProviderRegistry + defaultProviderRegistry
│   │   │   └── index.ts            barrel export
│   │   ├── evidence/               ← T2：证据链契约
│   │   │   ├── types.ts            Evidence / ExtractionResult / EvidenceSource
│   │   │   ├── schema.ts           JSON Schema draft-07
│   │   │   └── index.ts
│   │   ├── extractor/              ← T3：长文抽取器（✅ 已建）
│   │   │   ├── types.ts            ExtractRequest / ExtractOptions / ExtractDependencies
│   │   │   ├── ai-extractor.ts     AI 路径（Provider → JSON → Evidence[]）
│   │   │   ├── rule-extractor.ts   规则路径（正则 over workbookText, RULE_ID=basic-v1）
│   │   │   ├── requirement-extractor.ts  Orchestrator（whole-result fallback）
│   │   │   ├── requirement-extractor.test.ts  8 条用例
│   │   │   └── index.ts
│   │   ├── bootstrap.ts            ← T4：启动期注册 KimiProvider 到 defaultProviderRegistry
│   │   └── bootstrap.test.ts       3 条用例（注册 / 幂等 / 缺 key 不崩）
│   ├── db/                         ← DB 基座
│   │   ├── client.ts               Pool + drizzle 单例
│   │   ├── migrate.ts              手动迁移执行器（tsx src/db/migrate.ts）
│   │   └── schema/
│   │       ├── index.ts            barrel（drizzle-kit 扫描入口）
│   │       └── evidences.ts        evidences 表定义
│   ├── dsl/                        ← T6：DSL 引擎（待建）
│   ├── rbac/                       ← T7：权限（待建）
│   └── services/
│       └── ai.service.ts           T4 后：4 处 Kimi 调用点全部改走 provider；
│                                   旧 HTTP 底座（requestKimiCompletion 等）已删；
│                                   2541 → 2408 行（-133）
└── drizzle/                        ← Drizzle 生成的 SQL migration
    ├── 0000_spooky_vulcan.sql      evidences 初始建表
    └── meta/_journal.json
```

**旧持久化（保留不动，P1 才考虑迁移）**：
- `config/auth/users.json`、`config/auth/invite-codes.json`
- `config/versions/records.json`、`config/versions/version-code-rules.json`
- `config/system/*.json`

---

## 7. 未决事项（TBD）

### 7.1 P0 阶段内（本次重构执行相关）

| ID | 问题 | 归属任务 | 建议决策者 |
|---|---|---|---|
| ~~TBD-P0-T3-01~~ | ~~Extractor 降级字段粒度策略~~ | T3 | ✅ T3 落地时采用**整体兜底**（whole-result fallback）：AI 抛 ProviderError 或 JSON 解析空 → 整路径切到规则。字段级合并留待 P0.2/T4 与既有 `mergeRequirementImportData` 行为对齐时再决；当前不阻塞。 |
| TBD-P0-T5-01 | `change_logs` 表保留 **diff（JSON patch）** 还是 **全量快照**？影响存储量与审计精度 | T5 | 方案内部决策 |
| TBD-P0-T7-01 | 7 角色**权限矩阵**能力位清单；已从 §7.2 TBD-V2 裁决落地部分（PMO 审核/驳回、PRE_SALES+IMPL 写 SOW、ADMIN 模板管理），剩余位（Evidence 读写 / Extractor 触发 / 规则管理 …）待对齐 | T7 | 对照 v2 §11 D-1~D-15 + 与 user 对齐 |
| TBD-P0-T8-01 | 冒烟测试是否上 testcontainers？当前本地 PG 可用但 CI 未定 | T8 | 待 CI 方案 |
| ~~TBD-P0-DOC-03~~ | ~~TBD-V2-04 A/B 路径二选一~~ | — | ✅ 2026-04-24 user 拍板走**路径 B**；已销案 |
| TBD-P0-DOC-04 | TBD-V2-04 遗留：PMO checklist 中"差异分析表"指售前 `BusinessGapAnalysis`、PM `VarianceAnalysis`，还是两份都要？ | P1 PMO 状态机 | user（P1 PMO 建模前） |

**7 角色已从 v2 §03 落地**（T7 常量直接用这 7 个，能力位待定）：

| Code | 角色 | 样本出处 | P0 关键能力位（草案） |
|---|---|---|---|
| `SALES` | 销售（含新客户 + 老客户经营两个子场景） | 张锐 / 黄金艳 · 客户成功业务二部 | 发起商机、读自己的评估包、发起合同 |
| `PRE_SALES` | 售前顾问 | 郭俊贤 · 业财解决方案与项目经理部 | 上传原始物料、触发 Extractor、维护需求包 |
| `IMPL` | 实施顾问（第一轮评估） | 林悦填 · 业财项目交付部 | 创建初评版本、拉开发顾问、写假设 |
| `PM` | PM / 接力者（签约前整合封装） | 李加柱 · 业财解决方案与项目经理部 | 从初评接力、改人天/系数、生成 3 大交付物 + Narrative |
| `DEV` | 开发顾问（被拉进来评开发项） | 林宏果 · 专门开发部门 | 只读被分配的开发条目、写开发人天 |
| `PMO` | PMO / 质量门 | 魏春芳 · 项目管理部 | 审核交付物、驳回 / 通过、封版前校验 |
| `ADMIN` | 总部产品 / 管理员 | 郑工 · 总部 | 改 DSL 规则、灰度、改 RateCard、改方法论 |

### 7.2 v2 基线自带（2026-04-24 user 已全部给出裁决口径）

> v2 原话：「以下 9 条是讨论未完全覆盖的点，v2 已做占位设计，正式落地前需补齐。」
> **2026-04-24 user 裁决**（含当日二次澄清 + 三次路径选型）：**9 条全部已决**（TBD-V2-04 最终走路径 B · 两表并存）。裁决回填到各条 `裁决` 列。
>
> **ID 映射**：本文 `TBD-V2-NN` 对应 v2 HTML 原文 `TBD-N`（例如 `TBD-V2-01` = v2 原文 `TBD-1`）。

| ID | 问题 | user 裁决（2026-04-24） | P0 / 后续影响 |
|---|---|---|---|
| TBD-V2-01 | 方法论**七阶段**具体是哪 7 段？ | ✅ **已决**：**① 启动 → ② 需求 → ③ 方案（蓝图）→ ④ 构建 → ⑤ 测试（UAT）→ ⑥ 上线 → ⑦ 验收**。补齐的是 **⑥ 上线**（不是维保/切换）。 | v2 §08 `ProjectMethodology.phases[7]` 可直接落位；T6 DSL 可启用"七阶段齐全"校验；P1 PMO 审核 checklist 以此 7 段为基线。 |
| TBD-V2-02 | **PMO 权责边界** | PMO **不能否决整个评估**；只看"**交付物规范度 + 齐全性**"。无时效、无"有条件通过"。 | T7 权限矩阵：PMO 仅配"审核 / 驳回"位；`QualityGateReview` 状态机 P1 做。 |
| TBD-V2-03 | **实施顾问 → PM 接力触发条件** | **人为发起** —— 触发源是"该商机被任命项目经理"的**人事事件**（李加柱 2026-02-11 被任命后入群）。 | P1 `AssessmentHandoff` 按"人为发起 + 任命事件"建模，无自动触发器；P0 **不实现**。 |
| TBD-V2-04 | **差异分析表基准** | ✅ **已决 · 走路径 B（两表并存）**（2026-04-24 最终裁决）：<br/>• **售前侧**：新增 `BusinessGapAnalysis` 实体（质性 gap 文档 · 《业务需求与差异分析表》），归属 PRE_SALES；**独立交付物，不进 PM 4 件套**<br/>• **PM 侧**：保留 v2 §08 `VarianceAnalysis`（数字差异表）+ **4 选项 baseline 全部保留**（InitialEstimate / BidBaseline / HistoricalAvg / CustomerBudget），继续作 `Deliverable` 4 件套之一 | **P0 不受影响**（Evidence 当前契约无 baseline 字段）。P1 新增 `BusinessGapAnalysis` + 保留 `VarianceAnalysis`；v2 §08 已摘除 VarianceAnalysis 上 TBD-4 标。**遗留问题**：PMO checklist 中"差异分析表"指售前那份 / PM 那份 / 两份都要，P1 PMO 状态机细化时再确认。 |
| TBD-V2-05 | **决策冲突仲裁** | **无仲裁、无"保留意见"字段**。最终人天由当前接手顾问单独决定，过程走线下沟通，不入系统。 | T5 `change_logs` 只存"谁 / 何时 / 改了什么 / 为何"四元，不新增异议字段。 |
| TBD-V2-06 | **RateCard 多维度** | ✅ **已决（二次确认）**：只按**职级**一维（顾问成本表，user 将后续提供）；**无地区 / 无员工性质维度**。地区走独立路径：顾问 BASE 地为基准，项目地 ≠ BASE 地时触发**差旅成本**（新实体 `TravelCost`，独立于 RateCard）。 | P1 `RateCard`（职级 → 单价）+ P1 `TravelCost` 独立演化；P0 不做，但**设计空间已最终收敛**。 |
| TBD-V2-07 | **SOW 维护责任与变更流程** | 责任链：**售前顾问起草 → 实施顾问（PM）最终修订审核**。反向生成**部分可以** —— 评估包可派生 *组织范围 / 模块范围 / 开发范围 / WBS*，其余人工补充。 | T6 DSL "SOW 完备性"规则有了明确字段边界（前 4 项必填结构化，其余不校验）；T7 权限：写 = PRE_SALES + IMPL，只读 = SALES/PM/PMO。 |
| TBD-V2-08 | **Narrative 模板全司统一？** | **做成可配置化**。不绑定李加柱的"五段式"为标准，由 ADMIN 维护模板（可按行业/组织定制）。 | P1 `NarrativeTemplate` + ADMIN 工作台 CRUD；**T7 ADMIN 角色需预留"模板管理"权限位**；P0 不做。 |
| TBD-V2-09 | **开发顾问工时占用可视化** | **要做，但最后做**。排到 **P2+** 兜底。 | 不影响 P0 / P1 设计与数据结构。 |

**P0 决策**（2026-04-24 三轮裁决后最终更新）：
- **原 `TBD-P0-DOC-02`（七阶段第 7 段待复核）已销案** —— user 补齐"上线"后完整七阶段已锁（见 TBD-V2-01）。
- **原 `TBD-P0-DOC-03`（TBD-V2-04 A/B 路径待选）已销案** —— user 选定路径 B，两实体并存建模已定。
- **T6 DSL `sow-completeness-v1` 规则**：字段边界从 TBD-V2-07 的裁决中明确（前 4 项校验、其余提示），范围缩小；作为 P0 范围内任务保留。
- **T7 权限矩阵**：PRE_SALES/IMPL 的 SOW 写权限、ADMIN 的"模板管理"位、PMO 的"审核+驳回"二位，都已从 TBD-V2-02/07/08 的裁决中落地，可直接进草案。
- **v2 架构收敛已完成**：
  - TBD-V2-01 ProjectMethodology 七阶段锁定（启动/需求/方案/构建/测试/上线/验收）
  - TBD-V2-04 走路径 B —— 新增 `BusinessGapAnalysis` + 保留 `VarianceAnalysis`（4 baseline 保留）
  - TBD-V2-06 RateCard 简化为一维 + 新增 `TravelCost` 独立实体
  - §08 实体清单净增 `BusinessGapAnalysis` / `TravelCost` **2 枚新实体**（P1 建模），P0.2 / P1 总计 v2 新实体数从 9 个调整为 **11 个**

---

## 8. 本地环境与命令速查

### 环境

- **PG**：Postgres.app 18.3，监听 `localhost:5432`
- **两个库**：`workload_eval`（dev）、`workload_eval_test`（vitest）
- **owner**：当前系统用户（本机 `kevin`，部署到他机时要调整）
- **连接串**：`postgres://kevin@localhost:5432/workload_eval`（写在 `apps/api/.env.local`，不 commit）

### 常用命令

```bash
# 开发启动（和以前一样）
npm run dev:api                                          # 后端
npm run dev:web                                          # 前端

# DB 迁移工作流
npm run db:generate -w apps/api                          # 改 schema 后，生成 SQL migration
npm run db:migrate   -w apps/api                         # 应用到开发库 workload_eval
DATABASE_URL=postgres://kevin@localhost:5432/workload_eval_test \
  npm run db:migrate -w apps/api                         # 应用到测试库
npm run db:studio    -w apps/api                         # 可视化表 & 数据（drizzle-kit）

# 测试
npm run test:modules  -w apps/api                        # 既有单元/用例/handler
npm run test:rules    -w apps/api
npm run test:integration -w apps/api

# 类型检查
cd apps/api && npx tsc --noEmit                          # 三个 exports.module 报错是历史遗留，忽略
```

### 新机器冷启动清单

1. 装 PG 18.x（Postgres.app 或 brew）
2. `createdb workload_eval && createdb workload_eval_test`
3. `cp apps/api/.env.example apps/api/.env.local`，填 `DATABASE_URL`
4. `npm install`
5. 对两库各跑一次 `npm run db:migrate`

---

## 9. 变更记录

| 日期 | 变更 | 作者 |
|---|---|---|
| 2026-04-24 | 本文档首版。T1/T2/DB 基座完成，T3-T8 待做 | Opus 4.7（会话内） |
| 2026-04-24 | v2 HTML 入库；§2 补 13 节结构表、v2 路线图 P0 对应关系、产品原则映射；§7 清理 TBD-P0-DOC-01，补录 7 角色名录与 TBD-V2-01 ~ TBD-V2-09 全部内容 | Opus 4.7（会话内） |
| 2026-04-24 | user 给出 9 条 TBD 裁决；§7.2 由"待补齐表"改为"已裁决表"（含 P0 影响列）；§7.1 新增 TBD-P0-DOC-02（七阶段第 7 段待复核）；v2 HTML §13 每条 tbd-item 回填绿色裁决卡（TBD-1 橙色半决） | Opus 4.7（会话内） |
| 2026-04-24 | user 二次澄清：TBD-V2-01 七阶段补齐"上线"全决、TBD-V2-06 二次确认；TBD-V2-04 user 反问 "4 选项 baseline 是什么"，已解释并升级为 A/B 路径选型（半决）。§7.1 销案 DOC-02，新增 DOC-03 跟踪 A/B；v2 HTML 三条裁决卡同步更新 + 顶部 snapshot 换文案 | Opus 4.7（会话内） |
| 2026-04-24 | user 第三轮裁决：**TBD-V2-04 最终走路径 B**（两表并存 · 新增 `BusinessGapAnalysis` + 保留 `VarianceAnalysis` 含 4 baseline）。§7.1 销案 DOC-03、新增 DOC-04（PMO checklist 归属）；§7.2 TBD-V2-04 改全决；§5 追加 v2 架构最终收敛条目（v2 新实体从 9 个 → 11 个）。v2 HTML 同步：TBD-4 卡由半决改全决、§08 新增 `BusinessGapAnalysis` / `TravelCost` 两枚实体条目、摘除 VarianceAnalysis/RateCard/ProjectMethodology 上 TBD 标、US-33 改写、顶部 snapshot 改为"9 条全部已决（TBD-4 走路径 B）" | Opus 4.7（会话内） |
| 2026-04-24 | **T3 RequirementExtractor 完成**：`apps/api/src/ai/extractor/*` 5 个源文件 + 1 个测试文件；`npm run test:ai` 8 条用例全绿；包含 AI 成功、AI 部分缺字段、ProviderError 整体降级、JSON 解析失败、Provider 不可用、disableAi、无 Provider 注入、规则路径缺字段共 8 路径覆盖。降级粒度采用 whole-result（销案 TBD-P0-T3-01）。§4 状态矩阵 T3→✅，§6 文件地图填充，§7.1 销案 TBD-P0-T3-01 | Opus 4.7（会话内） |
| 2026-04-24 | **T4 ai.service 迁移到新 Provider 完成**：新增 `apps/api/src/ai/bootstrap.ts` + `bootstrap.test.ts`（3 条用例）；`createApp()` 入口注入 `bootstrapAiProviders()`（幂等）；`ai.service.ts` 四处 Kimi 调用全部改走 `provider.chatCompletion(...)` + `credentialsOverride`；删除旧 HTTP 底座（`requestKimiCompletion` / `fetchKimiCompletionOnce` / `buildKimiRequestError` / `sleep` / `isFetchAbortError`）共 ~85 行；`ai.service.ts` 2541→2408 行（-133，未达 30% 目标，剩余主要是非 T4 范围的归一化与 handler 逻辑）。`npm run test:ai` 11/11、`test:rules` 8/8、`parse-requirement-workbook.test.ts` 2/2 全绿。 | Opus 4.7（会话内） |

---

## 附录 A：旧 `ai.service.ts` 调用点锚定

供 T4 迁移时对照（基于 2026-04-24 时的行号；若有新提交可能漂移）：

| 行号 | 原函数 | 迁移后走 |
|---|---|---|
| L487-593 | `requestKimiCompletion` / `fetchKimiCompletionOnce` / `buildKimiRequestError` | 已整合到 `KimiProvider`（保留，T4 删除） |
| L595-676 | `parseRequirementImportByKimi` | 调用 `provider.chatCompletion` + 改走 `extractor`（T3） |
| L745-903 | `summarizeCompanyProfileByKimi` | 调用 `provider.chatCompletion`，保留业务逻辑在 `ai.service` |
| L905-969 | `chatWithKimi` | 调用 `provider.chatCompletion`（注意这个旧实现没用 `requestKimiCompletion`，只做单次 fetch） |
| L1628-1696 | `generateAssessmentDraftByKimi` | 调用 `provider.chatCompletion` |

---

## 附录 B：PG Schema 演进规则

> 未来 T5 及之后所有改 schema 的流程

1. 改 `apps/api/src/db/schema/*.ts`
2. `npm run db:generate -w apps/api` → 生成 `drizzle/NNNN_xxx.sql`
3. **肉眼 review 生成的 SQL**（drizzle-kit 不完美，偶有 DROP/CREATE 歧义）
4. `npm run db:migrate -w apps/api` 应用到开发库
5. `DATABASE_URL=... npm run db:migrate` 应用到测试库
6. 生成的 SQL + meta 文件 **必须** commit，和代码变更放同一个 PR
7. 不允许手动修改已合并的历史 migration；需要改就新增一条 migration 补丁
